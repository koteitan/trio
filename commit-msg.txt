move the normalized sheet to tmp/fixed-sheet and untrack it

- Relocate bms-rathjen/ to tmp/fixed-sheet/ and add tmp/ to .gitignore
- normalize_sheet.py writes there (creating the directory) and now also
  regenerates tools/omega_alpha_rows.tsv, so the whole chain is one command
  from the xlsx (output verified byte-identical)
- Replace the links to the now-untracked copy in README.md and
  ebp2bms-1{,-en}.md with a pointer to the script
