v0.71.0/W hierarchy: definitions and acc_of_W

- Add lean/Wset.lean (port of the yapss W-hierarchy) with the trio subscript
  encoding `lev = 2*row1 + row2` (lex on (row1,row2) has order type omega
  because row2 < 2), so the hierarchy stays indexed by naturals
- `domT`/`graft`/`based`/`natDom` over the srow-parent, `oper_eq_graft_nil_of_domT`
- `lfpS`, the operator `Aop`/`Aset`, the stage family `Wf`, `W`, A1/A2/A2'
- `acc_of_W`: cofinality turns W-membership into accessibility
- `wf_of_cofinality_and_membership`
- Register Wset in lakefile roots
