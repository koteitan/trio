v0.118.29/self-support of the equipment class is closed under the machine

- Add `SelfSup M v z` (every planted prefix of an equipped context is in `GX`)
  and prove the three closure lemmas: `selfSup_take`, `selfSup_graft` (the
  prefix splits into the context side or "planted context + datum mask", via
  the new instance-level `liftPlant_of_plant`), `selfSup_ltail`
- Factor `liftPlant_of_mask` through `liftPlant_of_plant`, which takes the
  single plant instance instead of the whole `CorePlantCtxLift` core
- These are the closure rules any future construction of a self-supporting
  equipment class will need
