v0.118.45/write down the remaining ctxInf_cross recipe

- GRAFTALL-PLAN.md 1.9.38: the seven-step recipe for the one remaining case of
  `ctxInf_graft`, including the reason `gx_graft` has to be strengthened to
  `E.dropLast in GXs` and the warning that a general `entry A i q` lemma is the
  wrong decomposition (only the three q = 0 entries and the `A.take` form are
  needed)
