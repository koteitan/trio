v0.114.1/split the lifted-plant core and record the new architecture

- Split CoreLiftPlant into CoreLift (GX is closed under the intrinsic
  row-1 lift) and CorePlantCtx (the context's planted peel is in GX),
  wired by coreLiftPlant_of and GX_loop'
- Record in GRAFTALL-PLAN 1.9.10: the graft-closure design, the
  probe_liftplant refutation (the composite lift acts through the coneV
  mask, not the argument's own Lift1), the gamma' reduction sketch
  (gcopies = graft E (gcopies n)) and the unified residue picture
  (context pieces in GX + lift closure)
- Revise the execution order and task.md to the new residue list
