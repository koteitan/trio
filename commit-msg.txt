v0.118.166/prove that oper is monotone in the copy count

- oper_prefix_of_le: M[n'] is a prefix of M[n] whenever n' <= n.  Both keep the
  untouched head M.take j0, and the copies are a flatMap over List.range, which
  grows by appending; the Pred branches are equal outright
- W_oper_mono: membership at a large copy count gives it at every smaller one,
  via W_take
