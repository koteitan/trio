v0.114.2/probe the lift residue and record its exact shape

- Add the four probes behind GRAFTALL-PLAN 1.9.11: probe_liftplant
  (composite lift acts by the coneV mask, not the argument's Lift1),
  probe_plift (both masks are lift-stable, so both lifts compose),
  probe_tower2lift (tower-2 family lift = one more Lift1, 0/21408;
  the row-1 tow analogue is false), probe_lowroot (at every tower site
  the datum's root sits above the ambient root, so the mask part never
  trivializes)
- Record 1.9.11: CoreLiftPlant = (lifted planted context peel) and
  (plift closure of GX); alpha and beta converge on one core
