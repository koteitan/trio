v0.118.42/record the design consequence of the graft case of CtxInf

- GRAFTALL-PLAN.md 1.9.37: the crossing case of `ctxInf_graft` reduces, via
  `shiftl0_seg_graft` + `slift_graft`, to a `GX` obligation for a STAIRCASE LIFT
  of the datum's prefix, so `gx_graft` must be strengthened from
  `E.dropLast in GX` to `E.dropLast in GXs`, and with it the surviving core to
  its staircase-closed form — the implementation cost of v0.119
