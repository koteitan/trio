v0.118.10/(A1): the expansion keeps the prefix's amin

- Add `rtg0_take_iff` and `amin_take` (the ancestor minimum is prefix-local)
- Prove `amin_oper_prefix`: for `i < M.length - 1` the expansion `M[n]` has the
  same `amin` at `i` as `M`, in every branch (via `oper_take_prefix`)
