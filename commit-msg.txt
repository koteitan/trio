v0.65.0/bad_A1 closed and bad_A2 assembled

- Add `gexp_guard_mod`: guard transport keyed by the position modulus
- Add `argDomCoreOn_bad_A1`: descend the whole instance one period, apply
  the copy-count IH, lift back through the guarded shift
- Prove the two marked columns carry the same copy guard (strict spine
  kills the (T,F) gap), so the descended pair keeps `f`
- Add `argDomCoreOn_bad_A2`: `inner` forces `ipos <= j0`, which leaves only
  the `desc` and `root` branches
