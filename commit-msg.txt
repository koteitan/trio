v0.118.177/carry the index-mask vs value-mask picture into RESIDUE-PROBLEM

- Add RESIDUE-PROBLEM 4.9: the single difference from 2-row Bashicu is that a
  row-2 collapse lifts row 1 of the copies, and the lift BM4 performs is masked
  by an INDEX cone while every proved lift law is masked by row-1 VALUES
- State the measured split: Lift1 commutes with oper in all 93231 instances
  where the bad root is interior, and fails in 47718 of the bad-root-at-0 ones
- State that the gap is exactly row-1 ties, that tie-freeness holds on ST_TS but
  is not implied by the proved invariants, and that the residue itself needs no
  ST_TS, so the residue is what to attack
