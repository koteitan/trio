v0.118.74/prove the easy half of the cone transport for j0 >= 1

- Add `le1_gexp_low`: on the head `M.take j0` of a copies block the cone from
  index 0 is unchanged, by `List.take_left'` and the prefix-locality of `le1`
- GRAFTALL-PLAN.md 1.9.60: mark that half done and point at the copies part as
  the remaining difficulty
