v0.118.53/the lift quantifier drops out of the GX core

- Add `ctxOK_ltail_self`: the equipment class is closed under the ambient root
  lift, so `CtxOK M v z` gives `CtxOK (ltail v z M t) (v+t) z`
- Add `CorePlantCtx0` (lift-free: `(0,v,z) :: M.dropLast ∈ GX`) with
  `corePlantCtxLift_of_plant0` / `plant0_of_corePlantCtxLift` — the two cores
  are equivalent, so `∀ t` was redundant
- Add `TRIO_terminates_of_plant0` / `no_infinite_expansion_of_plant0` in
  Final.lean; axioms stay [propext, Classical.choice, Quot.sound]
- GRAFTALL-PLAN.md 1.9.43 + task.md: residue is now `CorePlantCtx0` +
  `InfEquip`, neither carrying a lift quantifier
