v0.118.36/trio termination rests on exactly two context cores

- Parameterise `mem_of_Aclosed_aux` and the whole `W_membership` chain by a set
  `S` with `S subset Wstar` and its A-closedness, replacing the `TowerOK`
  argument that was only ever used for `Wstar_closed`
- Add `Wstar2_le_Wstar` / `Wstar2s_le_Wstar`
- Wire the old track through `S := Wstar` so `TRIO_terminates` is unchanged
- Add `wf_olt_ST_TS_of_cores`, `TRIO_terminates_of_cores` and
  `no_infinite_expansion_of_cores` in Final.lean, running the chain through
  `S := Wstar2s`: `CoreCtxSuffixLift -> CorePlantCtxLift -> WellFounded stepRel`,
  axioms [propext, Classical.choice, Quot.sound]
- Record the wiring in GRAFTALL-PLAN.md 1.9.33 and task.md
