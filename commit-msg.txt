v0.118.22/map the common bottom of the four cores

- GRAFTALL-PLAN 1.9.24: every remaining core bottoms out in the same step —
  the equipment `CtxOK` yields `in W a`, and lifting that to `in GX` is the
  self-reference (unusable inside the machine, and outside it the peel's GXs
  component has no equipment)
- Record that `CoreBlocked0` looks like an artifact of `GX`'s `2 <= M.length`:
  `(0,v,z) :: graft (M.take 1) C = graft ((0,v,z) :: M.take 1) C`
