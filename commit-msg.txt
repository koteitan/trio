v0.53.0/cross branch covers the aligned case as well

- Relax caseC_absurd's mirror-source condition from qj < i to qj <= i
- Relax the tower wrapper to jpos - Lb <= ipos with j0 < ipos explicit,
  so it also empties case (b) whenever the shallower column is strictly
  inside the block (probe: all 6270 aligned instances have ipos = j0)
