Omega arithmetic: the leaf rule and the upgrade suffix

- Name a level in a unit's leaf with one column, whose row-1 entry is that
  of M(v)'s last column, and keep the spelling rule for collapse arguments
- Append suffix(v), the trailing z1 run of M(v), when such a leaf ends the
  matrix, so Omega_w and Omega_1 stop sharing a matrix (the upgrade effect)
- Find the base's last add unit by its z1 root instead of a z1 pair, and
  put the next anchor one x further when that root carries no children
- Drop the refusal to write a limit level: every sheet row now builds
- Check that the matching rows come out in the sheet's row order (603
  rows, 0 order violations) and record the rules and the 204 open rows
