v0.79.0/drop the natDom guard from the second A-clause

- Remove `natDom M` from `Aop` clause 2: `acc_of_W` never consumed it, so the
  ungated `∀ n ≥ 1, M⟦n⟧ ∈ X` is sound and strictly more permissive
- This dissolves the z=1 obstruction: a block whose trailing orphan is a dead
  root (`domT M m` with `m ≥ lev (root M)`, unreachable by clause 3) satisfies
  `M⟦n⟧ = M.dropLast` and is now certified by clause 2
- Remove the unused `tbAll z m` hypothesis from `Aop` clause 3
- Adjust `Aop_mono_X`, `acc_of_W`, `W_shift`, `XA_closed`, `Om_mem_W` to the
  new clause shapes
