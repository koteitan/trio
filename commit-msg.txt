v0.118.57/soundness probe for the single remaining core

- Add tools/probe_cap.py: `W 0` is the hereditarily-terminating set, so
  termination of the capped composite is a necessary condition for `CoreCap`;
  15552 instances (including c = 2, outside the z<2 fragment) show 0 violations
- GRAFTALL-PLAN.md 1.9.47: record the result and its scope limits (the CtxOK
  approximation does not bite at this size, the n-schedule is not exhaustive,
  the stage is not checked)
