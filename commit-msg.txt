v0.118.67/(WL) shrinks to the parented case

- Add `lift_oper_of_noParent`: the two `Pred` branches of `oper` commute with
  the root lift, since `Lift1` preserves row 2, `srow` and `hasParent`
- Add `Lift1_of_length_one`, `LiftStageParented` and
  `liftStage_of_parented : LiftStageParented → LiftStage`: clause 1 and the
  one-column roots are stage arithmetic, clause 3 forces `¬ hasParent`, so only
  clause 2 with a parented last column survives
- Add `TRIO_terminates_of_parented` / `no_infinite_expansion_of_parented`;
  axioms [propext, Classical.choice, Quot.sound]
- GRAFTALL-PLAN.md 1.9.55 + task.md: the residue is now
  `LiftStageParented` + `TowerExp`, and by measurement the parented case only
  needs `j0 = 0` with collapse row <= 1
