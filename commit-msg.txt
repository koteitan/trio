say why subunits are multiplication: spell out the value of one unit

- Add the identity omega^{beta_i} = omega * prod_j omega^{omega^{gamma_ij}}
  to both parts: a sum inside the exponent is a product of values, so one
  subunit is one factor and the embedding carries that factor's gamma
- Restate the summary as "number of units = addition, number of subunits =
  multiplication, shape of the embedding = exponentiation"
