v0.66.0/assemble the guarded bad branch

- Add `argDomCoreOn_bad_guard`: strong induction on the copy count with the
  three-way positional split into bad_B / bad_A2 / bad_A1
- Update task.md: the guarded route is closed, the uniform route is next
