v0.63.0/positional criterion for SpineOK, case (a) work in progress

- Add spineOK_of_pos: a take/drop slice satisfies SpineOK as soon as the
  positional statement holds on it
- Park the half-finished descending branch of case A2 in wip-A2desc.txt
  (values, descended column and instance decomposition done; the A2' head
  bound, the descended spine and the transport remain)
