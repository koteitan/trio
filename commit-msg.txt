v0.118.12/(G2): the staircase lift commutes with the expansion

- Add `slift_oper` (Aexp.lean): `slift (A[n]) phi = (slift A phi)[n]`,
  unconditional in `A` and `n`, sorry-free
- Add `slift_oper_main` for the gexp branch: prefix columns by (A1), copies by
  (A2), the `le1` guard by `le1_slift`, `d1` by `amin_le1` on the row-2 parent
- Add `slift_take` / `slift_dropLast` (Pred branches) and `oper_zero_take`
  (`X[0]` is the truncation at the bad root)
- Complete the staircase calculus: (G2) joins (G3) `slift_graft`,
  (G5) `slift_slift`, (G6) `amin_slift`
