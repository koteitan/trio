v0.118.6/the staircase lift distributes over grafts (Cgraft.lean)

- Add `capV M` (the row-1 minimum over the graft point's strict context
  ancestors) with `siteV_iff_capV`
- Prove `amin_graft_low` / `amin_graft_high_eq`: the composite's parameter is
  the argument's own `amin` capped by `capV M`
- Prove `slift_graft`: the staircase lift distributes over a graft, with the
  argument's staircase cut at `capV M` — and `stair_cap` shows the cut is again
  a staircase, so the language is closed under the graft recursion
- Add `nat_eq_of_lt_iff` (a threshold characterisation determines the value)
