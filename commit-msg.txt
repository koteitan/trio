v0.117.0/planting a root descends the stage

- Add om_Aop: the bare planted root's Aop datum sits at stage 2v+z-1,
  strictly below the root's own level
- Add probe_pliftseg, which refutes the "mask lift = piecewise uniform
  row-1 shift" decomposition (the mask interleaves by row-1 value), and
  record in GRAFTALL-PLAN 1.9.15 that the mask is still recursive along
  the graft decomposition, whose base is the planted root
- Record that every residue funnels into "one stage lower W is in GX",
  a descending self-reference (unlike the old one at stage m+2t)
