v0.94.0/the row-2 tower core is proved

- Add `Wstar2`: `W*` closed under the guarded row-1 lift `Lift1`
- Prove `towerGraft2_holds` (the former open core `TowerGraft2`): induction on
  the copy count with the lift amount universally quantified, so the graft
  closure is consumed only at the single stage `m`
