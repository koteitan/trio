v0.50.0/guarded-lift transport of sle

- Add gliftAt: pointwise guarded lift with guards read at absolute
  positions (+ nil/cons/length lemmas)
- Add sle_cons_cons: head-splitting form of sle
- Add sle_gliftAt: the lift preserves sle given the hkey interface
  (guard implication at indices with equal row 0 and non-exceeding
  row 1); the (F,T) mismatch wins early, (T,F) is what hkey excludes
