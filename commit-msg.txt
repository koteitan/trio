v0.118.39/the (IL) law: the ambient mask lift restricts to a segment

- Add `nextrel0_shiftl0` / `rtg0_shiftl0` / `coneV_shiftl0` / `shiftl0_mlift`:
  re-basing row 0 leaves row-0 ancestry, the cone and the mask lift alone
- Add `le0_seg_iff` (row-0 ancestry inside a segment is the host's, translated)
  and `amin_seg`: under a window chain, `amin R (p+q) = min (amin (seg …) q)
  (amin R p)` — an ancestor of `p+q` is either inside the segment or an ancestor
  of `p` itself
- Add `coneV_seg` and `seg_mlift`, the measured (IL) law: `seg (mlift R v t) p
  (k+1)` is `mlift (seg R p (k+1)) v t` when the segment root is in the cone and
  the plain segment otherwise
- Add `ltail_eq_mlift`, `shiftl0_seg_mlift` and `shiftl0_seg_ltail`, the
  re-based forms the context component `CtxInf` will consume
