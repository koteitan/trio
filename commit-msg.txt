v0.118.50/the tower and the copies block both collapse onto a generated family

- Add `gpow` (the generated context family) and `graft_iter_gpow`: any family
  built by `f 0 = []`, `f (n+1) = graft E (f n)` satisfies
  `graft (gpow N E k) (f n) = (gpow N E (k+n)).dropLast`
- Instantiate it for the row-1 tower (`graft_tow_gpow`) and for the blocked
  copies block with `d1 = 0` (`graft_gcopies_gpow`)
- Add `graft_tow_succ` and `tower1_mem2_gpow`: the row-1 tower needs only
  "the generated family peels are W-packages" — no `forall y in W m` interface,
  hence no stage raise, and no `GX`
- Record the reduction in GRAFTALL-PLAN.md 1.9.41 with the next step
