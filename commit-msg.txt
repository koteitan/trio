v0.75.0/head and depth preservation for the W hierarchy

- Add `entry_zero_headD`, `oper_headD`, `oper_head_eq`, `entry_pair_mem`
- Add `oper_mem_ge` and `graft_mem_ge` (neither `oper` nor `graft` produces a
  column shallower than the block's shallowest one)
- Add `graft_head_eq` (grafting a based block keeps the anchoring depth)
