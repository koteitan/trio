v0.118.180/replace the (WL) core by (ROW1MONO), a statement with no mask in it

- Add Wtower2.Row1Mono and liftStage_of_row1mono: since Lift1 X d is the uniform
  shift shiftr01 0 d X with row 1 lowered outside the root cone, and the uniform
  shift is proved (ulift_mem_W), (WL) follows from W being closed under lowering
  row 1 -- the index-mask vs value-mask obstruction disappears from the statement
- Add Final.TRIO_terminates_of_row1mono and no_infinite_expansion_of_row1mono,
  axioms [propext, Classical.choice, Quot.sound]
- Add tools/probe_row1mono.py: 369068 multi-column, 106763 single-column and
  773483 adversarial instances (lowering at the bad root, its le1-cone and the
  terminal), 0 violations
- Record the closed route-C gate in PROOF-STATUS 5.7: the take/drop-rebase/oper
  closure stays tie-free (72561, 0) but the proved invariant cnf does not exclude
  ties (25 counterexamples), so no local invariant was available
