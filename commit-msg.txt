v0.57.0/generalise the cross-branch refutation

- Rename caseC_absurd to cross_absurd and generalise it to an arbitrary
  copy multiplier k, taking the copy-1 root bound as a hypothesis and
  allowing the mirror source anywhere in the block
- Adapt the tower wrapper to derive the root bound and call it at k = 1
