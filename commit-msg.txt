v0.118.58/the stage hierarchy is carried by the singletons alone

- Add `aop_clause3_to_clause2`: for length >= 2 a `domT` sequence's expansion
  is its peel, which clause 3 already supplies at `z = []`, so clause 3 is
  absorbed by clause 2 there; `oper` is the identity at length 1, so the roots
  `[(0,v,z)]` enter `W` only through clause 3 (`Om_mem_W`)
- GRAFTALL-PLAN.md 1.9.48: record that the stage hierarchy is generated by the
  root singletons, matching the reduction of the residue to `CoreSingleton`,
  plus the negative facts (W-elements can carry high-level dead orphans, so
  `tbAll` is not a W-property; level-bounded classes are not expansion-closed)
