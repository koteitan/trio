Omega arithmetic: psi with a cardinal index, and level-column coverage

- Write psi_{Omega_u}(X) as M(Omega_u) with its leaf dropped one level and
  the argument below it, instead of opening a new add unit
- Order such a collapse just below Omega_u (an ordinal index puts psi just
  above), which is what the sheet's row order asks for
- Measure how far the context covers M(v) by its level columns rather than
  by a plain staircase of anchors, so a limit subscript is copied whole
- 703 of the sheet's 813 rows now agree (was 681), with the matching rows
  back in the sheet's row order (the wrong psi order showed up as 14
  violations before the fix)
