v0.89.0/row-1 shift equivariance of oper

- Add the `shiftr01 0 d` twins of the row-0 shift package
  (entry0/1, nextrel0/1/2, le0/le1, nextR, hasParent, parent, lev,
  srow under `lev != 0`, gcopy/gcopies, Pred)
- Prove `oper_shiftr1`: `oper` commutes with a uniform row-1 shift
  exactly when the last column has positive level
- Add `entry_out_row`: entries past the end are zero
