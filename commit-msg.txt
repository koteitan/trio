v0.118.15/GXs: the datum-side lift core is gone from the machine loop

- Add `GXs = {y | y in GX and every staircase lift of y is in GX}`, closed
  under `slift` by definition (`gxs_slift` via `slift_slift` + `stair_comp`)
- Run the machine over `GXs`: `GX_closed` now consumes `Aop W u GXs Y`, and
  `CoreT1L` consumes `Y.dropLast in GXs`, so alpha reads its mask lift straight
  off the datum (`gxs_mlift`) — `coreT1L_of_plantctx : CorePlantCtxLift -> CoreT1L`
- Add `aop_slift`: the staircase transport of `Aop` for length >= 2 — clause 1
  by `Stair.zero`, clause 2 by `slift_oper` (G2), clause 3 collapsing to
  clause 2 through `domT`'s `¬ hasParent`
- Add `CoreStairOm` (the planted-root singleton, the only surviving hole in the
  transport) and `GXs_closed` / `W_le_GXs` / `W_le_GX`
- Retire `CoreLift` / `CoreLiftPlant` / `CoreMaskLift` from the loop: the
  residues are now CoreCtxSuffix, CoreBlockedEltHi, CoreBlocked0,
  CorePlantCtxLift, CoreStairOm (`GX_loop_pieces`)
