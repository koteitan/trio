v0.118.41/the staircase-closed context component CtxInf

- Add `CtxInf`: every re-based window-infix of the context that avoids the
  trailing column, and every staircase lift of it, is a planted `W`-package
- Prove `ctxInf_take` and `ctxInf_ltail`; the lifted context is where the naive
  root-lift form fails, and `Croot.seg_mlift` plus `slift_slift` / `stair_comp`
  carry it through
- Add `stair_id`, `slift_id` and `seg_take`
- Prove `infEquip_at_of_ctxInf`: `InfEquip`'s content is exactly the `phi = id`
  slice of `CtxInf`, so wiring `CtxInf` into GX's context condition will
  discharge `InfEquip`
