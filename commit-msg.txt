english comments for the remaining converter tools; stop clobbering the row table

- Translate the comments and messages of probe_omega_alpha.py,
  normalize_sheet.py and render_examples.py (the Japanese label data in
  render_examples stays, it is the text the Japanese pages print)
- probe_omega_alpha.py wrote tools/omega_alpha_rows.tsv from the raw sheet,
  silently undoing the format normalization; it now dumps to tmp/pure-rows.tsv
  and the docstring says which script owns the shared table
