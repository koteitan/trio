v0.118.5/the staircase lift in Lean (amin, Stair, slift)

- Define `amin A j` (the row-1 minimum along j's row-0 ancestor chain) and
  prove `coneV_iff_amin`: the ambient mask is exactly `amin > v`
- Define the staircase class `Stair` (phi m - m nondecreasing, phi m >= m) with
  `stair_step` (one step = a mask lift), `stair_comp` (closed under
  composition) and `Stair.mono`
- Define `slift A phi` (lift column j by `phi (amin A j) - amin A j`) with its
  entry lemmas and `nextrel0_slift` / `rtg0_slift`
- Prove the two structural laws: `amin_slift` (the parameter transforms by phi)
  and `slift_slift` (staircase lifts compose), plus `mlift_eq_slift`
- Import Mathlib.Data.Nat.Lattice for sInf on naturals
