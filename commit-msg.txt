v0.118.9/measure the amin copy-periodicity, the last law for (G2)

- Add tools/probe_aminexp.py: over 21693 expansions the prefix keeps its `amin`
  (A1) and every copy inherits its source column's `amin` (A2), with no
  violations — so (G2) reduces to exactly these two
- Record in GRAFTALL-PLAN 1.9.20 the Lean shape of `oper` and which pieces are
  already proven (branch data and both guards), leaving (A1)/(A2)
