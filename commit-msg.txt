v0.76.0/prefix closure and the Omega blocks of the W hierarchy

- Add `XA_closed` and `W_add` (W_u is closed under top-level concatenation)
- Add `graft_Om`, `domT_Om`, `Om_mem_W`: the single column `(0,v,z)` lands in
  the stage `2*v + z`
- Add `Wstar`, in the level form that carries the prefix-wise orphan bound
