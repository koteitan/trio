v0.118.185/add the generation test that tells a lever from a restatement

- Record in PROOF-STATUS 5.7 that the two unconditionally free classes are
  row 2 identically 0 (zeroRow2_mem_Wself) and row 0 identically 0 (flat_mem_W)
- State the test a candidate monotonicity has to pass: it is a lever only if the
  free classes do not generate everything under it -- raising row 2 and changing
  row 0 both do, hence both are equivalent to the theorem, while lowering row 1
  moves neither row 0 nor row 2 and so cannot leave a free class
