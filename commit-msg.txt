v0.118.60/an elementary characterisation of W a

- Add Wchar.lean: `oper_of_length_one`, `oper_mem_of_mem`, `mem_of_oper_mem`,
  `mem_iff_oper_mem` (length >= 2: membership is exactly "every expansion is a
  member"), `lev_le_of_mem_of_length_one`, `singleton_mem_W`, `mem_iff_lev_le`
  (length 1: membership is exactly "level <= stage")
- Move `aop_clause3_to_clause2` from Lind.lean to Wchar.lean and import it
- Consequence: `W a` is "the expansion tree closes on roots of level <= a",
  so the stage is only a bound on the roots the tree may reach — this is what
  probe_cap2.py implements
