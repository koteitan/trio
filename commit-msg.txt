v0.118.131/make the adversarial probe finishable

- Lower the sample count to 60000 and print progress every 10000 samples; the
  400000-sample run was killed by its own timeout before printing anything
- Record the measured 30000-sample result in the docstring
