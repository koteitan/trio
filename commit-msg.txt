v0.118.154/document the context available at the residue call sites

- Add RESIDUE-PROBLEM 4.5: the prefix package and the one-step Aop datum that
  subst1g_of_revive and end_subst_of_revive hold when they invoke the residue,
  so a proof may use them; passing them through is a small Lean change
- Verify that every Lean name cited in PROOF-STATUS and RESIDUE-PROBLEM exists,
  and that oper's copy range excludes the trailing column and copies row 2
  verbatim, which is what snoc_zeroRow2 rests on
