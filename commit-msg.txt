v0.118.62/the mask lift obeys the exact stage law, core-free

- Add `slift_mem_W_tight`: a staircase that never lifts by more than `d` raises
  the stage by exactly `2*d`, and `mlift_mem_W` as its instance — the data-side
  lift closure now comes from `W` itself, with no core
- GRAFTALL-PLAN.md 1.9.51: localise the remaining (WL) gap by measurement —
  `Lift1` commutes with `oper` except when the bad root's parent is index 0 and
  the collapse row is <= 1; the row-2 case (the one the tower branch needs)
  commutes in all 1512 instances
- Record the refuted repair `(Lift1 X d)[n] = slift (X[n]) phi_v` (false at
  9072/12312 for v=0 and 10368/24624 for v>=1)
