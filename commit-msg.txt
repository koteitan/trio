v0.118.24/the v0.119 design: everything based is in GX by length induction

- GRAFTALL-PLAN 1.9.26: `y in GX` is a CONDITIONAL statement (equipped contexts
  only) and `CtxOK` is where the strength sits, so a length induction proving
  "every based sequence is in GX" does not prove termination for free
- Spell out how `hin` (the expansion) is assembled from strictly shorter pieces
  in each bad-root case: towers via `tow_mem_GX` + (ML), blocks via
  `gx_graft` + `gcopies_mem_GX` over the strictly shorter window
- Name the circularity to break: `gx_of_pieces` needs cores stated over
  arbitrary-length contexts, so the induction must run on the composite length
  (or lexicographically on context/datum length)
- Record the measured cost of dissolving `CoreBlocked0` by relaxing `GX` to
  `1 <= M.length`: 12 errors, 11 mechanical, one real (the 1+1 degenerate case)
