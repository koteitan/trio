v0.118.1/the general mask form of the graft lift calculus

- Add the general cone transfer `cone_graft_mask`: with no single-tree
  hypothesis, the composite's root cone meets the argument in the ambient
  mask `coneV A v` (guarded by `SiteHigh E`), and `lift_graft_mask` turns it
  into `Lift1 (graft E A) d = graft (Lift1 E d) (mlift A v d)`
- Add `mlift` (ambient-threshold mask lift) with its entry lemmas, and the
  row-0 chain machinery it needs (`exists_root_anc`, `nextrel0_graft_site`,
  `rtg0_graft_split` / `rtg0_graft_join`, `rtg0_graft_le`, `rtg0_graft_embed`)
- Factor the pointwise assembly into `lift_graft_of_entries`, shared by the
  single-tree calculus and the mask calculus
- Add tools/probe_maskcalc.py (0/200000; mask differs from the argument's own
  cone in 131887 cases) and tools/probe_mliftgraft.py (0/200000: the mask lift
  distributes over grafts at a CONSTANT threshold, which `Lift1` never did)
- Record the consequence in GRAFTALL-PLAN 1.9.18: alpha's residue splits into
  the lifted planted context and `mlift Y v t` in GX; beta's lift residue is
  covered by the single-tree calculus
