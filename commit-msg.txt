v0.118.40/the general staircase (IL) law and the v0.119 recipe

- Add `amin_shiftl0` / `shiftl0_slift`, and `seg_slift`: a staircase lift
  restricts to a segment as the segment's own staircase lift, with the
  staircase capped at the segment root's ancestor-minimum (`stair_cap`)
- Add the re-based forms `shiftl0_seg_slift` and `shiftl0_seg_ltail`
- Write the v0.119 recipe into GRAFTALL-PLAN.md 1.9.36: the exact shape of the
  context component `CtxInf` (staircase-closed — the naive root-lift form is
  refuted), its take / ltail / graft propagation, and its supply from a
  `Wstar2i` induction that inherits the infixes from `R[1]` exactly as the
  prefixes are inherited
