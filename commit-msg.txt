Omega arithmetic: a storey between the columns of one digit

- Give block an optional context so it appends to the matrix being built,
  which lets a storey be laid between two summands of one digit as well
- Move the add unit's digit loop onto that context (Ctx tracks the last
  storey's start and the level the last leaf named)
- 626 of the sheet's 813 rows now agree (was 623), the matching rows keep
  the sheet's row order and the alpha collisions stay at 16
