v0.118.55/the residue loses GX: a pure W-level cap statement

- Add `cap` / `graft_singleton_eq_cap`: grafting a one-column datum only
  replaces the context's last subscript
- Add `CoreCap` with `coreSingleton_of_cap` / `cap_of_coreSingleton`, so the
  `GX`-level singleton core is exactly the `W`-level cap statement
- Add `TRIO_terminates_of_cap` / `no_infinite_expansion_of_cap`; both residues
  (`CoreCap`, `InfEquip`) are now `GX`-free, axioms unchanged
- GRAFTALL-PLAN.md 1.9.45 + task.md: record the closed loop
  CoreCap -> machine -> CoreCap and stop the quantifier-reshuffling track
