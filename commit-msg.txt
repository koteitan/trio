v0.118.181/refute the naive induction for (ROW1MONO) and record what is really needed

- Refute the domination lemma that a straight A2' induction would need: for all
  n there is n' with M'[n] <=1 (M[n']).take -- false in 26396 of 296121 cases,
  because lowering row 1 at the terminal turns an orphan strip into a copy, so
  M'[n] can be longer than every M[n']
- Record the sandwich that isolates what is actually required: only the middle of
  Lift1 (X[n]) d <=1 (Lift1 X d)[n] <=1 shiftr01 0 d (X[n]) is missing, both ends
  are already in W, and Lift1 and shiftr01 0 d preserve nextrel1 so the branch
  data j0/Lb/d0/d1 coincide -- a masked lowering suffices, weaker than (ROW1MONO)
