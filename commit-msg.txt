v0.118.72/identify the remaining (WL) branch as the tower graft recursion

- GRAFTALL-PLAN.md 1.9.59: with `i1 = 1` the deltas are `d1 = 0` and `d0 > 0`,
  so the copies are row-0 shifts and, for a based block,
  `X[n+1] = X.dropLast ++ shiftr01 d0 0 (X[n]) = graft X (X[n])` — the branch is
  literally the row-1 tower recursion, so running it needs a graft closure that
  the hypothesis does not give
- Record that this makes the two residues of the new track (`badPar = 0, i1 = 1`
  and `TowerExp`) the same phenomenon: running a tower with only `R.dropLast` in
  hand
