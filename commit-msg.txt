Omega arithmetic: measure coverage against every ladder in reach

- Take what the context provides as the deepest level column inside a
  longest common prefix with M(v), over the plain staircase, the matrix
  being built, and M(u) inside a psi_{Omega_u}
- Cap that at the chain's own depth, so a level column below it stays
  unwritten and the copy carries it instead
- 736 of the sheet's 813 rows agree (was 734); row order still clean and
  805 of the built matrices are standard forms
