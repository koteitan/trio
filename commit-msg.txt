v0.95.0/tower equivariance of the guarded row-1 lift

- Generalise the tower induction to an arbitrary lift amount
  (`towerGraft2_lift`); `towerGraft2_holds` is its `t = 0` instance
- Add `gcopy_Lift1`, `glift_gcopies'`, `gcopies_Lift1`: guarded copies pass the
  lift through as the periodic lift
- Prove `oper_Lift1_root` and, with `glift_eq_Lift1`, `oper_Lift1_tower`:
  on a root-parented row-2 tower `Lift1` commutes with the expansion
