label the part links by their alpha range instead of 1/2

- Navigation links now read "α < ε₀" and "ε₀ ≤ α < Λ", written in Unicode
  because GitHub does not render math inside a link
- Point from part 1's domain paragraph to part 2 for ε₀ ≤ α < Λ
- Use the same labels in README.md
