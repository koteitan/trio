v0.118.159/add drop_rebase_mem_W and record what W_shiftl0 unlocks

- drop_rebase_mem_W: a subtree re-based to depth 0 is a based W-member at its
  own level, which is the shape Aop clause 3 wants for its graft argument
- Record in PROOF-STATUS that with W_shiftl0 the strictly-lower-level half of
  the residue is exactly a clause-3 graft, so only the clause-2-datum case and
  the equal-level case survive there
