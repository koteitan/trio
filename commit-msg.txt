v0.111.0/gamma reduces to element membership + root slice

- Add CoreBlockedElt: the descended copies block is in GX (independent
  of the ambient root; the context-length descent lives here)
- Add CoreBlocked0: the p = 0 slice (single-column descended context =
  the shift case, outside GX's two-column interface)
- Add coreBlocked_of_elt: away from the root the descended context
  M.take (p+1) is equipped by restriction (ctxOK_take), so the package
  is one GX_full application of the copies element
- GX_loop now rests on CoreBlockedElt + CoreBlocked0 + CtxLiftT1 + the
  machine's own inclusion W subset GX
