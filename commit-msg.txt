v0.60.0/tower period lemmas

- Add gexp_getD_indep: a tower entry does not depend on the copy count,
  so the shorter tower is an entry-wise prefix of the longer one
- Add gexp_period: moving one period up adds d0 to row 0 and the block
  guard's d1 to row 1
