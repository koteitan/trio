v0.118.37/one GX core plus a W-level equipment statement

- Add `corePlantCtxLift_of_graftAll : Wset.GraftAll -> CorePlantCtxLift`,
  recording that the plant core is NOT stronger than the graft closure — so
  further reduction alone can never close the campaign
- Give `CoreCtxSuffixLift` the window hypothesis it always had at its call site
- Add `InfEquip`, a pure `W`-level statement (no `GX`): the context's re-based
  infix, planted at its own root, is itself equipped, and its root's row 2 is
  at most 1
- Add `coreCtxSuffixLift_of_plantctx : CorePlantCtxLift -> InfEquip ->
  CoreCtxSuffixLift`, `GX_loop_plant` and `graftAll_of_plant`
- Add `wf_olt_ST_TS_of_plant` and `TRIO_terminates_of_plant : InfEquip ->
  CorePlantCtxLift -> WellFounded stepRel`
- Record the status in GRAFTALL-PLAN.md 1.9.34 and task.md
