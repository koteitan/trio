v0.118.19/the plant family is staircase-closed

- Add `coneV_cons_iff` / `amin_cons` (Aexp.lean): the cons'd root caps the
  ancestor minimum, `amin ((0,B,z)::S) (1+j) = min B (amin S j)`
- Add `slift_cons_plant`: a staircase moves a plant's root to `phi B` and acts
  on the tail through the capped staircase, so the plant family
  `{(0,B,z) :: slift S psi}` is closed under staircase lifts — the language for
  the context-length induction
- GRAFTALL-PLAN 1.9.23: map the four remaining cores, record that
  `CorePlantCtxLift` is bounded above by the (unusable) self-reference and must
  go through a length induction whose only open step is `hin`
