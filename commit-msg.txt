v0.118.52/the generated-family reduction is a tautology (honest correction)

- Add `gpow_dropLast_eq_tow`: the family peel `(0,v,z) :: (gpow R ((0,v,z)::R)
  k).dropLast` IS `tow v z R (k+1)`, so `CoreGpowPeel` restates "every tower
  level is a W-package" and `tower1_mem2_fam` / `_gpow` only re-index k
- Add `CoreGpowPeel` + `coreT1L_of_gpow`: alpha's obligation in GX-free,
  stage-free form (no `∀ y ∈ W m` interface, no `GX_full`, no
  `CorePlantCtxLift` in the statement), labelled as a restatement not a
  reduction
- Add `coreGpowPeel_of_plantctx` carrying the old `coreT1L_of_plantctx` content
  (`tow_mem_GX` + `GX_full` + `liftPlant_of_mask`) — no regression
- GRAFTALL-PLAN.md 1.9.42: record why the family form looked like it removed
  the stage raise, and redirect the next probe to "can the equipment alone give
  `tow v z R k ∈ W m`?"
