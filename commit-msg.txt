Omega arithmetic: cascade the copies, and check standardness with yaBMS

- Keep copying the last storey, one sub-unit shorter each time, so a
  subscript that is uncountable several times over descends step by step
- Check every matrix the builder writes with yaBMS's bms -s: 762 of the 813
  are standard forms, which tells apart a wrong build from a right build of
  a different ordinal
- Print the row-order check from the validation run as well
- 706 of the sheet's 813 rows agree (was 703); of the 101 that do not, 50
  are non-standard (the builder is wrong) and 51 are standard forms
