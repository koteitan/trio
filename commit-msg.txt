v0.118.33/CoreBlockedEltHi falls: the residue is two cores

- Add `gcopiesLift_mem_GX`: the strengthened copy-index induction
  `forall n s, Lift1 (gcopies B 0 L d0 d1 n) s in GX`, riding on the window's
  root lifts alone, with the ambient mask absorbed by `mlift_Lift1_cons`
- Add the supporting `entry_gcopies_pos`, `argOK_gcopies_tail` and
  `gcopies_cons_root`
- Add `CoreWindowLift` (the re-based window AND all its root lifts) with
  `coreWindow_of_lift` (its `s = 0` slice is `CoreWindow`) and
  `coreBlockedEltHi_of_windowLift`
- Give `CoreBlockedEltHi` the ambient equipment so it can reach the window core
- Drop the unused `hz1` / `hctx` from `liftPlant_of_plant`, making it usable
  outside the equipped setting
- Add `GX_loop_lift : CoreWindowLift -> CorePlantCtxLift -> (Aop W u GXs Y ->
  Y in GXs)` — the whole machine now closes on two cores
- Record the reduction in GRAFTALL-PLAN.md 1.9.30 and task.md
