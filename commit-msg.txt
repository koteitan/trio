v0.118.167/record oper's monotonicity and what it says about the probes

- Add oper_prefix_of_le and W_oper_mono to the structure table
- Note that since M[n'] is a prefix of M[n], a larger n is a STRICTLY stronger
  condition, so the probes' n <= 2 truncation is the loosest possible check --
  which is why the truncation validation mattered
