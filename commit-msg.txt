v0.108.0/beta shape-complete: CoreT2EStep follows from W <= GX

- Add nil_mem_GX (the empty element's obligations are context packages)
- Add coreT2EStep_of_le: given the machine's own inclusion W subset GX,
  the single-step core follows outright - context-part prefixes route
  through the peel's prefix obligations (take_graft_low + dropLast_take),
  element-part prefixes through the equipped composite context
  (ctxOK_graft + take_graft_high + graft_assoc)
- beta's sole remaining content = well-founding the self-reference
  {cores <-> W_le_GX}; measure design recorded in GRAFTALL-PLAN
  1.9.7/1.12 (provenance probe: t2-orphans are 100% initial-context
  columns, position non-increasing; t1-orphans are 100% planted roots)
- Update GRAFTALL-PLAN.md (1.9.6/1.9.7/1.12, execution order)
