v0.118.158/prove the missing direction of W_shift

- Add Wset.lev_shiftl0 and Wset.oper_shiftl0: oper commutes with a downward
  row-0 shift whenever every column sits at depth >= d
- Prove Wset.W_shiftl0: M in W u -> (forall x in M, d <= x.1) ->
  shiftl0 d M in W u.  W_shift only moved a block deeper; this moves it back up,
  which is what re-basing a sub-block needs
- Clause 3 needs no parent transport: a dominant terminal only ever uses its
  peel graft N [] = N.dropLast
