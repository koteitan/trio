v0.118.179/measure the shortest equivalent form of the theorem, single-column insertion

- Add tools/probe_insert.py for (INS): Wself closed under inserting one
  arbitrary column at an arbitrary position, 1603817 decided instances with 0
  violations (1268993 for the variant that hangs the column under S[p])
- Note that (INS) at the end position is (SNOC), already wired as
  TRIO_terminates_of_snoc, so it adds no new core but states the whole theorem
  with no side condition at all
- Use the proved snoc_zeroRow2 as a base case of the decision procedure, which
  is what lets these probes decide an order of magnitude more instances
- Record both rows in PROOF-STATUS 4
