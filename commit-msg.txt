v0.118.70/record the recipe for the branch badPar = 0, i1 = 2

- GRAFTALL-PLAN.md 1.9.57: spell out the five steps that discharge the row-2
  branch (nextrel2 -> le1 -> rtg0_of_rtg1 -> window_of_rtg0 -> oper_eq_gexp ->
  gexp_guard_transport), and note that `gexp_guard_transport` is general in the
  parent index so the same recipe covers `1 ≤ badPar` with `i1 = 2`
