v0.118.61/the staircase lift transports W, and (WL) is measured exact

- Add Wslift.lean `slift_mem_W`: `Stair φ → X ∈ W m → slift X φ ∈ W (2*φ m + m)`,
  core-free (clause 2 by `slift_oper`, clause 3 absorbed above length 1, the
  one-column roots discharged by the stage bump) — so `CoreStairOm` is not a
  core once the stage is allowed to rise
- Add tools/probe_lift.py: `minstage (Lift1 X d) = minstage X + 2d` holds with
  EQUALITY on all 18300 instances, giving the conjecture (WL)
- GRAFTALL-PLAN.md 1.9.50: compare with the yapss `Wstar_closed` (no context,
  no lift, no cores), show (WL)'s stage arithmetic matches the row-2 tower
  branch exactly, and record that `Lift1` fails to commute with `oper` only
  when the bad root's parent is index 0
