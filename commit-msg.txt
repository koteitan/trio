v0.82.0/W-membership layer: WF of olt on ST_TS modulo three hypotheses

- Add `maxlev`/`le_maxlev`
- Add `mem_of_Aclosed_aux`/`mem_of_Aclosed`/`mem_Wstar`: the length induction
  over the last top-level tree, with the stage picked as the tree maximal
  subscript level so that `tbAll` follows from `tbAll_of_lev_bound`
- Add `mem_W_of_bound`/`mem_W_maxlev`/`W_membership`
- Add `wf_olt_ST_TS_of_cofinality`: sorry-free, axioms [propext,
  Classical.choice, Quot.sound], open only on `TowerOK`, `TbOper` and the
  cofinality hypothesis
