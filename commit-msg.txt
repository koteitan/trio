v0.118.2/split the alpha lift residue into context and datum (Gamma.lean)

- Add `CoreMaskLift` (GX is closed under the ambient mask lift `mlift`) and
  `CorePlantCtxLift` (the lifted planted context peel is in GX)
- Prove `coreLiftPlant_of_mask`: the graft lift calculus rewrites the lifted
  planted block as the lifted planted context grafted with the datum's mask
  lift, so `CoreLiftPlant` follows from the two new cores via `plantCtx_graft`
- Add `GX_loop_mask` / `GX_loop_pieces_mask`: the machine closes on
  (CoreCtxSuffix, CoreBlockedEltHi, CoreBlocked0, CorePlantCtxLift,
  CoreMaskLift) — the lift residue now carries no element/context mixture
- Import Cgraft in Gamma
