build_omega_alpha CLI now accepts the whole range alpha < Lambda

- Route the CLI through probe_eps_range.Many, so psi(W), psi(W_X), W and
  W_X parse (the two builders are checked to agree on all 121 w-CNF rows)
- Rewrite --help with the new atoms, examples and the domain statement
- Note the CLI's range in ebp2bms-1.md and ebp2bms-1-en.md
