v0.117.3/self-audit: the aligned calculus also needs a single-tree argument

- Add probe_alignedforest: with a FOREST argument the aligned lift calculus
  breaks (30448/93024 at tower1/aligned/d0=0), so the earlier statement was
  too strong
- Correct GRAFTALL-PLAN 1.9.17 (and the memory) to require X to be a
  principal block; note that everything the machine lifts is one, so the
  condition is automatic at the use sites
