v0.118.18/planted roots in GXs, and the upper bound on CorePlantCtxLift

- Add `amin_zero` and `slift_singleton` (Aexp.lean): a staircase moves a
  singleton's root to `phi b`
- Add `singleton_mem_GXs`: every planted root `[(0,b,c)]` is in `GXs`, with all
  its staircase lifts (generalises `coreStairOm_holds`)
- Add `corePlantCtxLift_of_self`: `CtxOK M v z` at `k = |M|-1` already puts the
  lifted planted peel in `W a`, so the core is bounded above by the
  self-reference "based W-elements are in GX" — recorded as an upper bound, not
  as the intended route (the core is to be proved by induction on |M|)
