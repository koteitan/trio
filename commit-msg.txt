v0.58.0/A2 straddling: the shallower column is never inside the block

- Add mul_ite_zero and argDomCoreOn_bad_A2_inner: for an arbitrary copy
  index the straddling case with j0 < ipos is empty, via cross_absurd
  fed by the block spine, the copy-k mirror spine and the copy-1 root
- Drop argDomCoreOn_bad_A2_c, now subsumed (it was the k = 1 instance)
