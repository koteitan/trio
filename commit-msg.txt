v0.118.157/record what zle1 concretely buys

- Under zle1 the level determines the column (2b+c = 2b'+c' with c,c' <= 1
  forces c = c' mod 2), so the residue's lev C 0 <= lev S p splits into
  "C's head IS the column" (the head-matching form) and "C sits at the very
  stage Aop clause 3 allows"
- The g-ness of (SUBST1g) is needed only for (TOW)'s constant diagonal host,
  which falls in the second case
