v0.54.0/bad_A2 aligned-at-the-root case

- Add map_range'_getD, list_eq_of_getD and argdom_B_getD (positional form
  of the argument segment)
- Add argDomCoreOn_bad_A2_root: when the marked columns are the block root
  and the copy-1 root, the tower's period-Lb self-similarity makes the
  argument an initial segment of the comparator, so sle holds by prefix
