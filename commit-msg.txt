v0.118.49/correct the measure: the tower re-association preserves composite length

- Add `graft_tow_succ` in Gamma.lean: grafting the (k+1)-st tower level into N
  is grafting the k-th into the one-step grown context, and the two composites
  are the same list
- Correct GRAFTALL-PLAN.md 1.9.40 accordingly: composite length is INVARIANT
  along the tower, so the tower must be driven by its own index; the measure is
  the lexicographic pair (composite length, tower/copy index), with the length
  descending only on the plant / window obligations
