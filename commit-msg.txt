v0.118.44/the crossing infix of a composite context, general length

- Add `seg_graft_cross` and `shiftl0_seg_graft_cross`: the generalised forms of
  `seg_graft_eq` / `shiftl0_seg_graft` for an arbitrary infix length, so the
  crossing case of `CtxInf` can be reduced to a graft of the datum prefix
