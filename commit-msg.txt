v0.118.59/probe CoreCap with the stage included

- Add tools/probe_cap2.py: uses the exact recursive characterisation
  (`S in W a <-> forall n, S[n] in W a` for length >= 2, leaf test
  `[(d,v,z)] in W a <-> 2v+z <= a`), so a reachable over-level singleton would
  refute `CoreCap`; 5048 ok / 136 unknown / 0 violations
- Mark tools/probe_cap.py as superseded: `trio.expand` peels a length-1
  sequence to `[]` while Lean's `oper` is the identity there, so its
  "reaches []" criterion tested a different statement and never saw the stage
- GRAFTALL-PLAN.md 1.9.49: record the model discrepancy and the new result
