v0.84.0/thread the z<2 fragment condition through W*

- Add `zle1` (row 2 never exceeds 1) with `zle1_oper`, `zle1_diagSeqT` and
  `zle1_ST_TS`: the generators are z-capped and `oper` copies row 2 verbatim
- Add `z ≤ 1` to `Wstar` and to `TowerOK`; the srow=1 tower needs `2v+z ≤ m`,
  which follows from `v < w` only when the root row 2 is capped
- Carry `zle1 M` through `mem_of_Aclosed_aux` and `mem_W_of_bound_aux`, and
  discharge it in `W_membership` from `ST_TS`
