v0.118.152/finish the inW truncation check

- Fix the parameter block, which had silently reverted: two of the earlier
  replacements carried no assert, so they were no-ops and the run used the
  original bounds and never completed
- Result at MAXDEPTH 8 / MAXLEN 30 / AMAX 5 over 414 sequences: n <= 2 and
  n <= 3 agree on all 2209 decided instances, 0 disagreements, 275 undecided
- Record it in PROOF-STATUS next to the audit caveats
