v0.118.121/document the (SUBST1) reduction

- Add GRAFTALL-PLAN 4.02: the left-to-right substitution argument, the two
  probe results, and why (SUBST1g) still cannot absorb (CAT)'s m < a branch
- Mark the (SUBST) -> (SUBST1) reduction done in task.md
