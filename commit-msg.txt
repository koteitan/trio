v0.110.0/lift composition + ltail prefix locality (alpha equipment assets)

- Add Lift1_Lift1 (Wset): iterated root-cone lifts add - the cone is
  lift-invariant (via le1_Lift1)
- Add ltail_take (Wset): take commutes with the lifted tail - the root
  cone is prefix-local (via Lift1_take + lift_cons + injection)
- Probe-verified first (probe: 0/18021 and 0/4000 violations); with
  these, the ALIGNED instances of CtxLiftT1 reduce to CtxOK M + the
  peel's prefix packages at lift t + t'; the residue is the unaligned
  roots over lifted bodies (recorded in GRAFTALL-PLAN)
