v0.118.32/root-cone transport at the block root + the guarded copies' root-lift recursion

- Add lean/Croot.lean with `gexp_cone_mir_root`, the `j0 = 0` case of Lcone's
  root-cone transport, under the two hypotheses the probe isolates
  (`0 < d1` for the copy roots, `le1 M 0 Lb` for the cross-copy chains)
- Add the segment re-basing `le1_seg_root` / `gcopies_seg` (both free from
  `le1_take` + `le1_append_right`) and `le1_shiftl0`
- Add `gcopies_succ_graft_lift`: for `d1 > 0` the copies block is
  `graft B (Lift1 (previous block) d1)` — the root-lift analogue of
  `shiftl0_gcopies_succ`
- Add tools/probe_gcopieshi.py and tools/probe_coneroot.py measuring the
  recursion and both cone transports (0 violations on the real blocker shapes)
- Register Croot between Aexp and Xbar in lakefile.toml
