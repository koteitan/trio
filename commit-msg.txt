v0.118.64/the row-2 tower core falls to the stage law, bypassing GX

- Add Wtower2.lean `LiftStage` (= (WL): `X ∈ W m → Lift1 X d ∈ W (m + 2*d)`) and
  `towerGraft2_of_liftStage : LiftStage → Wset.TowerGraft2` for the LIFT-FREE
  `Wstar`: the tower consumes only the single lift `d1`, and
  `2v+z+2*d1 = 2w+z ≤ m` closes the stage bookkeeping, so the `∀ s`
  strengthening that forced `Wstar2`/`GraftAll`/`GX` is unnecessary
- Add `TRIO_terminates_of_liftStage` / `no_infinite_expansion_of_liftStage` in
  Final.lean; axioms [propext, Classical.choice, Quot.sound]
- GRAFTALL-PLAN.md 1.9.53 + task.md: record the three tracks and that the new
  residue is `LiftStage` + `TowerExp`
