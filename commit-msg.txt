v0.118.162/correct the (GC) reduction: it narrows the proof, not the statement

- The clause-2 case hands back S.dropLast in W u, but W_dropLast already gives
  that for free, so adding it as a hypothesis yields the same proposition
- What the clause analysis buys is that a proof only has to treat one case
