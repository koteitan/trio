v0.118.193/show the residue's substitution half is free from the prefix package

- Confirm `hpre` (every proper prefix of S satisfies SubstProp) is in scope at the residue call site and currently discarded
- Prove `takeC_mem_of_prefixPackage`: instantiating it at `S.take (p+1)` yields `S.take p ++ C` directly
- Record in RESIDUE-PROBLEM that both branches then reduce to an append step, with an explicit warning to check v0.118.122 for circularity with (CAT)
