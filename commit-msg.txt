v0.118.48/verify the row-1 tower family form in Lean

- Add `tower1_mem2_fam`: the row-1 tower needs only its OWN elements grafted
  into R, so the `forall y in W m` interface can be dropped; this confirms in
  Lean that the stage raise m -> m+2t is an artefact of the interface, not of
  the tower itself
