v0.118.73/correct the plan: the j0 >= 1 branch needs a new transport lemma

- GRAFTALL-PLAN.md 1.9.60: `gexp_guard_transport` transports the cone from the
  parent index `j0`, but the commutation needs the cone from index 0 of the
  expansion; for `j0 >= 1` the two differ, which is also why `glift` and
  `glift_eq_Lift1` are restricted to `j0 = 0`
- Restate the remaining work: a cone-from-0 transport lemma for copies blocks,
  the row-1 tower branch, and `TowerExp`
