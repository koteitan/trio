v0.107.0/GX prefix internalization + composite-context equipment

- Strictify CtxOK to k < M.length (no consumer used the full-block package)
- Internalize element-prefix obligations into GX (packages of
  graft M (y.take i) for i <= |y|); add GX_full for the full-element form
- Discharge the new prefix obligations uniformly in GX_closed: one step
  from the datum in all three clauses (clause 2 via the new
  oper_take_prefix - copy 0 is unshifted; clause 3 via dropLast_take;
  short elements via the context package)
- Add ctxOK_graft: the composite context graft M Y is equipped from
  CtxOK M + Y.dropLast in GX - the CtxOK-composition half of beta
- Add oper_take_prefix, dropLast_take (Column); shiftr01_take,
  take_graft_low, take_graft_high (Xbar)
