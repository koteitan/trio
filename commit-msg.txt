v0.92.0/the tower's periodic mask is the intrinsic cone

- Add `oper_eq_gexp`: a root-parented expansion is `Gtrans.gexp` at `j0 = 0`
- Prove `glift_eq_Lift1` from `Gtrans.gexp_guard_transport`: on a root-parented
  tower the periodic `glift` mask coincides with `le1` of the expanded block,
  so `glift M L 0 t (M[n]) = Lift1 (M[n]) t`
