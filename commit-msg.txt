v0.118.4/the staircase lift closes the obligation language (probes)

- Identify the mask family's parameter: `mu A j` = the minimum row 1 along j's
  row-0 ancestor chain, so `coneV A v = {j | mu j > v}` and mask lifts compose
  into staircases `phi(m) = m + sum s_i [m > v_i]`
- Add tools/probe_glift.py and tools/probe_glift2.py: over 100000 samples the
  staircase lift `glift A phi` satisfies, with no violations, (G6) mu maps by
  phi, (G5) glift composes, (G2) glift commutes with the expansion
  unconditionally, (G3) glift distributes over grafts with the context cap,
  which is again a staircase
- Record in GRAFTALL-PLAN 1.9.19 the consequence: parameterising the machine's
  obligations by a staircase makes lift closure definitional, so CoreLift,
  CoreMaskLift and CoreLiftPlant should all disappear, leaving the context
  pieces and the row-2 blocker
- Update task.md with the v0.119 target
