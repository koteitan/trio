v0.87.0/prove the row-1 tower; two named cores remain

- Add `graft_eq_shift`, `shiftr01_comp`, `shiftr01_zero`, `parent_cons_eq_zero`
- Add the tower `tow v z R` and prove `oper_cons_tower1`: for a row-1 collapse
  the copies carry no row-1 lift, so `p_{v,z}(R)[n]` is the plain graft tower
- Add `tower1_le` (`2v+z <= m` in a row-1 tower) and `tower1_mem`
- Split `TowerOK` into `TowerGraft2` (guarded row-2 tower) and `TowerExp`
  (tower over an argument block that arrived by the successor clause), and
  prove `towerOK_of`: the row-1 graft tower is now discharged
- `TRIO_terminates` depends on `TowerGraft2` and `TowerExp`
