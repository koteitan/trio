v0.118.163/free the orphan half of (LOW)

- Prove snoc_orphan: appending a column that stays an orphan is free, since oper
  peels it straight back off and the root is untouched
- Add dropLast_mem_Wself
- Define LowerLast and LowerLastParented and prove lowerLast_of_parented, so
  (LOW) now only concerns the case where the lowered column finds a parent
