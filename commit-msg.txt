v0.118.194/record that the append reduction is circular with (CAT)

- Read v0.118.122: its direction is (SUBST1g) => the tower facts => "(CAT) dropped from the residue", so (CAT) has no independent proof
- Conclude that reducing the residue to an append step closes a loop, and mark that direction as refuted
- Keep the two real assets: `takeC_mem_of_prefixPackage` and the confirmed availability of the prefix package
- Record the one untested difference: the append we need carries `S` itself and the package, which bare (CAT) does not
