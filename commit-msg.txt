v0.118.178/separate the two reasons tie-freeness cannot be discharged

- Measure the closure of ST_TS under take and drop+rebase, which is exactly what
  mem_of_Aclosed_aux recurses on: 1225966 sequences, 411600 row-2 bad parts at
  root row 1 >= 1, 0 ties -- so the induction does NOT leave the tie-free class
- Record in PROOF-STATUS 5.7 that the real obstacle is instead that Wstar is
  used as an A-closed set in A2', so the tower argument is needed for every
  argOK block and no class restriction survives (Aop data never implies class
  membership)
