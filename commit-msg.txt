v0.118.38/measure the infix law for the lifted context

- Add tools/probe_infltail.py: the naive "infix root lift" form of the lifted
  context's infix is FALSE (1911/35973 even under the window condition) because
  the ambient root's row-1 value can sit below the infix root's
- Establish the correct law with the AMBIENT MASK LIFT `mlift . v t`: 0
  violations in every branch that matters (in/window-free 0/12283, in/window
  0/35973, out/window 0/75328); the window condition is needed only in the
  "root outside the ambient cone" branch
- Record in GRAFTALL-PLAN.md 1.9.35 that `CtxInf` must therefore be a staircase
  closure, exactly like `GXs`, and that the infixes `InfEquip` needs never touch
  the last column so they inherit from `R[1]` just like the prefixes do
