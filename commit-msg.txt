v0.118.51/the family peels descend in k: the shape of the new induction

- GRAFTALL-PLAN.md 1.9.41: `(gpow N E (k+1)).dropLast = graft (gpow N E k)
  E.dropLast`, so the family-peel obligation Phi(k+1) needs exactly Phi(k) as its
  context-plant obligation — the core that never descended over arbitrary
  contexts DOES descend over the generated family
- Phi(0) is the base context peel, which the equipment `CtxOK N` already gives;
  the outer measure is the datum length (E, E.dropLast, ...), the inner one is k
