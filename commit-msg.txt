v0.80.0/index W* by tbAll on the principal block

- Restore `natDom` in `Aop` clause 2 and `tbAll z m` in clause 3, reverting the
  v0.79.0 experiment: ungating clause 2 certifies dead-root blocks but leaves
  consumers with only `M.dropLast`, which breaks the tower branch
- Redefine `Wstar` to quantify over every stage `u` with `2v+z <= u` and
  `tbAll ((0,v,z) :: R) u`, indexing by the dead orphans of the PRINCIPAL
  block rather than of the argument block; this puts the z=1 pathology back
  inside clause 3 range (probe: 0/264 tower and 0/184 non-tower violations,
  against 52/119 for the R-indexed form)
- Add `oper_closed`: `W u` is closed under one expansion step
- Add `oper_cons_succ`: the successor principal step is `n` literal copies of
  `p_{v,z}(R.dropLast)`
- Add `seg_zero_eq_take`
