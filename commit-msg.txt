v0.118.68/split the parented residue of (WL) into four branches

- Add `LSPOn`, `badPar`, `srow_cases` and `liftStageParented_of_cases`: the
  parented residue splits into `1 ≤ badPar` and `badPar = 0` with each of the
  three collapse rows, so the branches can be filled independently
- GRAFTALL-PLAN.md 1.9.56 + task.md: pair each branch with the tool that should
  discharge it (`gexp_guard_transport` for `i1 = 2`, `glift_eq_Lift1` for
  `badPar = 0, i1 = 2`, `W_flatMap_copies` for `badPar = 0, i1 = 0`) and record
  that `badPar = 0, i1 = 1` is the real core
