v0.118.25/measured: baking the GX component into the equipment is circular

- GRAFTALL-PLAN 1.9.27: the v0.119 design needs `CtxOK` to assert that the
  context's own planted prefixes are in `GX`, but `GX` is DEFINED from `CtxOK`,
  so the reference is a definitional cycle (verified in Lean: `Unknown
  identifier GX`), and unfolding it makes `CtxOK` self-referential
- Record the escape routes; the favoured one is to STRATIFY by context length
  (`GX_n` obliges contexts of length <= n, `CtxOK_n`'s GX component uses
  `GX_{n-1}` since `M.take k` is strictly shorter, `GX = intersection of GX_n`)
