v0.106.0/beta reduces to the single-step core CoreT2EStep

- Add CoreT2EStep (Gamma): at a beta-site, graft Y e in GX for every
  e in W (2*w1 + z) - a stage strictly below the orphan level (z < w2)
- Prove coreT2EFam_of_step: the family induction on the copy index is
  mechanical - base = clause-2 datum peel (Y.dropLast in GX) or the
  context package for singleton Y; step = the previous obligation's own
  Wstar2 package puts the next tower element in W (2*w1 + z), then
  graft_assoc re-expresses its graft over the equipped context M
- Wire coreT2E_of_step, so GX_closed now rests on
  CoreBlocked + CoreT1L + CoreT2EStep
