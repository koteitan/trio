v0.118.23/the campaign converges on the plant family

- GRAFTALL-PLAN 1.9.25: chasing each of the four cores to the end lands on the
  same object, the plant family {(0,B,z) :: slift S phi}
- `CoreBlockedEltHi` (ascending copies, d1 > 0) is the d1 = 0 iterated-graft
  pattern with each level a ROOT LIFT of the previous, so (ML) absorbs the
  outer lift and what is needed is the window `cwin` together with all its root
  lifts -- and a root lift of a based sequence is exactly a plant-family member
- So v0.119 is a single question: how to propagate the equipment `CtxOK` along
  the plant family
