v0.118.30/record: self-support closes as a class, but not inside GX's definition

- GRAFTALL-PLAN 1.9.28: with `selfSup_take` / `selfSup_graft` / `selfSup_ltail`
  the machine's own context constructions preserve self-support, so
  `CorePlantCtxLift` reduces to "the TOP-LEVEL context is self-supporting"
- But `gx_of_pieces` only ever has `CtxOK` in hand, and putting `SelfSup` into
  `GX`'s definition is blocked by variance: `GX` is antitone in the equipment
  while the equipment's GX-component is monotone, so there is no fixpoint;
  depth stratification oscillates and length stratification breaks `gx_graft`
- Updated the v0.119 options accordingly
