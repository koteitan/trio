v0.118.34/both surviving cores are now pure context statements

- Add `CoreCtxSuffixLift` (the context's re-based infix from the blocker, with
  all its root lifts) and `coreWindowLift_of_ctxSuffixLift`: the window splits
  as the suffix's planted block grafted with the datum's peel, so
  `liftPlant_of_plant` sends the lift to the suffix's own root lift plus the
  datum's ambient mask
- Restore `Y.dropLast in GXs` in CoreBlocked / CoreBlockedElt /
  CoreBlockedEltHi / CoreWindow / CoreWindowLift so the datum's mask is
  available; `gx_of_pieces` already holds the GXs form
- Add `GX_loop_ctx : CoreCtxSuffixLift -> CorePlantCtxLift -> (Aop W u GXs Y ->
  Y in GXs)`
- Record the reduction in GRAFTALL-PLAN.md 1.9.31 and task.md
