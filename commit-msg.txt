v0.117.1/two decisive probes: TbOper holds, the natDom route is refuted

- Add probe_tboper: the principal block's minimal tbAll bound survives
  expansion (0/11304), so the tbAll bookkeeping the campaign dropped is
  actually available
- Add probe_deadgraft: at dead sites the graft does NOT commute with the
  composite lift (9377004/26740030, and 247734/2083496 even for
  level-bounded data), so clause 3 cannot be supplied there
- Record GRAFTALL-PLAN 1.9.16: the measurement confirms that restoring
  natDom only moves the wall to branch (c)
