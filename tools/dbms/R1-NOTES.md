# 課題 R1: `ResidBlkT` は**偽**（反例つき）。`DmapInT` は真で、証明の道具も揃った

2026-08-28。計測は `tools/dbms/r1.py`（`rows3.py` を**触らずに** `conv3` /
`conv_resid` / `dmap_at` をモジュール属性の差し替えで包むだけ。像は 1 ビットも
変えない）と `tools/dbms/r1ext.py`（接頭辞を固定した全数展開）。

---

## 0. 結論を先に（3 行）

1. **`ResidBlkT`（残余は単一の木）は偽。** 最小の反例は **8 列**。
   `gen3 <=8` 全数 781605 個で**森の枝が 32 回**発火する。
   長い行列ではむしろ普通で、接頭辞族 108650 個では **5253/12872 = 41%** が森。
2. **`conv3` は直さなくてよい。** 反例の像はどれも `blockok 0` を満たし、
   DBMS 標準形である。**壊れているのは仮定の書き方だけ**で、
   `BlkOK rd` を **`BlkOK d`（外側の `conv3` の深さ）**に替えると
   **8 節すべてが全母集団で違反 0** になる。破れるのは
   **節 6（森の枝と 1 対 1）と、その巻き添えの節 2** だけ。
3. **`DmapInT` は真。** しかも `dmapAt` の枝は「`k = |dmap|` か `|dmap|-1`」
   だけではない（9 列で `k+1 < |dmap|` を **3 例**見つけた）ので、
   `dmapAt_bounds` だけでは閉じない。**節 10**（下）が要る。

---

## 1. 測った母集団と数字

`conv3` の**全呼び出し**で測っている（課題 L12 §2 の訂正 A を踏まないため）。

| 母集団 | 母数 | `conv3` 呼び出し | `convResid` 発火 | **森の枝** | `dmapAt` の枝 |
|---|---:|---:|---:|---:|---:|
| `gen3 <=6` 全数 | 8387 | 106560 | 44 | **0** | 0 |
| `gen3 <=7` 全数 | 77282 | 1139322 | 338 | **0** | 6 |
| `gen3 <=8` 全数 | 781605 | 13108043 | 2414 | **32** | 83 |
| `<=6` の展開閉包（n=1,2,3 を 2 巡, 最長 24 列）| 46855 | 1181746 | 5236 | **0** | 167 |
| 接頭辞 `(0,0,0)(1,1,1)(2,1,0)(1,1,0)` <=9 列 | 15746 | 288358 | 1539 | **488** | 860 |
| 接頭辞 `(0,0,0)(1,1,1)(2,1,0)(1,1,0)` <=10 列 | 108650 | 2200967 | 12872 | **5253** | 7850 |
| 接頭辞 `(0,0,0)(1,1,1)(2,1,0)(3,1,0)` <=9 列 | 31605 | 589569 | 26 | 0 | 9 |
| 接頭辞 `(0,0,0)(1,1,1)(2,1,0)(3,1,0)` <=10 列 | 213955 | 4417583 | 331 | **62** | 141 |

⟹ **`<=7` 列と展開閉包だけを見ていたから 0 だった。** 森は 8 列から出る。

---

## 2. `ResidBlkT` の反例（最小・8 列）

    M = (0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,0,0)(2,0,0)

BMS 3 行 z<2 の**標準形**（`core.isstd` で確認）。末尾を
`(2,1,0)` / `(2,2,0)` / `(2,2,1)` に替えた 3 つも同じく反例。

### 何が起きているか（手で追える）

* 上の行列に対し `conv3` は `off = 1`（`p = (1,1,1)`, `p.1 = 1`）で
  `lad0` の枝に入り、`ps = (0,0)` なので `e = 1` のとき `qlab = (1,0)`。
* `units_split` は最初の柱 `(1,1,0)` がそのまま `qlab` なので **`U = []`**。
  `q = (1,1,0)`（深さ 1 = `p.1`、ラベル `(1,0)` = `qlab`）。
* `Aq = split0(q, r2) = [(2,2,1),(3,1,0),(3,0,0),(2,0,0)]`。
* `blk = [p] + A + U = [(1,1,1),(2,1,0)]`、
  `pre = contrPre(...) = [(2,2,1),(3,1,0)]`。`Aq[:2] == pre` で**一致する**。
* したがって

      rest2 = Aq[2:] = [(3,0,0), (2,0,0)]
                        ^^^^^^^ 深さ 3   ^^^^^^^ 深さ 2 < 3   ← **森**

* `conv_resid` は 2 周する。`rd = dmap_at(st, 3-1) = dmap[2] = 4`、
  2 周目は `rd = max(0, 4 - (3-2)) = 3`。
* 像は `(0,0,0)(1,0,0)(2,1,0)(3,2,1)(4,1,0)(4,0,0)(3,0,0)` で
  **DBMS 標準形**・`blockok 0` を満たす。行 0 の並びは `0 1 2 3 4 4 3`。
  **末尾の `4 3` が `convResid` の 2 本の木**である。

### `ImgBlockT3` 自体は森でも壊れない（測った）

森が 488 回発火する接頭辞族 15746 個と、`k+1 < |dmap|` が出る族 31605 個、
**合わせて 47351 個で `blockok 0` の破れ 0**。
⟹ **`conv3` は直さなくてよい。直すのは仮定の書き方だけ。**

### なぜ「`<=7` 列で 0」だったのか（構造の説明）

`split0` から**無条件に**言えるのは次の 1 本だけである。

> **補題 A** `Aq = (split0 q r2).1` かつ `q.1 = p.1` なので
> **`∀ c ∈ Aq, p.1 + 1 ≤ c.1`**。`rest2 = Aq.drop |pre|` は `Aq` の接尾辞
> なので同じ下界を持つ。

つまり `rest2.head.1 = p.1 + 1` のときは、それが `Aq` の**最小の深さ**なので
**単一の木は自動**である（`<=7` 列の 338 発火のうち **328** がこれ）。

問題は `rest2.head.1 > p.1 + 1` のとき、すなわち **`pre` が `Aq` の木の
途中で切れる**ときである。`pre` は `blk = [p]+A+U` の写し（深さ +1）で、
`blk` の最後の木は**もとの場所では完結している**（次の柱が `q`、深さ `p.1`）。
ところが**写しの側 `Aq` は `q` の引数ブロックの終わりまで続く**ので、
写しが尽きたあとに `q` の次の子（深さ `p.1+1`）が来てよい。そこが森になる。

`gen3 <=7` では「深い先頭」が **6 回**しか起きず、しかも 6 回とも
**そこで行列が終わっていた**（`rest2` の長さが 1）。1 列足すだけで森になる。
上の反例はまさに `<=7` の 6 例のうちの 1 つ

    (0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,0,0)

に `(2,0,0)` を足したものである。

---

## 3. 直し方: `BlkOK rd` ではなく **`BlkOK d`**

`convResid` の結果について `BlkOK` の 8 節を、開始深さを
**`rd`** にした場合と**外側の `conv3` の `d`** にした場合の両方で数えた。

| 母集団 | `convResid` 発火 | `BlkOK rd` の破れ | 破れた節（内訳）| **`BlkOK d` の破れ** |
|---|---:|---:|---|---:|
| `gen3 <=7` | 338 | 0 | — | **0** |
| 展開閉包 46855 | 5236 | 0 | — | **0** |
| **`gen3 <=8` 全数** | 2414 | **32** | 節 6: 32 / 節 2: 16 | **0** |
| 反例 4 個 | 4 | **4** | 節 6: 4 | **0** |
| 接頭辞 `(2,1,0)(1,1,0)` <=9 列 | 1539 | **488** | 節 6: 488 / 節 2: 220 | **0** |
| 接頭辞 `(2,1,0)(1,1,0)` <=10 列 | 12872 | **5253** | 節 6: 5253 / 節 2: 2242 | **0** |
| 接頭辞 `(2,1,0)(3,1,0)` <=10 列 | 331 | **62** | 節 6: 62 / 節 2: 36 | **0** |

**破れるのは `BlkOK rd` の節 6 `∀ c ∈ res.1, rd ≤ c.1`（森の枝と 1 対 1）と、
その巻き添えの節 2 `rd ≤ |res.2.ST|` の 2 本だけ**である。
`rd` を外側の `d` に替えると **8 節すべてが全母集団で違反 0**。
破れ数は**森の枝の回数とちょうど一致する**（節 6 の列と `resid_FOREST` が同数）。

`blk_step` が組み立てたいのは `BlkOK d`（外側）なので、
**`BlkOK d` を直接もらうのがいちばん素直**である。

### Lean に渡す形（命題 1 行）

    def ResidBlkT2 : Prop :=
      ∀ (rest : TrioSeq) (d rd : ℕ) (Lr : List Lent) (ps pw : ℕ × ℕ)
        (st : St) (nx : Option Col) (off : ℕ),
        rd ≤ st.ST.length → DmOK st →
        (∀ c ∈ rest, d + (rest.headD (0,0,0)).1 ≤ rd + c.1) →
        BlkOK d st (convResid rest rd Lr ps pw st nx off)

側条件 `∀ c ∈ rest, d + rest.head.1 ≤ rd + c.1` は
「木を 1 本降りるたびに開始深さが `m0 - tail.head.1` だけ下がるが、
それでも `d` を割らない」を書いたもので、**`convResid` の再帰でそのまま保たれる**
（`rd' = rd - (m0 - m1)`, `head' = m1` を入れると同じ式になる）。
実測: `rd` の `max(0, ...)` の**切り詰めは一度も起きない**（全母集団で 0 回）ので、
引き算は素直に伸びる。

### 呼び出し点で側条件が出ること

補題 A から `∀ c ∈ rest2, p.1 + 1 ≤ c.1` なので、側条件は

    d + rest2.head.1 ≤ rd + p.1 + 1

に落ちる。`rd` の 2 つの枝で:

* **`rd = d + 1 + e` の枝**（`rest2 = []` または `rest2.head.1 = p.1+1`）:
  `d + p.1 + 1 ≤ d + 1 + e + p.1 + 1` は自明。
* **`rd = dmapAt dmap k` の枝**（`k = rest2.head.1 - 1 ≥ p.1 + 1`）:
  `dmap[k] ≥ d + (k - p.1)` が要る。これが**節 10**（下）である。

---

## 4. `DmapInT`: 真。ただし「範囲内の中ほど」も起きる

### 4.1 `<=7` 列の 2 例（要求どおり全部書き出す）

どちらも `off = 1`, `p = (1,1,1)`, `e = 1`, `d = 1`, `k = 2`。

    M   = (0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,0,0)
    M'  = (0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,1,0)
    rest2 = ((3,0,0),) / ((3,1,0),)
    dmap  = [0, 3, 4]                       |dmap| = 3
    ST    = [(0,0),(0,0),(1,0),(2,1),(1,0)] |ST|   = 5
    dmap[k] = dmap[2] = 4
    d ≤ dmap[k] ≤ |ST|  ⟺  1 ≤ 4 ≤ 5       ✔

`k = 2 = |dmap| - 1` なので `DmOK`（`dmap.last + 1 = |ST|`）から
`dmap[k] = |ST| - 1` が出る。下界は `d + 2 ≤ |ST|`（`BlkOK_ST_ge`）から
`d ≤ |ST| - 2 < dmap[k]`。**この 2 例だけなら `dmapAt_bounds` で閉じる。**

### 4.2 ところが `k + 1 < |dmap|` は起きる（**課題 L13 の訂正**）

`Dbms3.lean` の `DmapInT` の docstring は「338 発火で一度も踏まれない」と
書いているが、**9 列で踏む**。接頭辞 `(0,0,0)(1,1,1)(2,1,0)(3,1,0)` の
全数展開 31605 個で **3 例**:

    M = (0,0,0)(1,1,1)(2,1,0)(3,1,0)(1,1,0)(2,2,1)(3,2,0)(4,1,0)(3,X,Y)
        X,Y ∈ {(0,0),(1,0),(2,0)}
    d = 1, k = 2, dmap = [0,3,4,5]  (|dmap| = 4), |ST| = 6, dmap[2] = 4
    k + 1 = 3 < 4 = |dmap|          ← DmapInT の第 2 項がここで効く
    d ≤ dmap[k] ≤ |ST|  ⟺  1 ≤ 4 ≤ 6   ✔

同じ接頭辞を **10 列**まで伸ばすと `k+1 < |dmap|` は **39 例**になる
（`dmapAt` の枝 141 回のうち、範囲内 67 のうち 39）。

⟹ **`dmapAt_bounds`（`k` が `|dmap|` か `|dmap|-1`）では閉じない。**
`d ≤ dmap[k]` と `dmap[k] ≤ |ST|` を**本当に**出す道具が要る。
なお 39 例すべてで `d ≤ dmap[k] ≤ |ST|` は成り立つ（違反 0）。

### 4.3 `k ≤ |dmap|`（`DmapInT` の第 1 項）は**証明できる**

`|dmap|` は「最後に処理したもとの柱の深さ + 1」である
（`st['dmap'] = st['dmap'][:c.1] + [dd]` なので、柱 `c` を処理した直後は
必ず `|dmap| = c.1 + 1`）。`cU` の直後にいるので、最後に処理した柱は
`blk[-1] = ([p]+A+U)[-1]` で、`|dmap| = blk[-1].1 + 1`。
一方 `pre[-1].1 = blk[-1].1 + 1 = |dmap|` で、`rest2.head` は
**行列の中で `pre[-1]` の直後の柱**である。BMS の標準形は
「隣の柱の行 0 は高々 +1」なので

    rest2.head.1 ≤ pre[-1].1 + 1 = |dmap| + 1
    ⟹ k = rest2.head.1 - 1 ≤ |dmap|                    ∎

実測（全母集団の `dmapAt` の枝 **9000 回超**）:

    k > |dmap|                                        **0 回**
    |dmap| = blk[-1].1 + 1（上の証明の要）  縮約の発火 全部で真
      gen3<=7  338/338 / 接頭辞族 <=9 列 1539/1539（不一致 **0**）
    rest2.head.1 ≤ pre[-1].1 + 1                      **0 違反**

⚠ Lean で厳密にやるときの注意: `|dmap| = blk[-1].1 + 1` は
「`cA` / `cU` が最後に処理したもとの柱が `blk[-1]` である」と同値だが、
`cA` の中で入れ子の縮約が起きると `q` と `pre` の列は**飛ばされる**ので、
「最後に処理した柱 = ブロックの最後の柱」は自明ではない。
実測は 100% だが、**帰納の 1 節として立てる**必要がある
（候補: `res.1 ≠ [] → res.2.dmap.length = (最後に処理した柱).1 + 1`、
すなわち `BlkOK` の節 5 `res.1.last.1 + 1 = |res.2.ST|` の `dmap` 版）。

---

## 5. Lean に渡せる不変量（全部「全 `conv3` 呼び出し」で測った）

`BlkOK d st res` に足す候補。**母数はすべて `conv3` の呼び出し回数**、
陽性対照は「わざと 1 だけきつくした版」。

### 節 9 — この呼び出しが**書いた** `dmap` の項は `d` 以上

    ∀ k < |res.2.dmap|,
      d ≤ res.2.dmap[k] ∨ (k < |st.dmap| ∧ res.2.dmap[k] = st.dmap[k])

| 母集団 | 母数 | 違反 | 陽性対照（`d+1 ≤`）|
|---|---:|---:|---:|
| `gen3 <=7` | 1139322 | **0** | 327552 |
| **`gen3 <=8`** | **13108043** | **0** | 3783921 |
| 展開閉包 | 1181746 | **0** | 366072 |
| 接頭辞族 <=10 列 (1) | 2200967 | **0** | 650235 |
| 接頭辞族 <=10 列 (2) | 4417583 | **0** | 1289112 |

### 節 10 — もとの深さ 1 段につき像は少なくとも 1 段（自分の根から下）

    M ≠ [] → ∀ j, M.headI.1 ≤ j < |res.2.dmap| →
               d + (j - M.headI.1) ≤ res.2.dmap[j]

| 母集団 | 母数 | 違反 | 陽性対照（`d+1+…`）|
|---|---:|---:|---:|
| `gen3 <=7` | 1139322 | **0** | 612758 |
| **`gen3 <=8`** | **13108043** | **0** | 7548369 |
| 展開閉包 | 1181746 | **0** | 1133149 |
| 接頭辞族 <=10 列 (1) | 2200967 | **0** | 1143016 |
| 接頭辞族 <=9 列 (森 488) | 288358 | **0** | 140946 |

どちらの節も、森が大量に出る母集団（接頭辞族 <=10 列, 森 5253）でも違反 0。

**節 10 が `DmapInT` の下界と `ResidBlkT2` の側条件を同時に出す。**
`k ≥ p.1 + 1` なので `dmap[k] ≥ d + (k - p.1) ≥ d + 1 > d`。

帰納で保たれる理由（コードから）:
`p` は `dmap[p.1] = dd ≥ d` を書き、`cA` は深さ `dd+1` で先頭 `p.1+1`、
`cU` は深さ `d+1` で先頭 `p.1`、`cB` は深さ `d` で先頭 `p.1`、
`cR` は深さ `rd` で先頭 `rest2.head.1`。どれも
`d' + (j - m') ≥ d + (j - p.1)` を満たす向きに**ずれている**。

### 節 11 — `dmap` の全項は `|ST|` 以下（`DmapInT` の上界）

    ∀ k < |st.dmap|, st.dmap[k] ≤ st.ST.length

| 母集団 | 母数（入口 ＋ 出口）| 違反 | 陽性対照（`< |ST|`）|
|---|---:|---:|---:|
| `gen3 <=7` | 1139322 x2 | **0** | 123 / 148 |
| `gen3 <=8` | 13108043 x2 | **0** | 605 / 632 |
| 展開閉包 | 1181746 x2 | **0** | 1966 / 178 |
| 接頭辞族 <=10 列 | 2200967 x2 | **0** | 228 / 384 |

さらに的撃ちの追試: `gen3 <=7` で `dmap[k] = |ST|` **ちょうど**になる行列 42 個を
**+3 列まで全数展開**（6928 個）しても破れ 0。

**課題 L12 の訂正**: L12 の表は「`∀ k ∈ dmap, k < |ST|`」を測って
6/32 の違反を報告し、「`dmapAt` の範囲内を `|ST|` で抑える道具は無い」と
結論した。しかし**不等号を `≤` にすると違反は 0** である。
`DmOK`（`dmap.last + 1 = |ST|`）と合わせると 節 11 は

    ∀ k < |dmap|, dmap[k] ≤ dmap.last + 1

と同値で、「`dmap` は最後の項より 1 だけ深いところまでは出っ張ってよい」。
狭義単調は**偽**のままだが、`≤ |ST|` は**真**である。これが L12 で
「道具が無い」と判定された箇所の答えである。

---

## 6. まとめ: `ImgBlockT3` に残る仮定の新しい形

    ResidBlkT   ✘ 偽（8 列の反例）  →  ResidBlkT2（`BlkOK d` ＋ 側条件）
    DmapInT     ✔ 真（違反 0）      →  節 10 ＋ 節 11 から出る
                                       第 1 項 `k ≤ |dmap|` は §4.3 で証明ずみ

    足すべき BlkOK の節: 節 9（弱）/ **節 10**（強・これだけでよい）/ **節 11**

節 10 と 節 11 はどちらも**全 `conv3` 呼び出し**で違反 0（陽性対照つき）。
`conv3` 側は**直さない**（反例の像は全部 `blockok 0` で DBMS 標準形）。

## 6.5 Lean 側への推奨（課題 L14 として）

### (a) `ResidBlkT2` は**仮定にしなくてよい**（帰納の中で証明できる）

側条件 `H(rest, rd, d) := ∀ c ∈ rest, d + rest.head.1 ≤ rd + c.1` は
`convResid` の再帰でそのまま保たれる:

    次の木の開始深さ  rd' = rd - (m0 - m1)   （m1 = tail.head.1）
    H(tail, rd', d) ⟺ ∀ c ∈ tail, d + m1 ≤ rd - m0 + m1 + c.1
                    ⟺ ∀ c ∈ tail, d + m0 ≤ rd + c.1     ← H(rest, rd, d) そのもの

しかも `H` から `c := rest.head` を取ると `d ≤ rd` が出て、
`rd - (m0 - m1) ≥ d ≥ 0` なので **`max(0, ·)` の切り詰めは起きない**
（実測でも全母集団で切り詰め 0 回）。`convResid` は `conv3 head` を
`|head| ≤ |rest| < |M|` で呼ぶので、`blkInv_aux` の**強い帰納の中に入る**。
⟹ `ResidBlkT` は**仮定から消せる**。

### (b) 残る Lean の仕事は `BlkOK` に 2 節足して帰納で通すこと

    節 10  M ≠ [] → ∀ j ≥ M.headI.1, j < |res.2.dmap| →
             d + (j - M.headI.1) ≤ res.2.dmap[j]
    節 11  ∀ k < |st.dmap|, st.dmap[k] ≤ st.ST.length     （状態の不変量。DmOK の仲間）

節 10 は帰納で素直に通るはず（`cA` は `dd+1` / 先頭 `p.1+1`、`cU` は `d+1` /
先頭 `p.1`、`cB` は `d` / 先頭 `p.1`、`cR` は `rd` / 先頭 `rest2.head.1` で、
どれも `d' + (j - m') ≥ d + (j - p.1)` の向きにずれている）。
**節 11 の帰納は未検討**で、ここが唯一の残り。実測は違反 0 だが証明は別である。

### (c) `DmapInT` は消える

    第 1 項 `k ≤ |dmap|`      §4.3（`|dmap| = pre[-1].1` ＋ BMS の隣接 +1）
    下界 `d ≤ dmap[k]`        節 10（`k ≥ p.1+1` を入れる）
    上界 `dmap[k] ≤ |ST|`     節 11

---

# 課題 R2: 森の枝で成り立つ**弱い形**（証明つき）

## R2-0. 結論

    (T1) 木ごとに「出した柱の深さ >= その木の開始深さ `rd_k`」   … 実測 違反 0
    (T2) 開始深さは**真に下がる**  `rd_{k+1} = rd_k - (m0_k - m0_{k+1})`, 差 >= 1
    (T3) どの木の `rd_k` も**外側の `d` 以上**                     … 実測 違反 0
    ⟹ `max(0, ·)` の切り詰めは**死んでいる**（証明できる。下）

`BlkOK_app'`（開始深さが下がってもよい連結、L13 で証明ずみ）を (T1)(T2)(T3) で
鎖状につなぐと、`convResid` 全体が **`BlkOK d`**（`rd` ではなく外側の `d`）を
満たす。これが求める弱い形である。

## R2-1. `rd` は真に下がる（証明）

`conv_resid` の切り方から

    i = 「`rest[i].1 < m0` になる最初の添字」（無ければ `len(rest)`）
    head = rest[:i],  tail = rest[i:]

なので **`tail ≠ [] → tail[0].1 < m0`**。したがって

    δ_k := m0_k - m0_{k+1} ≥ 1
    rd_{k+1} = max(0, rd_k - δ_k)

* もとの深さ `m0_k` は**真に下がる**（狭義単調減少）。
* 補題 A から `m0_k ≥ p.1 + 1` なので、
  **木の本数は高々 `rest[0].1 - p.1` 本**（ループは必ず止まる。実測の最大は 2 本）。
* 切り詰めが無ければ**望遠鏡的に** `rd_k = rd_1 - (m0_1 - m0_k)`。

## R2-2. `rd = 0` の穴は**塞がる**（証明）

切り詰めが起きると `rd_{k+1} = 0` になり、深さ 0 の柱が出て `BlkOK d`（`d ≥ 1`）が
壊れる。**しかし起きない。** 側条件

    (H)  ∀ c ∈ rest,  d + rest[0].1 ≤ rd + c.1

を仮定すると、`c := rest[0]` から `d ≤ rd`。さらに `c := 最後の木の頭` から

    rd_k = rd_1 - (m0_1 - m0_k) ≥ d ≥ 0

なので**引き算は負にならない**＝ `max(0, ·)` は恒等写像。しかも (H) は
再帰でそのまま保たれる:

    H(tail, rd - δ, d)
      ⟺ ∀ c ∈ tail, d + m0_{k+1} ≤ rd - m0_k + m0_{k+1} + c.1
      ⟺ ∀ c ∈ tail, d + m0_k ≤ rd + c.1        ← H(rest, rd, d) の tail への制限

**⟹ (H) 1 本で (T2)(T3) と「切り詰めなし」が全部出る。**

### 呼び出し点で (H) が出ること

補題 A（`∀ c ∈ rest2, p.1 + 1 ≤ c.1`）から (H) は

    d + rest2.head.1 ≤ rd + p.1 + 1

に落ちる。`rd` の 2 つの枝で:

* **`rd = d+1+e` の枝**: この枝の条件は `rest2 = []` または
  `rest2.head.1 = p.1+1`。後者なら `rest2.head` が `Aq` の**最小の深さ**なので
  **そもそも単一の木**（ループは 1 周で終わる）。式も
  `d + p.1+1 ≤ d+1+e + p.1+1` で自明。
* **`rd = dmapAt dmap k` の枝**（`k = rest2.head.1 - 1 ≥ p.1+1`）:
  `dmap[k] ≥ d + (k - p.1)` が要る。これが**節 10**である（§5, 実測 違反 0）。

**⟹ 森は `dmapAt` の枝でしか起きない。** 実測でも一致する
（`gen3 <=8`: 森 32 ⊂ 深い先頭 83、`rest2.head.1 = p.1+1` の 2303 件では森 0）。

## R2-3. Lean に渡す 1 行の命題

    def ResidBlkD : Prop :=
      ∀ (rest : TrioSeq) (d rd : ℕ) (Lr : List Lent) (ps pw : ℕ × ℕ)
        (st : St) (nx : Option Col) (off : ℕ),
        rd ≤ st.ST.length → DmOK st →
        (∀ c ∈ rest, d + (rest.headD (0,0,0)).1 ≤ rd + c.1) →
        BlkOK d st (convResid rest rd Lr ps pw st nx off)

`ResidBlkT` との違いは 2 つだけ:

    結論   `BlkOK rd` -> **`BlkOK d`**（外側の `conv3` の深さ）
    仮定   側条件 (H) が付く（呼び出し点で 補題 A ＋ 節 10 から出る）

しかも **`ResidBlkD` は仮定にしなくてよい**: `convResid` は
`conv3 head` を `|head| ≤ |rest| < |M|` で呼ぶので `blkInv_aux` の強い帰納の
中に入り、(H) の保存（R2-2）と `BlkOK_app'` で閉じる。
⟹ **`ImgBlockT3` は `ResidBlkD` を仮定に置かなくても無条件になる**
（残るのは `BlkOK` に節 10 と節 11 を足して帰納を回すこと）。

## R2-4. 森になる 32 例の全数（`gen3 <=8` 全数 781605 個）

    conv_resid の発火 2414 / 木 2418 / **森 32**（全部 **2 本**、3 本以上は 0）
    (T1) 木ごとに「出した柱の最小深さ >= その木の `rd`」  違反 **0**（64 木）
         陽性対照 `omin >= rd+1` は **64/64 で破れる**（＝ `omin = rd` ちょうど）
    (T2) `rd` は真に下がる / もとの深さ `m0` も真に下がる  違反 **0**
    (CLAMP) `rd_{k+1} = rd_k - (m0_k - m0_{k+1})` がそのまま  違反 **0**
    (T3) `min_k rd_k >= d`                                違反 **0**
         陽性対照: `>= d+3` で 24/32、`>= d+4` で 32/32 が破れる（余裕はちょうど 2〜3）
    (H)  側条件 `∀ c ∈ rest, d + rest[0].1 <= rd + c.1`   違反 **0**（同じ余裕）

32 例は**同じ縮約の場所**（接頭辞 `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)`）で、
末尾 2 列だけが違う。`d` は全部 1。

| # | 行列 | d | rest | 木ごとの (m0, rd, 出した柱の最小深さ) |
|--:|---|--:|---|---|
| 1 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,0,0)(2,0,0)` | 1 | `(3,0,0)(2,0,0)` | (3,4,4) (2,3,3) |
| 2 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,0,0)(2,1,0)` | 1 | `(3,0,0)(2,1,0)` | (3,4,4) (2,3,3) |
| 3 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,0,0)(2,2,0)` | 1 | `(3,0,0)(2,2,0)` | (3,4,4) (2,3,3) |
| 4 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,0,0)(2,2,1)` | 1 | `(3,0,0)(2,2,1)` | (3,4,4) (2,3,3) |
| 5 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,1,0)(2,0,0)` | 1 | `(3,1,0)(2,0,0)` | (3,4,4) (2,3,3) |
| 6 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,1,0)(2,1,0)` | 1 | `(3,1,0)(2,1,0)` | (3,4,4) (2,3,3) |
| 7 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,1,0)(2,2,0)` | 1 | `(3,1,0)(2,2,0)` | (3,4,4) (2,3,3) |
| 8 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(3,1,0)(2,2,1)` | 1 | `(3,1,0)(2,2,1)` | (3,4,4) (2,3,3) |
| 9 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,0,0)(2,0,0)` | 1 | `(4,0,0)(2,0,0)` | (4,5,5) (2,3,3) |
| 10 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,0,0)(2,1,0)` | 1 | `(4,0,0)(2,1,0)` | (4,5,5) (2,3,3) |
| 11 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,0,0)(2,2,0)` | 1 | `(4,0,0)(2,2,0)` | (4,5,5) (2,3,3) |
| 12 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,0,0)(2,2,1)` | 1 | `(4,0,0)(2,2,1)` | (4,5,5) (2,3,3) |
| 13 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,0,0)(3,0,0)` | 1 | `(4,0,0)(3,0,0)` | (4,5,5) (3,4,4) |
| 14 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,0,0)(3,1,0)` | 1 | `(4,0,0)(3,1,0)` | (4,5,5) (3,4,4) |
| 15 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,1,0)(2,0,0)` | 1 | `(4,1,0)(2,0,0)` | (4,5,5) (2,3,3) |
| 16 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,1,0)(2,1,0)` | 1 | `(4,1,0)(2,1,0)` | (4,5,5) (2,3,3) |
| 17 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,1,0)(2,2,0)` | 1 | `(4,1,0)(2,2,0)` | (4,5,5) (2,3,3) |
| 18 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,1,0)(2,2,1)` | 1 | `(4,1,0)(2,2,1)` | (4,5,5) (2,3,3) |
| 19 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,1,0)(3,0,0)` | 1 | `(4,1,0)(3,0,0)` | (4,5,5) (3,4,4) |
| 20 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,1,0)(3,1,0)` | 1 | `(4,1,0)(3,1,0)` | (4,5,5) (3,4,4) |
| 21 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(2,0,0)` | 1 | `(4,2,0)(2,0,0)` | (4,5,5) (2,3,3) |
| 22 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(2,1,0)` | 1 | `(4,2,0)(2,1,0)` | (4,5,5) (2,3,3) |
| 23 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(2,2,0)` | 1 | `(4,2,0)(2,2,0)` | (4,5,5) (2,3,3) |
| 24 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(2,2,1)` | 1 | `(4,2,0)(2,2,1)` | (4,5,5) (2,3,3) |
| 25 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(3,0,0)` | 1 | `(4,2,0)(3,0,0)` | (4,5,5) (3,4,4) |
| 26 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(3,1,0)` | 1 | `(4,2,0)(3,1,0)` | (4,5,5) (3,4,4) |
| 27 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,1)(2,0,0)` | 1 | `(4,2,1)(2,0,0)` | (4,5,5) (2,3,3) |
| 28 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,1)(2,1,0)` | 1 | `(4,2,1)(2,1,0)` | (4,5,5) (2,3,3) |
| 29 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,1)(2,2,0)` | 1 | `(4,2,1)(2,2,0)` | (4,5,5) (2,3,3) |
| 30 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,1)(2,2,1)` | 1 | `(4,2,1)(2,2,1)` | (4,5,5) (2,3,3) |
| 31 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,1)(3,0,0)` | 1 | `(4,2,1)(3,0,0)` | (4,5,5) (3,4,4) |
| 32 | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,1)(3,1,0)` | 1 | `(4,2,1)(3,1,0)` | (4,5,5) (3,4,4) |

読み方: `(m0, rd, 出した柱の最小深さ)`。**3 つ目は必ず 2 つ目と同じ**
（木の頭の柱がちょうど開始深さに置かれる）。`rd` は `m0` と同じ幅だけ下がる。

## R2-5. 木の本数は**いくらでも増える**（上界は R2-1 のとおり tight）

R2-1 の上界

    木の本数 ≤ rest[0].1 - p.1        （`m0_k` が狭義単調減少 ＋ `m0_k ≥ p.1+1`）

は**達成される**。手で作った族で確かめた（どれも BMS 標準形）:

| 列数 | 行列 | 木 | `(m0, rd, 最小深さ)` |
|--:|---|--:|---|
| 8 | `…(3,1,0)(3,0,0)(2,0,0)` | 2 | (3,4,4) (2,3,3) |
| **9** | `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,0,0)(3,0,0)(2,0,0)` | **3** | (4,5,5) (3,4,4) (2,3,3) |
| **12** | `(0,0,0)(1,1,1)(2,1,0)(3,1,0)(1,1,0)(2,2,1)(3,2,0)(4,1,0)(5,0,0)(4,0,0)(3,0,0)(2,0,0)` | **4** | (5,6,6) (4,5,5) (3,4,4) (2,3,3) |

* 9 列の族は 32 個（`(4,*,*)(3,*,*)(2,*,*)` の全組み合わせのうち標準形）、
  **32/32 が 3 本**。12 列の族は 96 個で **96/96 が 4 本**。
* どの場合も (T1)(T2)(T3)(CLAMP)(H) の違反 **0**、
  陽性対照 `omin ≥ rd+1` は **全木で破れる**（＝ `omin = rd` ちょうど）。
* `rd` は `m0` とまったく同じ幅だけ下がり、`min_k rd_k - d = 2` で一定。

⟹ **Lean 側は「木は 1 本か 2 本」の場合分けにしてはいけない。**
`convResid` の再帰をそのまま帰納で回す必要がある（`BlkOK_app'` の鎖）。
本数は行列の深さとともに増える。

## R2-6. **9 列の全数**（7755191 個。誰も測っていなかった領域）

`r2gen8.py` で 8 列全数 704323 個を 1 行列 24 バイトの生バイト列にし、
`r2col9.py` で 9 列目を足しながら**貯めずに流す**（4 分割、各 1830 秒）。

    候補 14751 千（4 shard 合計）  ->  **9 列の標準形 7755191 個 全数**
    conv_resid の発火 **15027** / 木 **15402**
      森 **456**（2 本 **424** / **3 本 32** / 4 本以上 0）

| 主張 | 母数 | 違反 | 陽性対照 |
|---|---:|---:|---|
| (T1) 木ごとに `出した柱の最小深さ ≥ rd_k` | 15402 木 | **0** | `≥ rd_k+1` が **15402/15402 で破れ** |
| (T2) `rd` が真に下がる / `m0` が真に下がる | 456 森 | **0** | — |
| (CLAMP) `rd_{k+1} = rd_k - (m0_k - m0_{k+1})` | 456 森 | **0** | — |
| (T3) `min_k rd_k ≥ d` | 456 森 | **0** | `≥ d+3` が 356/456、`≥ d+4` が **456/456 で破れ** |
| (H) `∀ c ∈ rest, d + rest[0].1 ≤ rd + c.1` | 456 森 | **0** | 同上 |

* **`omin = rd` は 944 木すべてで等号**（木の頭の柱はちょうど開始深さに置かれる）。
* 余裕 `min_k rd_k - d` は **2 が 356 件 / 3 が 100 件**。`d` は森では**全部 1**。
* **9 列で 3 本が出る**（32 件）。形は
  `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,*,*)(3,*,*)(2,*,*)` で、
  `rd` は 5 -> 4 -> 3、`m0` は 4 -> 3 -> 2。
* 12 列では **4 本**（R2-5）。⟹ **本数に定数の上界は無い。**

### 測っていない場合分け（正直に）

    10 列以上の全数           … 未測定（9 列で 1830 秒 x 4 なので 10 列は 1 日仕事）
    5 本以上の木               … 未観測。ただし R2-1 の上界は `rest[0].1 - p.1` で
                                 深さとともに伸びるので、**あると考えるべき**
    `d ≥ 2` の森               … 未観測（森の `d` は 8 列でも 9 列でも全部 1）
    `min_k rd_k - d = 0 or 1` … 未観測（余裕はいつも 2 以上）

---

# 課題 R3 の前段: L16 の 3 件を `<=8` 列 ＋ 展開閉包で測り直した（2026-08-28）

計測は `tools/dbms/r3.py`（`rows3.py` は無改変。呼び出し点は**呼び出し元
フレームの行番号**で見分ける）。母数は **`conv3` の全呼び出し**。

| 母集団 | 母数（行列） | `conv3` 呼び出し |
|---|---:|---:|
| `gen3 <=7` 全数 | 77282 | 530680 |
| **`gen3 <=8` 全数** | **781605** | **6160775** |
| `<=6` の展開閉包（最長 24 列）| 46855 | 562160 |

## R3-0. (i) 節 12'「`∀ k < p.1, st.dmap[k] ≤ d`」は**偽**。`d+1` なら真

**最小反例は 5 列**（`gen3 <=4` では 0、`<=5` で 1 件）:

    M = (0,0,0)(1,1,1)(1,0,0)(2,1,1)(2,0,0)
    ブロックの頭 p = (2,0,0)、d = 2、st.dmap = [0,3]、k = 1
    dmap[1] = 3 > d = 2

| 主張 | `gen3 <=7` | **`gen3 <=8`** | 展開閉包 |
|---|---:|---:|---:|
| `st.dmap[k] ≤ d - 1`（陽性対照）| 449 | 3471 | 7361 |
| **`st.dmap[k] ≤ d`** | **50** | **284** | **1876** |
| **`st.dmap[k] ≤ d + 1`（修正版）** | **0** | **0** | **0** |
| `= d + 1` ちょうど（タイトさ）| 50 | 284 | 1876 |

⟹ **節 12'' `∀ k < p.1, st.dmap[k] ≤ d + 1` を使うこと。**
L2 が要るのは `st.dmap[k] ≤ dd2 + 1` で、`d ≤ dd2` があるので
`dmap[k] ≤ d + 1 ≤ dd2 + 1` で**そのまま通る**。境界は**タイト**
（破れはすべてちょうど `d+1`）。

## R3-1. (ii) `Dm12`（保存）は `rA` の形が**偽**。含意にすれば真

L2 の形:

    Dm12 m st res := ∀ k, k < m → k < |res.2.dmap| →
                       **k < |st.dmap| ∧** res.2.dmap[k] = st.dmap[k]

| 主張 | `gen3 <=7` | **`gen3 <=8`** | **展開閉包** | 陽性対照 |
|---|---:|---:|---:|---:|
| `rA` が `< p.1+1` を保つ | 0 | **11** | **349** | `< p.1+2` で 3580273 / 369884 |
| `rB` が `< B.head.1` を保つ | 0 | **0** | **0** | 298921 / 19354 |
| 任意のブロックで `< M.head.1` | 4 | 72 | 516 | 4663273 / 442003 |
| **弱い版（`k < |st.dmap| →` の含意）** | **0** | **0** | **0** | — |

`rA` の最小反例（**8 列**）:

    M = (0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,0,0)(5,0,0)
    `cA` の呼び出し、p.1 = 4、k = 4
    呼び出し前 dmap = [0,3,4,5]（長さ 4）、後 [0,3,4,5,6]（長さ 5）
    k = 4 < p.1+1 = 5、k < |res.dmap| = 5、だが k は |st.dmap| = 4 の**外**

**値は 1 つも変わっていない。落ちるのは `k < |st.dmap|` の連言だけ。**
⟹ 結論を

    ∀ k, k < m → k < |res.2.dmap| → **k < |st.dmap| →** res.2.dmap[k] = st.dmap[k]

に直せば全母集団で違反 0。`cols` の直後は `|dmap| = p.1+1` なので、
`Dm10_app` が使う `j = p.1` の場所では添字は必ず存在し、**証明は変わらない**。
`gen3 <=7` だけでは 0 だったので、**8 列と展開閉包で初めて出た**（今日 3 度目）。

## R3-2. (iii) `BlkOK_app` の側条件 6 本は**全部真**（`<=8` 列 ＋ 展開閉包）

| # | 連結点 | 条件 | `<=7` | **`<=8`** | 閉包 | 陽性対照（1 段きつく）|
|---|---|---|---:|---:|---:|---:|
| a1 | 縮約 `cA` | `A.head.1 ≤ p.1+1` | 0 | **0** | 0 | 37 / 354 / 4665 |
| a3 | 縮約 `cU` | `U.head.1 ≤ p.1+1` | 0 | **0** | 0 | 0 / 50 / 1264 |
| **a4** | 縮約 `cR` | `d + rest2.head.1 ≤ rd + p.1` | 0 | **0** | 0 | **等号 369/2386**・対照 369 |
| a5 | 縮約 `cB` | `Bq.head.1 ≤ p.1` | 0 | **0** | 0 | 98 / 864 / 57 |
| b1 | 非縮約 `cA` | `A.head.1 ≤ p.1+1` | 0 | **0** | 0 | 304386 / 3579919 / 364983 |
| b2 | 非縮約 `cB` | `B.head.1 ≤ p.1` | 0 | **0** | 0 | 148533 / 1795537 / 139095 |

**a4 はタイト**（`<=8` 列で 2386 発火中 369 が等号、1 段きつくすると同じ 369 が落ちる）。

---

# 課題 R3: (S2-b) — `r'` は **t=2 では書ける。t=1 では書けない**（2026-08-28）

計測は `tools/dbms/r3s2.py`（`g2/provc.py` の `PROV` を使う。
`provc` の像が `rows3.b2d3` と一致することを `gen3 <=7` 全数 77282 個で確認: **差 0**）。

## R3-3. 答え: `r'` は**像の行 0 の親を `d0` 段のぼった柱**

    r' = anc0(B, |B|-1, d0)          （`anc0` は行 0 の親の鎖）
       ⟺ B[|B|-1].0 - B[r'].0 = d0   （＝ d0' = d0）

| 母集団 | `t=0` | **`t=2`** | `t=1` |
|---|---:|---:|---:|
| `gen3 <=6`（母数 7368）| **0 破れ**（2696）| **0 破れ**（1616）| 771/3056 が `d0'=d0+1` |
| `gen3 <=7`（母数 68894）| **0 破れ**（24113）| **0 破れ**（16046）| 7849/28735 |
| **`gen3 <=8`（母数 704322）**| **0 破れ**（235633）| **0 破れ**（173802）| 85199/294887 |
| **展開閉包**（母数 46651）| **0 破れ**（8083）| **0 破れ**（14917）| 2099/23651 |

* **`t = 2`（3 行の困難の本体）では `r'` は完全に決まる。**
  `t' = t`・`d1' = d1`（H13）と合わせて **(S2) は `t=2` で閉じる**。
* `t = 1` でも **`d0' - d0 ∈ {0, 1}` で、2 以上は一度も出ない**（4 母集団すべて。
  `gen3 <=8` の 294887 例を含む）。
* `t = 1` の必要条件（どちらも一方向だけ、母数全部で例外 0）:

      「`r` と末尾のあいだの行 0 の祖先に 行 1 のタイが無い」  ⟹ d0' = d0
      「(r, last] に `行 1 = 末尾の行 1` かつ `z > 0` の列が無い」 ⟹ d0' = d0

  逆は成り立たない（タイがあっても `d0' = d0` が 583 / 9996 / 7091 件）。

## R3-4. `t = 1` は**書けない** — 同じ悪い部分で `d0'` が割れる 2 例

`gen3 <=7` 全数で「`(t, 悪い部分 A[r..], d0, d1)` が同じなのに `d0'` が違う」組を
探すと **3 組**（45166 組中）。最小のもの（**7 列**）:

    A  = (0,0,0)(1,1,1)(2,1,0)**(1,1,0)**(2,0,0)(3,1,1)(4,1,0)   d0' - d0 = **1**
    A' = (0,0,0)(1,1,1)(2,1,0)**(1,1,1)**(2,0,0)(3,1,1)(4,1,0)   d0' - d0 = **0**

    どちらも t = 1、r = 4、悪い部分 = (2,0,0)(3,1,1)(4,1,0)、d0 = 2、d1 = 1

**違うのは悪い部分の外、第 3 列の `z` だけ**である。
⟹ **`t=1` の `r'` は `(t, r, d0, d1)` と悪い部分からは決まらない。**
`conv3` が梯子の影 `sh1` をどこに置いたかに依存し、それは `r` より前の
接頭辞で決まる（H13 の「影の柱は `r` と末尾のあいだの列のもの」の正体）。

他の 2 組も同じ形（`z` を 0 -> 1 に替えると割れる）:

    (0,0,0)(1,1,1)(2,1,0)(3,0,0)(4,1,1)(5,1,0)  vs  (0,0,0)(1,1,1)(2,1,1)(3,0,0)(4,1,1)(5,1,0)
    (0,0,0)(1,1,1)(2,1,0)(3,1,0)(4,0,0)(5,1,1)(6,1,0)  vs  (0,0,0)(1,1,1)(2,1,1)(3,1,1)(4,0,0)(5,1,1)(6,1,0)

## R3-4b. ついでに測った 2 本（母数 `gen3<=7` 68894 ＋ 展開閉包 46651）

    (S2-a) t' = t      **違反 0**（両母集団、t=0/1/2 すべて）
    (S2-c) d1' = d1    **違反 0**（t=1 と t=2。t=0 は行 1 を使わないので空虚）
           陽性対照 `d1' = d1+1` は **母数の全部で破れる**

**ただし悪い部分の長さは保たれない。** `bp = last - r`, `bp' = |B|-1-r'` として

| | `bp'-bp` の分布（`gen3<=7`）|
|---|---|
| t=2 | 0: 15728 / **+1: 315** / +2: 3 |
| t=1 | -2: 10 / -1: 56 / 0: 14442 / **+1: 13856** / +2: 368 / +3: 3 |
| t=0 | -1: 6 / 0: 16419 / +1: 1638 / **+2: 5681** / +3: 364 / +4: 5 |

⟹ **像の悪い部分は 0〜2 本ぶん長い**（梯子の影が入るぶん）。
`SandwichUT3` で「悪い部分を n 回写す」を突き合わせるときは、
**`bp` は保存量ではない**ことに注意。`t=2` でも 318/16046 = 2% で伸びる。

## R3-5. Lean に渡す形

    (S2-b')  t ≠ 1 → B[|B|-1].0 - B[r'].0 = d0
             （＝ `r'` は `B` の行 0 の親を `d0` 段のぼった柱）
    (S2-b1)  t = 1 → d0' ∈ {d0, d0+1}

`t=2` は無条件。`t=1` は上限つきの非決定で、**悪い部分だけからは決まらない**
（反例あり）。`SandwichUT3` を `t=1` でも閉じたいなら、
**接頭辞まで見る量（梯子の影の位置）を (S2) の入力に足す**しかない。

---

# 課題 L18 の 2 件: 節 9 は**範囲を絞れば `d+1` まで強められる**（2026-08-28）

計測は `tools/dbms/r4.py`（`rows3.py` 無改変）。母数は **`conv3` の全呼び出し**。

## R4-1. 節 9 のタイトさ — 破れはほぼ全部**自分が書いた項 `k = p.1`**

    節 9      ∀ k < |res.2.dmap|,
                d ≤ res.2.dmap[k] ∨ (k < |st.dmap| ∧ res.2.dmap[k] = st.dmap[k])

| 版 | 全 `k` | **`k > p.1`** | `k = p.1` | `k < p.1` |
|---|---:|---:|---:|---:|
| **`gen3 <=7`（530680 呼び出し）** | | | | |
| `d ≤ …` | **0** | **0** | **0** | **0** |
| `d+1 ≤ …` | 327552 | **0** | 327548 | 4 |
| `d+2 ≤ …`（陽性対照）| 483769 | **120849** | 362916 | 4 |
| **`gen3 <=8` 全数（6160775 呼び出し）** | | | | |
| `d ≤ …` | **0** | **0** | **0** | **0** |
| `d+1 ≤ …` | 3783897 | **0** | 3783826 | 71 |
| `d+2 ≤ …`（陽性対照）| 5627364 | **1499189** | 4128103 | 72 |
| **展開閉包（562160 呼び出し）** | | | | |
| `d ≤ …` | **0** | **0** | **0** | **0** |
| `d+1 ≤ …` | 365874 | **0** | 365358 | 516 |
| `d+2 ≤ …`（陽性対照）| 609442 | **205389** | 403537 | 516 |

⟹ **節 9+**（L2 が使うべき形）:

    ∀ k, **p.1 < k** < |res.2.dmap| →
      d + 1 ≤ res.2.dmap[k] ∨ (k < |st.dmap| ∧ res.2.dmap[k] = st.dmap[k])

**`k > p.1` に絞れば `d+1` で違反 0、`d+2` にすると破れる**（タイト）。
理由も素直で、`k > p.1` の項を書くのは `cA`（深さ `dd2+1`）と `cU`（深さ `d+1`）
だけなので必ず `d+1` 以上になる。全 `k` で破れる 32 万件は
`dmap[p.1] = dd2` が `dd2 = d` になりうる点そのものである。

`DmapInT` が要る `k = rest2.head.1 - 1 ≥ p.1 + 1` は**この範囲に入る**。

`k < p.1` の破れ（4 / 516）は `conv_resid` の木の呼び出しで、
**呼び出し前に無かった添字が生える**ケース:

    M の頭 = (4,2,0)（深さ 4）、d = 5、dmap 前 [0,3,4]（長さ 3）、後 [0,3,4,5]
    k = 3 は |dmap 前| = 3 の外なので「保存」が使えず、`d+1 = 6 ≤ 5` が偽
    （`d ≤ 5` は真なので 節 9 そのものは成り立つ）

`k > p.1` に絞れば関係ない。

## R4-2. `d + 1 + (p.1 - U.head.1) ≤ dd2` は**真**。ただし `p.1 - U.head.1 = 0`

| 母集団 | `cU` の発火（`U ≠ []`）| `U.head.1 = p.1` | 違反 | 等号 | 陽性対照（`dd2-1`）|
|---|---:|---:|---:|---:|---:|
| `gen3 <=7` | 6 | 6/6 | **0** | 1 | 1 |
| **`gen3 <=8`** | **50** | **50/50** | **0** | 6 | 6 |
| 展開閉包 | **1264** | **1264/1264** | **0** | 155 | 155 |

`units_split` は深さ `p.1` の柱から取るので **`U.head.1 = p.1` は構造から真**。
したがって条件は **`d + 1 ≤ dd2`**（＝ `d < dd2`）に潰れる。
縮約の枝は `lad0 = true` なので `dd = d + 1` から始まり、`lad1` があればさらに +1。
⟹ **`d + 1 ≤ dd2` は縮約の枝では構造から出る**（`lad0` の取り出しは
`blk_contr` で既にやっている手）。`dd2 = d+1` の場合があるので**タイト**で、
余裕は使えない。

## R4-3. L18 の「鎖が閉じない」は**向きの誤り**。4 つの再帰は外側で揃う

`Dm10` は **`res.2.dmap`（出口の状態）しか見ない**ので**入口の前提が無い**。
だから「`cA` の出口を `cU` の入口に移す」必要はそもそも無い。要るのは逆向きの

    各再帰の帰納法の仮定 `Dm10 d' m'`  ⟹  外側の `Dm10 d p.1`
    に要る不等式は   d + (j - p.1) ≤ d' + (j - m')  ⟺  **d + m' ≤ d' + p.1**

で、これは**課題 L16 の側条件そのもの**（`m_X = p.1`, `m_Y = m'`）。

`tools/dbms/r5.py` で**呼び出し点ごとに直接測った**（`Dm10 d_outer p.1 (r*.2)`）:

| 呼び出し点 | `<=7` 発火 | 違反 | **`<=8` 発火** | **違反** | 閉包 発火 | 違反 | 陽性対照（外側 `d` を `+1`）`<=7`/`<=8`/閉包 |
|---|---:|---:|---:|---:|---:|---:|---|
| `rA`（非縮約）| 530342 | **0** | **6158361** | **0** | 556924 | **0** | 619386 / **7805196** / 1007683 |
| `rA`（縮約）| 338 | **0** | **2414** | **0** | 5236 | **0** | 0（`dd2+1 ≥ d+1` で構造的に強い）|
| `rU` | 338 | **0** | **2414** | **0** | 5236 | **0** | 0（`d+1` で呼ぶので同上）|
| **`rR`** | 338 | **0** | **2414** | **0** | 5236 | **0** | **101 / 567 / 5541** |
| `rB`（縮約）| 338 | **0** | **2414** | **0** | 5236 | **0** | 147 / **1038** / 5600 |
| `rB`（非縮約）| 530342 | **0** | **6158361** | **0** | 556924 | **0** | 612611 / **7547283** / 1126497 |

**6 か所すべて違反 0。** 隙間（`p.1 ≤ j < m'`）が残るのは `rA`（`j = p.1` の 1 点）と
`rR`（`[p.1, rest2.head.1)`）だけで、`rU`（`m' = p.1`）と `rB`（`m' ≤ p.1`）は隙間なし。
⟹ **L17 の「`(d, m)` を揃える」設計はそのまま通る。**

---

# 課題 R6: a4 の**たるみ**と、ブロックの先頭の深さは**等号**（2026-08-29）

計測は `tools/dbms/r6.py`（`rows3.py` 無改変）。母数は `conv3` の全呼び出し。

## R6-1. 側条件 a4 の**たるみは 0 か 1 だけ**

    slack := (rd + p.1) - (d + rest2.head.1)      （a4 は slack ≥ 0）

| 母集団 | `conv_resid` 発火 | slack=0 | slack=1 | **slack≥2** | a4 の破れ | 陽性対照（slack=0 を要求）|
|---|---:|---:|---:|---:|---:|---:|
| `gen3 <=7` 全数 | 334 | 73 | 261 | **0** | **0** | 261 |
| **`gen3 <=8` 全数** | 2386 | 369 | 2017 | **0** | **0** | 2017 |
| 展開閉包（46855, 最長 24 列）| 5139 | 3012 | 2127 | **0** | **0** | 2127 |

**たるみの最大値は 1、最小値は 0**（3 母集団すべて）。分布は 0 と 1 の
**2 つのバケツしか立たない**（他の値のカウンタが 1 つも生えない）。
⟹ **`rd + p.1 ≤ d + 1 + rest2.head.1` は真**（L2 の入口側の 1 本）。

## R6-2. ブロックの先頭の深さは `≤` ではなく **`=`**

| 呼び出し点 | 主張 | `gen3 <=7` | **`gen3 <=8`** | 展開閉包 |
|---|---|---|---|---|
| `cA`（縮約）| `A.head.1 = p.1 **+1**` | 37/37 | **354/354** | 4665/4665 |
| `cA`（非縮約）| `A.head.1 = p.1 **+1**` | 304386/304386 | **3579919/3579919** | 364983/364983 |
| `cU` | `U.head.1 = p.1` | 6/6 | **50/50** | **1264/1264** |
| **`cB`（縮約 `Bq`）**| **`Bq.head.1 = p.1`** | 98/98 | **864/864** | 57/57 |
| `cB`（非縮約 `B`）| `B.head.1 = p.1` | 148533/148533 | **1795537/1795537** | 139095/139095 |

（母数は**ブロックが空でない**呼び出しだけ。空のブロックには先頭が無いので判定しない。）

**全部違反 0。** 判定は「`head.1 - p.1` の値ごとにバケツを立てる」形なので、
**バケツが 1 つしか立たない**ことが陽性対照そのものである
（`= p.1 - 1` / `= p.1 + 1` などのバケツは 3 母集団すべてで **0 件**）:

    A_contr  {+1: 37}      {+1: 354}      {+1: 4665}
    A_plain  {+1: 304386}  {+1: 3579919}  {+1: 364983}
    U        {+0: 6}       {+0: 50}       {+0: 1264}
    Bq       {+0: 98}      {+0: 864}      {+0: 57}
    B        {+0: 148533}  {+0: 1795537}  {+0: 139095}

### 構造的な理由（Lean で使える）

> **不変量**: `conv3` に渡るブロックは、どの柱も**先頭の柱の深さ以上**である。

* 入口 `Mo` は根 `(0,0,0)` から始まり全柱 `≥ 0`。
* `A = split0(p, r).1` は「深さ `> p.1`」の極大接頭辞。BMS の隣接条件
  （行 0 は高々 +1）から **`A.head.1 = p.1 + 1`**、A の全柱は `> p.1 = A.head.1 - 1`。
* `B = split0(p, r).2` は「深さ `≤ p.1`」で切った残り。もとのブロックの全柱が
  `≥ p.1` なので `B.head.1 ≤ p.1` と合わせて **`= p.1`**。B の全柱も `≥ p.1`。
* `U = units_split(p, B).1` は深さちょうど `p.1` の柱から取るので **`U.head.1 = p.1`**。
* `Bq = split0(q, r2).2` も同じ（`q.1 = p.1`）で **`= p.1`**。

唯一この性質を持たないのが **`cR` に渡る `rest2`（森）** だが、
`conv_resid` が木ごとに切ってから `conv3` を呼ぶので `conv3` の側では保たれる。

⟹ `Dm10_of_child` の前提は `≤` ではなく **`=`** で書ける（`rU` の隙間が消える）。

---

# 課題 R7: **`SeqEmbT3` は偽**（＝ `OrderT3` も偽）。反例は 8 列 + 9 列（2026-08-29）

計測は `tools/dbms/r7.py`（`SeqEmbT3`）と `tools/dbms/r8.py`（接頭辞単調）。

## R7-0. 測り方（母数の 2 乗は要らない）

`seqlex` は **Python のタプルの自然順序とまったく同じ**:

    seqlex [] N          = (N ≠ [])                    ⟺ () < N
    seqlex (p::M) []     = False                       ⟺ ¬((p,…) < ())
    seqlex (p::M) (q::N) = collt p q ∨ (p=q ∧ seqlex M N) ⟺ タプルの辞書式

しかも `seqlex` は**狭義全順序**（三分律）なので

    「全対で `seqlex M N ↔ seqlex (f M) (f N)`」
      ⟺ 「`M` の昇順に並べたとき像も**狭義単調増加**」（推移律で隣だけ見れば十分）

**裏取り**: 無作為の非隣接対 199994 個を直に判定 -> 破れ 0（隣接だけで十分なことの確認）。

## R7-1. 母集団は **`ST_TS` 展開閉包**（`gen3` ではない）

対角 `diag(3,v,zcap=1)` から `n = 1,2,3` の展開で到達できるもの。

| 母集団 | 個数 | **(→) の破れ** | **(←) の破れ** |
|---|---:|---:|---:|
| `v<=4, len<=9` | 44063 | **0** | **0** |
| **`v<=5, len<=10`** | **416607** | **41** | **38** |

陽性対照（`v<=4, len<=9`、隣接 44062 対）:

    P1 向きを逆（像が狭義**減少**を要求）   破れ **44062 / 44062**
    P2 像の末尾 1 柱を落とす                破れ 23741 / 44062
    P4 像を逆順に並べ替える                 破れ 18848 / 44062
    P3 `f = 恒等`（**負**の対照）           破れ **0 / 44062**   ← 期待どおり 0

## R7-2. 最小の反例（**8 列 + 9 列**）

    M1 = (0,0,0)(1,1,1)(1,1,0)(2,2,1)(2,1,0)(3,0,0)(4,1,1)(4,1,0)
    M2 = M1 ++ (5,1,0)                    （＝ M1 は M2 の**真の接頭辞**）

    seqlex M1 M2 = **真**

    f M1 = (0,0,0)(1,0,0)(2,1,0)(3,2,1)(3,1,0)(4,0,0)(5,1,0)(6,2,1)**(6,2,0)**
    f M2 = (0,0,0)(1,0,0)(2,1,0)(3,2,1)(3,1,0)(4,0,0)(5,1,0)(6,2,1)**(5,1,0)**(6,1,0)

    第 8 柱で `(6,2,0)` vs `(5,1,0)`。`collt` は 6 > 5 なので
    seqlex (f M1) (f M2) = **偽**            ⟹ **`SeqEmbT3` の破れ**

どちらも BMS 標準形（`core.isstd`）で、**どちらも `ST_TS`**。
対角 `diagSeqT 0 2 = (0,0,0)(1,1,1)(2,2,1)` からの展開の道を復元した（16 手 / 17 手）。
最後の 3 手だけ書くと、共通の祖先

    X = (0,0,0)(1,1,1)(1,1,0)(2,2,1)(2,1,0)(3,0,0)(4,1,1)(4,1,0)(5,2,1)

から **`M1 = X⟦1⟧`**、**`M2 = X⟦2⟧⟦2⟧`** である。

`OrderT3 ↔ SeqEmbT3`（`OrderT3_iff_seqemb`、証明ずみ）なので
**`OrderT3` も偽**である。

## R7-3. 破れ 41 件の内訳（`v<=5, len<=10`）

| | 件数 |
|---|---:|
| **`M1` が `M2` の真の接頭辞** | **37** |
| そうでない | 4 |
| **縮約が 1 度も発火しない**（`nc(M1)=nc(M2)=0`）| **39** |
| 縮約が発火（`nc=1` 同士）| 2 |
| 像が一致（単射の破れ）| **0** |
| 列数 (8,9) / (9,10) / (10,8) / (10,9) | 13 / 24 / 3 / 1 |

⟹ **縮約は犯人ではない。** 犯人は**分岐列 `(a,1,0)` の綴りが「次の列」に依存する**こと。
反例では最後の列 `(4,1,0)` が

    行列の末尾のとき      深く綴られる  `(6,2,0)`
    後ろに `(5,1,0)` が来ると 浅く綴られる  `(5,1,0)`

**両方の列に 8 列と 9 列が要る**ので、`gen3 <=8` の突き合わせでは**原理的に見えない**
（両方 `<=8` の対は 41 件中 **0 件**）。`gen3 <=9`（7755191 個）か
`ST_TS len<=10` が最小の母集団である。

## R7-4. 接頭辞単調の破れの構造（課題 (4)）

`b2d3(M[:k])` が `b2d3(M)` の接頭辞にならない `(M, k)` を全数で分解した。

| | `gen3 <=6` | **`gen3 <=7`** |
|---|---:|---:|
| `(M, k)` の対 | 40743 | 454113 |
| 接頭辞になる | 38625 | 426024 |
| **破れ** | 2118 | **28089** |
| 破れる行列 | — | **22624 / 77282**（`NOTES` の 22635 とほぼ一致）|

**破れの形はきわめて規則的**（`gen3 <=7`, 破れ 28089 件）:

| 読み | 件数 |
|---|---:|
| **ずれ始めの柱の種類が `body`** | **28089（100%）** |
| **ずれ始めが縮約の中** | **0（0%）** |
| ずれるのは像の**末尾 1 柱**だけ | 27708（98.6%）|
| 末尾 2 柱 / 3 柱 / 4 柱 | 295 / 79 / 7 |
| **ずれ始めの入力列 = `M[:k]` の末尾**（0 本目）| **27708（98.6%）**|
| 末尾から 1 本目 / 2 本目 | 338 / 43 |
| ずれの型: **行 1 だけ違う** | **27208（96.9%）**|
| ずれの型: 深さが違う | 881 |
| 像が短くなる（`|f(M[:k])| > |f(M)|`）| 91 |

⟹ **接頭辞単調の破れは「`M[:k]` の最後の 1〜3 列の綴り」だけ**であり、
**影の柱でも縮約でもない**。分岐列 `(a,1,0)` の浅い／深いを決める
`closes_unit(nxt)` / `p0_shallow` が「次の列」を見るので、
**接頭辞を切ると最後の列の綴りが変わる**。

    最小の例
      M      = (0,0,0)(1,1,0)(1,0,0)(2,1,0)(2,0,0)
      M[:3]  = (0,0,0)(1,1,0)(1,0,0)
      f(M[:3]) = (0,0,0)(1,0,0)(2,1,0)**(1,0,0)**
      f(M)     = (0,0,0)(1,0,0)(2,1,0)**(2,0,0)**…

## R7-5. 判定

* **`SeqEmbT3`（＝ `OrderT3`）は今の `conv3` では偽。** Lean 側で証明しに行くと落ちる。
* 原因は**縮約ではなく、分岐列の綴りが次の列に依存すること**。
  接頭辞単調の破れ（`gen3<=7` で 28089 件）とまったく同じ機構である。
* **これまでの「順序保存 違反 0」は母数が狭かった**（`gen3 <=7` / `<=8`）。
  破れる対はどちらか一方が **9 列以上**なので、`<=8` では 1 件も見えない。
* 直すなら `conv3` 側（課題 H）で、**分岐列の綴りを「次の列を見ない読み」に
  置き換える**しかない。それは NOTES §5 の「残る非同変な読み」と同じ的である。

## R7-6. **犯人は 3 条項**（旗で切って特定した）

破れる柱の `why`（`g2/provc.py` の `PROV`。`provc` の像が `rows3` と一致することは
破れ 41 対 x 2 で確認、差 0）:

| ずれ始めの柱の `why` | 件数 |
|---|---:|
| **`tie`**（浅い／深いの選択肢が無い枝）| **34** |
| `after_w`（左 deep / 右 shallow）| 4 |
| `plain`（左 deep / 右 shallow）| 3 |

⟹ 旗で 1 本ずつ切って数え直した（母集団 `ST_TS v<=5 len<=10` の 416607 個）:

| 旗 | 破れ |
|---|---:|
| **既定（v18）** | **41** |
| `RS_NOSIBNB=1` | 41 |
| `RS_NOHIBLK=1` / `RS_NOWROOT=1`（`NOTIESD` と併用）| 7 |
| `RS_NOAWFLIP=1` | 37 |
| `RS_NOH1=1` | 38 |
| `RS_NOH1=1 RS_NOAWFLIP=1` | 34 |
| **`RS_NOTIESD=1`** | **7** |
| `RS_NOTIESD=1 RS_NOH1=1` | 4 |
| `RS_NOTIESD=1 RS_NOAWFLIP=1` | 3 |
| **`RS_NOTIESD=1 RS_NOAWFLIP=1 RS_NOH1=1`** | **0** |

**`SeqEmbT3` を壊しているのはちょうど 3 条項**:

    `tiesd`  （v18, 課題 H14）  34 件。`tie_sd(Mo, off)` が**行列全体**を見る
    `awflip` （v17）            3〜4 件
    `h1`     （v14, 課題 H1 の 5 条項）3〜4 件

`sibnb` / `wroot` / `hiblk` は**無関係**（切っても 41 のまま）。
3 本とも切ると `ST_TS v<=5 len<=10` の 416607 個で**破れ 0**
（`v<=4 len<=9` でも 0）。

## R7-7. 3 条項を切る代償（`ST_TS v<=5 len<=10`、416607 個）

| | 既定（v18）| **3 旗オフ** |
|---|---:|---:|
| `SeqEmbT3` の破れ | 41 | **0** |
| **非標準の像**（`isstd(·, 'DBMS')` が偽）| **177** | **105** |
| 像が変わる行列 | — | 2276（0.55%）|
| 像の長さが変わる行列 | — | **0** |

⟹ **`ST_TS` 閉包の上では 3 条項を切るほうが非標準も減る**（177 -> 105）。
3 条項はどれも `gen3` の母集団で測って入れたもの（シート点・`ImgClosedT`）なので、
**`ST_TS` 閉包で測り直す価値がある**。像が変わるのは 0.55% だけで長さは不変。

⚠ シート点・`ImgClosedT`・`C2` への影響は**測っていない**（課題 H の土俵）。
ここで言えるのは「順序保存と非標準性については 3 旗オフが良い」だけである。

---

# 課題 R8: **シートは順序を破っていない**。破っているのは `conv3` の 3 条項だけ

計測は `tools/dbms/r10.py`（シートの順序）と `r10s.py` / `sheet3.py`（シート点）。

## R8-1. シート自身は**完全に順序保存**

母数: `check_sheet.load()` の正しい行のうち **z<2 の 3 行断片 1589 行**
（既知の誤記 592 / 891 / 897 / 898 / 1532 を除く。除かないと 1618 行で結果は同じ）。

| 判定 | 増 | 等 | 減 | **破れ** |
|---|---:|---:|---:|---:|
| **A 列（BMS）が行番号（＝ OCF）順に昇順** | 1588 | 0 | **0** | **0** |
| **E 列（DBMS）が行番号順に昇順** | 1588 | 0 | **0** | **0** |
| **A 列の `seqlex` 昇順で E 列も昇順** | 1588 | 0 | **0** | **0** |

⟹ **シートの A↔E の対応は順序同型**である。`OrderT3` は**設計として達成可能**。

## R8-2. 反例 41 対は**シートに 1 件も載っていない**

    両方載っている       **0**
    片方だけ            **0**
    どちらも載っていない  **41**

⟹ 破れは `conv3` が**シートの外で自由に決めている綴り**の中でだけ起きている。

## R8-3. 3 条項を切ってもシート点は**変わらない**（`sheet3.py`）

| 旗 | シート点 |
|---|---|
| 既定（v18）| **1354 / 1358**（不一致 4 はシート側の誤り）|
| `RS_NOTIESD=1` | **1354 / 1358** |
| `RS_NOH1=1` | **1354 / 1358** |
| **`RS_NOTIESD=1 RS_NOAWFLIP=1 RS_NOH1=1`** | **1354 / 1358** |

**`SeqEmbT3` を壊す 3 条項は、シートには 1 行も寄与していない。**

## R8-4. 代償の全体像（正直に）

| | 既定（v18）| **3 旗オフ** |
|---|---|---|
| シート点 | 1354/1358 | **1354/1358**（不変）|
| `SeqEmbT3` の破れ（`ST_TS v<=5 len<=10`, 416607）| **41** | **0** |
| 非標準の像（同上）| **177** | **105**（減る）|
| 像が変わる行列（同上）| — | 2276（0.55%）。**長さは不変** |
| lim=5 の 7 土俵: 非標準 / 単射 / 順序 / z=0 / d2b3 / C2 | 0 | 0（すべて不変）|
| lim=5 の **C1 の破れ** | 2 | **4**（悪化）|
| lim=5 の **ImgClosedT の破れ** | 2 | **4**（悪化）|

⟹ **トレードオフは `ImgClosedT` / `C1` ただ 1 つ**（lim=5 で 2 -> 4）。
シート点・非標準・単射・`C2` はすべて不変か改善する。

## R8-5. 判定

> **順序保存はシートと両立する。** シートは 1589 行で違反 0、反例 41 対は
> シートに 1 件も載っていない、3 条項を切ってもシート点は 1354/1358 のまま。
> ⟹ **`OrderT3` は設計の限界ではなく、`conv3` の実装の問題**である。

課題 H が直せる見込みがある。ただし **`ImgClosedT` と `OrderT3` が同じ 3 条項を
奪い合っている**（`tiesd` / `awflip` / `h1` はどれも `ImgClosedT` のために入った）ので、
**両立させるには「行列から読める・写しに同変な」新しい綴り**が要る。
`tie_sd(Mo, off)` が**行列全体を見る**のが順序を壊す直接の原因なので、そこが的である。

## 7. 再現の仕方

    python3 tools/dbms/r1.py 6        # gen3 <=6 全数
    python3 tools/dbms/r1.py 7        # gen3 <=7 全数（5 秒）
    python3 tools/dbms/r1.py 8        # gen3 <=8 全数（生成に 500 秒）
    python3 tools/dbms/r1.py exp 6    # <=6 の展開閉包（最長 24 列）
    python3 tools/dbms/r1ext.py "((0,0,0),(1,1,1),(2,1,0),(1,1,0))" 10   # 森が 41%

課題 R2（木ごとの `rd` と最小深さ、森の表）:

    python3 tools/dbms/r2.py 8       # gen3 <=8 全数 -> 森の表
    python3 tools/dbms/r2gen8.py     # 8 列全数を r2_e8.pkl に
    python3 tools/dbms/r2gen8.py     # 8 列全数を r2_e8.pkl に（578 秒）
    python3 -c "…"                   # r2_e8.pkl -> r2_e8.bin（24 バイト/行列）
    for i in 0 1 2 3; do python3 tools/dbms/r2col9.py $i 4 & done   # 9 列全数
    python3 tools/dbms/r2agg9.py     # 4 分割をまとめる

課題 R3（L16 の 3 件 ＋ (S2-b)）:

    python3 tools/dbms/r3.py 7 / 8 / exp        # 節 12' / Dm12 / 側条件 6 本
    python3 tools/dbms/r3s2.py 6 explore        # r' が何者か（分布）
    python3 tools/dbms/r3s2.py 7 steps          # d0' - d0 の分布
    python3 tools/dbms/r3s2.py 0 steps          # 展開閉包で同じもの
    python3 tools/dbms/r3s2.py 6 anc            # 仮説「行 0 の親を d0 段」

課題 L18 の 2 件:

    python3 tools/dbms/r4.py 7 / 8 / exp        # 節 9 のタイトさ / dd2 の下界
    python3 tools/dbms/r5.py 7 / 8 / exp        # 4 つの再帰の Dm10 d p.1
    python3 tools/dbms/r6.py 7 / 8 / exp        # a4 のたるみ / ブロックの先頭の深さ

課題 R7（`SeqEmbT3` と接頭辞単調）:

    python3 tools/dbms/r7.py 4 9      # ST_TS 展開閉包 44063 個（破れ 0）
    python3 tools/dbms/r7.py 5 10     # ST_TS 展開閉包 416607 個（**破れ 41**）
    python3 tools/dbms/r8.py 6 / 7    # 接頭辞単調の破れの構造
    python3 tools/dbms/r9.py 5 10     # 破れる柱の why（条項）を読む
    RS_NOTIESD=1 RS_NOAWFLIP=1 RS_NOH1=1 python3 tools/dbms/r7c.py 5 10   # 破れ 0
    python3 tools/dbms/r7cost.py 5 10 <out.pkl>   # 非標準の像の数と像の差

課題 R8（シート自身の順序）:

    python3 tools/dbms/r10.py drop 3     # シートの A 列 / E 列 / 行番号の順序
    python3 tools/dbms/sheet3.py 0       # シート点（旗を変えて比べる）
    RS_NOTIESD=1 RS_NOAWFLIP=1 RS_NOH1=1 python3 tools/dbms/sheet3.py 0
