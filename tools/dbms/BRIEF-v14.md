# v14 課題ブリーフ（2026-08-28）

読む順: `NOTES.md` の末尾 4 節（`## conv3 v12` 以降）→ この file。
コードは `tools/dbms/rows3.py`（変換器 conv3, v13）。**すべて絶対パスで走らせる**
（`cd tools/dbms` は失敗すると `&&` の鎖が切れる）。

## いまの成績（v13, commit 027d2cd）

| 土俵 | 値 |
|---|---|
| シート `python3 sheet3.py`（満点 1354） | 1354 |
| z=0 断片 <=9 列 295014 個 vs 証明済み `convC` | 一致 0 差 |
| 生成 <=7 列 77282 個: 非標準 / 順序違反 / 交差衝突 | 0 / 0 / 0 |
| 生成 <=8 列 781605 個: 非標準 | 3 |
| **ImgClosedT 破れ A**（<=5 / <=6 / <=7 列） | **4 / 88 / 1468** |
| SandwichU 違反 A（<=7 列） | 2 |
| 単射の破れ（d2b3 往復, <=7 列） | 2 組 |

**ImgClosedT が唯一の未証明要件**（Lean `lean/Dbms3.lean` の
`ST_D3_conv3_of_parts` が要求する 6 つのうち、他は道具が揃っている）。

    ImgClosedT: 任意の BMS 3 行 z<2 標準形 A (|A|>1) と m>=1 に対し
                ある BMS 3 行 z<2 標準形 B があって (conv3 A)<m> = conv3 B

## 残る 4 個（<=5 列, `python3 imgfast.py` で再現）

    A1 = (0,0,0)(1,1,1)(2,0,0)(3,1,1)(1,1,1)      m=2,3 で破れ
    A2 = (0,0,0)(1,1,1)(1,1,0)(2,2,1)(2,1,0)      m=3 のみ
    A3 = (0,0,0)(1,1,1)(2,1,0)(2,1,0)(1,1,0)      m=2,3
    A4 = (0,0,0)(1,1,1)(2,1,0)(3,1,0)(1,1,0)      m=2,3

再現:

    python3 -c "
    import imgfast, rows3
    r = imgfast.score(lim=5, mmax=3, verbose=0)
    print(r[0], r[1], len(r[2]))
    for M,m,T in r.badpairs: print(rows3.key(M), m, rows3.key(T))
    "

所要 ~1 分（<=5）。<=6 は ~15 分、<=7 は 1 時間超なので**先に `imgfast.py est` で
見積もる**こと。

## v13 で効いた 2 条項（同じ手が次も効く見込み）

* `wchain`（`rows3.wchain_head`）— `after_w` の窓が**直前 1 本**しか見ておらず、
  写しの中に入ると見失っていた。窓を「この写しの頭まで」広げた。
  後ろ向き走査 1 本。`st['Mo']` と `off` だけで書ける。
* `sibL` + `sib_anchbefore`（`rows3.sib_ok`）— 行 1 の影を立てた柱の
  「深い側」を兄弟にも渡す。ただしアンカー `(1,1,0)` より前でだけ。

## 直すのは `A<m>` の側であって `A` の側ではない（v13 で判明した肝）

    A       = (0,0,0)(1,1,1)(2,1,0)(2,1,0)
    conv3 A = ...(4,2,0)(4,1,0)      シート行 525 と一致。これは正しい
    A<2>    = ...(3,1,1)(4,1,0)
    v12     = ...(6,2,1)(7,1,0)      こちらが 1 段浅かった

`A<m>`（長い展開）は**シートに 1 行も載らない**。だから
「シート満点のまま ImgClosedT が破れる」。**シートを落とさない修正だけが正解**。

## 逆算のやり方（F2 の `w2.py` step1..step5 と同じ）

1. 破れた対 `(A, m, T)` について `T = conv3(A)<m>` を出す。
2. `T` の**接頭辞**が `conv3` の像になるような標準形 `B` の候補を
   `inv3.d2b3(T)` と誘導つき DFS（`imgfast.search2`）で探す。
   見つからないときは「`T` のどの柱が像として有り得ないか」を特定する。
3. その柱を出したのは `conv3` のどの分岐か（`rows3.conv3` に print を刺す）。
4. その分岐が見ている窓を広げる／条件を足すと直るか、**最小の条項**を書く。
5. 旗 `V14 = {...}` に入れて 7 つの土俵を全部測る。**1 つでも悪化したら捨てる**。

## 土俵の測り方

    python3 sheet3.py                       # シート（1354/1358 が満点）
    python3 rows3.py 6                      # 生成 <=6: 非標準/順序/衝突/z=0
    python3 imgfast.py scan 5               # ImgClosedT <=5
    python3 imgfast.py scan 6 3 1 28        # ImgClosedT <=6（~15 分, 28 並列）
    python3 imgfast.py scan 6 3 0           # z=0 の対照（破れ 0 になるはず）
    python3 inv3.py 7                       # 単射（往復）

## 規約

* `git add -A` / `git add .` は**禁止**。file を名指しで add する。
* 30〜60 分ごとに commit する（セッションが落ちても作業が残るように）。
* Lean は `leanman check -C /home/koteitan/proofs/dbms/lean <file>.lean` だけ。
  `lake build` は**絶対に走らせない**。終了コードだけで判定（0=緑, 1=sorry,
  2=error, 124=timeout）。出力が空でも緑とは限らない。
* 長い shell は**先に見積もってから**走らせる。

---

# 追記（2026-08-28, セッション側の実測）

## 1. どちら側が正しいかはシートで確定した

| | シート行 | conv3(A) がシートと一致 |
|---|---|---|
| A2 = (0,0,0)(1,1,1)(1,1,0)(2,2,1)(2,1,0) | **300** | 一致 |
| A3 = (0,0,0)(1,1,1)(2,1,0)(2,1,0)(1,1,0) | **526** | 一致 |
| A4 = (0,0,0)(1,1,1)(2,1,0)(3,1,0)(1,1,0) | **627** | 一致 |
| A1 = (0,0,0)(1,1,1)(2,0,0)(3,1,1)(1,1,1) | 載っていない | — |

A<n>（展開したもの）は 4 つとも**シートに 1 行も載っていない**。
つまり **`conv3(A)` は正しく、直すのは `conv3(A<n>)` の側**。確定。

## 2. 族 α（A3, A4）— 柱 1 本の行 1 が 1 だけ浅い

長さは**ぴったり一致**する。ちがうのは写しごとに 1 本だけ:

    A3, m=2: |f(A)<m>| = |f(A<m+1>)| = 11、ちがう柱 1 本
             [9]  f(A)<2>   = (5,2,0)      <- 正解
                  f(A<3>)   = (5,1,0)      <- conv3 がこう出している
    A3, m=3: ちがう柱 2 本  [9] (5,2,0)/(5,1,0)  [14] (6,2,0)/(6,1,0)
    A4 もまったく同じ位置・同じ値。

**正解は深い側 `(k,2,0)`。** 位置は「写しの 4 本目」。
これは分岐列 `(a,1,0)` の `base_s`（浅い）／`base_d`（深い）の選択そのもの
（`rows3.conv3` の `if is_branch(p) and base_s != base_d:` の枝）。
**写しの中に入ったとき、深い側を取り損ねている。**

`SandwichU` の 2 個（<=7 列）も v13 では**同じ病**である:

    (0,0,0)(1,1,1)(2,1,0)(3,2,1)(3,2,0)(3,1,0)(1,1,1)  n=1 の柱 7
       f(A<1>)  = (5,1,0)      (f A)<2> = (5,2,0)
    (0,0,0)(1,1,1)(2,2,1)(3,2,1)(3,2,0)(3,1,0)(1,1,1)  同上

**1 つの条項で族 α と SandwichU の両方が落ちる見込みがある。**

## 3. 族 β（A1, A2）— 余分な影の写しが出る

長さが**合わない**。`conv3(A<n>)` が、像の展開には無い写しを余分に出す。

    A1, m=2: |f(A)<2>| = 12、|f(A<3>)| = 17
             f(A)<2> = 接頭 7 本 + (3,2,0)(4,3,1)(5,0,0)(6,1,0)(7,2,1)
             f(A<3>) = 接頭 7 本 + (2,1,0)(3,2,1)(4,0,0)(5,1,0)(6,2,1)
                                 + (3,2,0)(4,3,1)(5,0,0)(6,1,0)(7,2,1)
             -> f(A)<2> は f(A<3>) から**最初の写しを 1 本抜いたもの**。
    A2, m=3: f(A<4>) には (4,1,0)(5,2,1) / (6,1,0)(7,2,1) という
             繰り返しの影の対が余分に入っている。

族 β は族 α とは**別の病**。逆像 B は A<n> ではなく別の標準形になる。
**先に族 α を落とすこと**（こちらは正解が完全にわかっている）。

## 4. 逆写像 d2b3 は m>=2 で全滅する

    A1  m=1 のみ当たる    A2  m=1,2 のみ    A3  m=1 のみ    A4  m=1 のみ
    （m=1..14 を全部試した）

つまり**散発的な取りこぼしではなく系統的な誤り**。だから
「大きい m なら当たる」ということは無い（次の項も参照）。

## 5. 要件は `ImgClosedT`（すべての m）ではなく `ImgCofinalT`（いくらでも大きい m）でよい

`ReindexT1_of_block` が `ImgClosedT3` から実際に使うのは **`m := n+1` ただ 1 つ**。
だから

    ImgCofinalT3: 任意の m0 に対し、ある m >= m0 と標準形 B があって
                  (conv3 A)<m> = conv3 B

で足りる。差を埋める補題（展開指数についての単調性）は
**`lean/Cofidx.lean` に証明済み（sorry 0, 終了コード 0）**、
`ReindexT1_of_cofinal` の証明本体は `lean/PATCH-imgcofinal.md` にある。

採点するときは「破れた／破れない」だけでなく
**A ごとに逆像を持つ m の集合**を出すこと。上の項 4 のとおり、
いまの 4 個は m>=2 が全滅なのでこの弱めでは救えない（＝ conv3 を直すしかない）。

---

# 追記 2（2026-08-28, 方針）

## 6. 根本は「写しをまたぐと状態が漏れる」こと

G2 の測定でいちばん効く数字はこれ:

    悪い部分の根 N[r] の出どころ:  sh0 42 / sh1 36 / body 10
    -> **78/88 で「conv3(A) の悪い部分は影の柱から始まる」**
    直す向き:  浅すぎ 71 / 深すぎ 9 / 行 0 がずれ 6 / 候補が非標準 2
    -> **一律に深くするのは誤り**（F4 の 7 個・F8 の 2 個は深すぎる）

`A<n>` は `A` の末尾を n 回写したものである。もし `conv3` が

    **写し同変**: conv3(G + C + C' + ...) の写し k 番目 =
                  写し k-1 番目を上昇分だけずらしたもの

を満たすなら、`(conv3 A)<m>` と `conv3(A<n>)` は自動的に一致する
（DBMS の展開がやっているのはまさに「ずらして写す」だけだから）。
**破れはすべて「写しの境目で状態が漏れている」ことの現れ**である。

漏れる状態は `conv3` が持ち回っている 5 つ:

    st['prev']  1 ビットの分岐状態     <- F1(31) F11(2) F13(1) がここ
    st['dmap']  もとの段 -> 像の段      <- F2(21) の (-1,-1,0) はここの疑い
    st['ST']    祖先スタック
    L / F       行 1 のユニットの表
    st['Mo'], off  行列ぜんぶ＋位置（後ろ向きの窓）

## 7. だから、枝ごとの逆算より先に「同変性を直に測れ」

条項を 1 本ずつ当てる前に、**どこで漏れているか**を測るほうが速い:

1. `A` と `n` を取り、`A<n>` を作る。`A<n>` の写しの境目の位置は既知
   （悪い部分の根 r と写しの長さ `len(A)-r` から出る）。
2. `conv3(A<n>)` を走らせながら、**各写しの頭で `st` を丸ごと記録**する。
3. 写し k の頭の `st` と、写し k-1 の頭の `st` を「上昇分だけずらしたもの」を
   比べる。**最初にずれた成分が犯人**である。
4. その成分がなぜずれたか（どの分岐が書き換えたか）を見て条項にする。

G2 が作った `tools/dbms/g2/provc.py`（柱ごとの出どころを記録する `conv3` の写し、
<=6 列 8387 個で `b2d3` と食い違い 0）がそのまま土台に使える。

## 8. `ImgCofinal` への弱めは、いまの 4 個は救わない（測った）

    逆写像 d2b3 が当たる m:  A1 {1} / A2 {1,2} / A3 {1} / A4 {1}   （m=1..14 を全部）
    探索（段 2）の救出:      <=6 列 173 対で **0 件**

系統的な誤りなので「大きい m なら当たる」ことは無い。
弱め自体は Lean に入れた（commit 7faa432、`ST_D3_conv3_of_parts` の仮定が
`ImgCofinalT3` になった）が、**conv3 を直す仕事は減らない**。

---

# 追記 3（2026-08-28）— 条項を 1 本ずつ測った表（**ほぼ全部が改悪**）

`mkt.py` が作る旗つき写し `rows3t.py` で、lim=5 imgc=3 の 7 土俵を測った
（旗ぜんぶ off で gen<=7 の 77282 個と像の差 0 を毎回確認）。

| 条項 | シート | 非標準 | 順序 | 衝突 | z0 | d2b3 | C1 | **C2** | **Img 破れ** |
|---|---|---|---|---|---|---|---|---|---|
| **なし（v14）** | 1354 | 0 | 0 | 0 | 0 | 0 | 4 | **0** | **4** |
| `closes_w` | 1354 | 0 | 0 | 0 | 0 | 0 | 4 | 0 | **4** |
| `wch_repeat` | 1354 | 0 | 0 | 0 | 0 | 0 | 4 | 0 | 5 |
| `wch_first` | 1354 | 0 | 0 | 0 | 0 | 0 | 4 | 0 | 8 |
| `residprev` | 1354 | 0 | 0 | 0 | 0 | 0 | 2 | 10 | 12 |
| `wch_anyprev` | 1354 | 0 | 0 | 0 | 0 | 0 | 5 | 7 | 12 |
| `wch_free` | 1354 | 0 | 0 | 0 | 0 | 0 | 3 | 11 | 14 |
| `anchbr_sib` | **1282** | 0 | 1 | 91 | 0 | 51 | 37 | 3 | 18 |
| `newterm_w` | 1354 | 0 | 0 | 0 | 0 | 0 | 2 | 11 | 30 |
| `sibbody_nolad0` | 1354 | 0 | 1 | 0 | 0 | 0 | 5 | 43 | 47 |
| `sibbody_d2` | 1354 | 0 | 1 | 0 | 0 | 0 | 5 | 43 | 47 |
| `sibbody_v0` | 1344 | 0 | 45 | 92 | 0 | 92 | 57 | 46 | 48 |
| `sibbody` | **1219** | 0 | 2 | 242 | 0 | 242 | 140 | 3 | 58 |

**どれ 1 つとして主指標を下げない。** `closes_w` だけが中立。
（`C1` = SandwichL は Lean の分解が**使わない**ので要件ではない。**C2 = SandwichU が要件**。）

## `sibbody` を絞ると A2 は直る（が他が壊れる）

`sibbody` がシートで壊す 10 行はぜんぶ兄弟が `(1,0,0)`、つまり
**行 0 の影も立てた**柱の兄弟だった。行 1 の影だけの柱に絞ると:

    sibbody_nolad0:  lad1 かつ not lad0 かつ 兄弟の行 1 = 0 のときだけ本体の横に

シートは満点のまま、A2 の証人 `(0,0,0)(1,1,1)(1,1,0)(2,2,1)(2,0,0)(3,1,1)(3,0,0)(4,1,1)`
が目標 `f(A2)<3>` に**ぴったり一致**する。しかし ImgClosedT は 4 -> 47、C2 は 0 -> 43。

## 読み取り: **条項を足す道はもう伸びない**

v13 は 26 -> 4 に効いたが、v14 以降は**足すほど悪くなる**。
1 ビットの状態 `st['prev']` ＋ 6 本の特例（`closes_unit` / `after_w` / `wchain` /
`closes_hi_unit` / `sibL` / `tie`）という設計が限界に来ている。

## 次にやるべきこと: **正しい綴りを教師データにして規則を学習し直す**

シートは長い展開を 1 行も含まないので、「写しの中での綴り」を**シートからは決められない**。
しかし **ImgClosedT の目標 `(conv3 A)<m>` そのものが教師**になる:

* `|f(A)<m>| == |f(A<m+1>)|` の対では柱ごとに整列できる（族 α）。
  ずれた柱の**正しい値がそのまま読める**。
* 整列できる対を全部集めれば「この文脈では深い／浅い」の**教師つきデータ**になる。
* そのうえで、`st['prev']` を捨てて**行列から直に読む述語**を厳密に当てはめる
  （2 行のときの `depth_learn.py` / `flip_learn.py` と同じ道具立て）。

学習した規則は**写しに対して同変**でなければならない（追記 2）。
`st` を持ち回らない述語なら同変性は自動的に成り立つ。

---

# 追記 4（2026-08-28）— **写しの中ではアンカーが `(k,0,0)` に化ける**

## 見つけた仕組み

`closes_unit(nxt)` は「行 1 のユニットが閉じる」を

    nxt is None  or  (nxt[0] <= 1 and nxt[2] == 0)

で判定している。つまりアンカー `(1,1,0)` を認める。ところが**写しの中では
アンカーが `(k,0,0)` に化ける**（悪い部分の上昇で行 1 が 0 に落ちる）:

    A3    = (0,0,0)(1,1,1)(2,1,0)(2,1,0)(1,1,0)
                                          ~~~~~~~ アンカー
    A3<4> = ... (2,0,0)(3,1,1)(4,1,0)(4,1,0)(3,0,0)(4,1,1)(5,1,0)(5,1,0)
                                             ~~~~~~~ 同じ役をする柱

`closes_unit` はこれを認めないので、**写しの中でユニットの終わりを見失う**。
これが「写しをまたぐと状態が漏れる」（追記 2）の具体的な中身である。

## 試した 2 条項（`rows3u.py` の旗）

    closes_w2   : nxt が (k,0,0) で nxt[0] < p[0] なら _cw = True
                  （アンカーと同じにユニットを閉じる）
    nxtge_last  : prev==0 の枝を「nxt[0] >= p[0] なら深い」に置き換え、
                  after_w / wchain より**後**に当てる

| 旗 | シート | 非標準 | 順序 | 衝突 | C2 | **Img 破れ** | A3/A4 の目標 |
|---|---|---|---|---|---|---|---|
| なし | 1354 | 0 | 0 | 0 | 0 | **4** | 当たらない |
| nxtge_last | 1353 | 0 | 0 | 0 | 2 | 25 | m=2 だけ |
| closes_w2 | 1349 | 0 | 4 | 4 | 4 | 8 | 当たらない |
| **両方** | 1348 | 0 | 4 | 4 | 4 | 23 | **m=2,3,4 すべて** |

**A3 と A4 は完全に直る。** しかし他が 21 個壊れる。要るのは**絞り込み**である。

## 絞り込みの的（未解決、ここが次の仕事）

closes_w2 がシートで壊す 5 行は、(k,0,0) が次に来ても**深いまま**の例:

    行691   (0,0,0)(1,1,1)(2,1,0)(3,1,0)(2,0,0)
       正 (0,0,0)(1,0,0)(2,1,0)(3,2,1)(4,2,0)(5,2,0)(4,0,0)   深い
       誤 (0,0,0)(1,0,0)(2,1,0)(3,2,1)(4,2,0)(5,1,0)(4,0,0)
    行692   (0,0,0)(1,1,1)(2,1,0)(3,1,0)(2,0,0)(3,1,1)          同上
    行744   (0,0,0)(1,1,1)(2,1,0)(3,1,0)(3,1,0)(2,0,0)          2 本とも深い
    行1091  (0,0,0)(1,1,1)(2,1,0)(3,2,1)(4,1,0)(2,0,0)
    行722   (0,0,0)(1,1,1)(2,1,0)(3,1,0)(2,1,0)(3,1,0)(2,0,0)

直る側（写しの中）と見比べると、直前の柱が違うように見える:

    壊れる  ...(2,1,0)(3,1,0)(2,0,0)         直前は 1 段浅い分岐列（親）
    直る    ...(3,1,1)(4,1,0)(4,1,0)(3,0,0)  直前は同じ深さの兄弟

しかし行744 は直前が同じ深さの兄弟なのに深いので、これだけでは分かれない。
**教師データ（teach.py）で述語を総当たりして分けること。**

## 教師データの使い方（tools/dbms/teach.py）

    python3 teach.py 6          <=6 列 x m<=3
    整列できた対 6356 / 長さが違う対 18802
    分岐列の柱 5298 本、綴りが読める（tie でない）もの 2650 本
      plain/deep      1583 本  誤り 0
      closes/shallow   667 本  誤り 0
      prev0/shallow    393 本  誤り 105（closes=False の 229 本のうち 105 本）
      after_w/shallow    7 本  誤り 2
    誤りは 107 本すべて向きが (0,+1,0)（行 1 が 1 だけ浅い）

prev0/shallow の closes=False 229 本（深い 105 / 浅い 124）に対する
単一述語の総当たり:

    nxt[0] >= p[0]            214/229   <- 最良
    nxt が分岐列 かつ >= p[0]   193/229
    nxt[1] != 0               184/229

**外す 15 本**は全部「深くすべきなのに浅いと言う」もので、
nxt = (p0-1, 1, 0) か (p0-1, 1, 1)。ここを分ける述語が要る。

## 潰した仮説（測って捨てた）

* brnext（状態を全部捨てて「次が自分以上の分岐列なら深い」だけ）
  → **シート 260/1358**。教師データでの一致率 49%（いまの conv3 は 96%）。
* nxtge_shal（「次が自分より浅いなら浅い」を最後に上書き）→ シート誤り 499。
* nxtge_pure（状態なし、最後に当てる）→ シート誤り 500。
* sibbody 系、residprev、newterm_w、anchbr_sib → 追記 3 の表。
