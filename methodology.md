# methodology.md — 小さい行列から少しずつ証明を増やす方法論

`(0,0,0)(1,1,1)(1,0,0)` から始めて、シート行 336 `psi(W_w * psi(W))` まで来た
やり方を、考え方とメタ的な技法として書き残す。（行 374 まで到達）Lean の名前は最小限にし、
数式は ASCII で書く。対象は 3 行バシク行列（BM4, z < 2 の断片）、目標は
「行列 M が W 0 に入る」= M の全ての展開 M[n] が W 0 に入ること。

## 0. 基本の姿勢

- **証明の単位は 1 列。** 証明できている行列 A の右に列 c を 1 本足した A ++ c を
  次の目標にする。標準形の順に、飛ばさずに埋める（シートは飛んでいる）。
- **標準形かどうかは形で判断しない。** `~/code/yaBMS/c/bms -s "<matrix>"`（1 なら標準形）。
  展開も手で追わず `bms -d "<matrix>[n]"` で確かめる（bad root, delta, 複製の形）。
- **こまめに書く。** 1 回の返答で出せる量に上限があり、超えると全部消える。
  検討→ notes.md に書く→検討、の短い繰り返し。書く内容は「今の方針、分かったこと、
  失敗した手とその理由、次にやること」。補題の候補は 1 個ずつ Lean で確かめる。

## 1. 行き詰まったときの手順

A ++ c が証明できないとき、次の順に試す。

1. **c を 1 つ落とす。** c = (4,2,0) が駄目なら (4,1,0)、それも駄目なら (4,0,0)、
   高さを 1 つ下げて (3,x,0)。通るものと通らないものの境界に、壁の正体が出る。
2. **A ++ c を展開する。** `bms -d` で A ++ c[1] を見る。A ++ c[1] は A ++ c より
   「大きい」が構造が見えている行列で、A ++ c の証明にはこれが全部要る。
   まず A ++ c[1] を証明してみる。
3. **A ++ c[1], A ++ c[2], A ++ c[3] をいくつか証明する。** 個別の証明が通れば、
   その証明の形（どの集合の元として読んだか、どの補題で継いだか）にパターンが
   見えてくる。
4. **パターンが見えたら A ++ c[k] を k の帰納法で証明する一般化を考える。**
   一般化できれば「∀ n, A ++ c[n] ∈ W 0」なので、それが A ++ c の証明そのもの
   （`A1_intro` で全展開の所属に帰着、実際には `snocY_mem` / `snocYd_mem` /
   `oper_flat` が展開の形ごとに用意してある）。
5. **一般化が閉じないときは、その理由を 1 文で notes.md に書く。** 「junk の条件が
   複製先の文脈で足りない」など。次に同じ壁に当たったとき、同じ理由かどうかを
   すぐ照合できる。

実例: (0,0,0)(1,1,1)(1,1,0)(2,2,0)(3,3,0)(4,3,0)(4,1,0) は壁だった。
(4,1,0)[1] = 行列全体を高さ 4 に吊るしたもの、を証明しようとして
「吊るしの不変量を、どの台座でもよい形に取る」着想が出て、壁が消えた（§4）。

## 2. 行列を「集合の元」として読む

1 本ずつ証明していると同じ証明を何度も書くことになる。同じ作り方で作れる行列を
集合にまとめ、集合について 1 回証明する。

- 集合の元は「土台 + 階段の列」。階段とは、高さ 1, 2, ..., h の列が 1 本ずつ
  この順に並んでいること。集合の**レベル**は階段の最後の列の高さ h。
- 階段の列の右に付く、その列より高い列を **junk** と呼ぶ（造語）。junk はレベルに
  数えない。junk には条件「この集合の別の元の同じ場所に付けても W 0 に入る」
  （再継ぎ条件）を付ける。
- 集合に要る性質（`BaseOk`）: 元が W 0 に入る / 頂上（レベル + 1）にブロックを
  吊るせる（hang）/ 再継ぎできる junk を頂上の列に吸収できる（close）。
  これがあれば、頂上に (h+1, 1, 0) を置く塔（行列全体の複製を上に積む）が回る。
- **同じ行列でも読み方は複数ある。** (0,0,0)(1,1,1)(1,1,0)(2,2,0)(3,3,0)(4,3,0) は
  「レベル 2、(3,3,0)(4,3,0) は 2 の junk」とも「レベル 3、(4,3,0) は 3 の junk」とも
  読める。上に置きたい列の高さに合わせて読み方を選ぶ。
- 列 c を頂上に置くとき、c の bad root は階段の列の中にある（junk は祖先にならない）。
  bad root が階段のどの列かで塔の形が決まる。
  - bad root が (1,1,0)（2 を置くとき）: 複製は 1 から始まる。前の段を台座
    （`LwA`）に飲み込んで rank 0 からやり直せる。
  - bad root が 2 のブロック（3 を真横に置くとき）: 複製は 2 のブロックの積み上げ。
    同じ層の中で `RunG` の段が 1 つ増えるだけ。
  - bad root が 2 段以上下: 複製は層を登る。層をまたぐ条件が要る（§4）。

## 3. 定義のコツ（循環を避ける）

- **集合の定義は exact rank でよい。** `RunG E (j+1)` のブロックの条件は
  `RunG E j` の元の上だけ。∀ rank を定義に入れると非構造的になる。
- **普遍な条件は、集合を定義した後の述語として書く。** 例: junk J が
  「どの j でも `RunG E j` の元の上に継げる」は `RunG` の定義後に書ける
  （`JkG`）。手で置いた junk（(3,3,0)(4,3,0) など）はこの普遍条件を満たすと
  証明できる。
- **吊るしの不変量は自由に選べる。** `blkD_memS` の不変量 P は任意の述語。
  「∃ 台座 E, Iface E ∧ ...」のように ∃ で潰した集合を不変量に取ると、
  吊るしの途中で生まれる junk（断片）は不変量全体の上で再継ぎでき、
  普遍な junk になる。これが行 295 まで届いた鍵（`PkGA`, `Pk3U`, `Lk`）。
- **台座を抽象化する界面** `Iface E` = `BaseOk E` + 再台座性（E の元は
  `LwA` の頭 ++ 単位 M で、M はどの `LwA` の台座にも継げる）。この 2 つがあれば
  `RunG E`, `Pk3 E` を E の上に積めて、界面も継承される。層は
  `Lay (n+1) = Pk3 (Lay n)` のような再帰で作る。
- **循環の見分け方。** 「A の条件は B の元の上で要る」「B は A を含む集合を
  定義してから作る」なら循環。壊す方法は (a) rank や層で層別して構造的にする、
  (b) 条件を ∃ で潰した集合の上に取る、(c) 条件を「自分の接頭辞」に限る。
  どれも効かないときは、その形はこの手法の外（本体プロジェクトの残核）。

## 4. 壁の記録（何が壁で、どう消えたか）

- **「2 番目の 2 の真横の 3」**（`(2 3 2 3)`）: 壁だと思っていたが、複製が同じ層に
  積まれるだけで、junk 条件を「`RunG (Lay i)` の全 rank」で書けば通った。
- **「3 の上に 3」**: 単位の頂上が 2 から 2 段上（`(2 3 3)`）で、複製が層を登る。
  吊るしの不変量を「∃ 台座」に取り、2 の junk を普遍にした集合 `Pk3U` を作ると
  吊るしが回り、それを階層 `Lk k` にすると `(2 3^n)` 全部が出て行 295 になった。
- **「3 の上に 4」「4 の上に 4」**（行 295 の次、D_5、∀ v の D_v）: 新しい 4 の
  row1 親は頂上の 4 を飛ばして下の 3 になり、複製は (3 J3)(4 J4)^k の全部で、
  複製の 3 は「4 が頂上の段」の上に来る。3 の junk 条件を「Stk C 2 の上」で書く
  設計（層 G C）では、junk 付きの 3 の複製が次の段に入らず閉じない。
  解: junk の条件を **「任意の台座の上」** にする。`PU y` = { 任意の `IfcV (y+1)`
  台座の元 ++ (y+1 の記録) ++ 普遍 junk }。台座を ∃ で消すので、複製がどの段の
  上に来ても入る。これは `PkGA` の「∃E Iface E」と同じ手で、値 y で一様にすると
  `Dv (k+1) ∈ PU (k+2)` が k の帰納法で出て ∀ v の D_v が塔なしで出た。
  塔は「値 v の snoc」（`IfcV_snoc`）の中に閉じ込めてある。
- **行 296 の先（z=1 の列）**: 壁はなかった。§7 を見よ。

## 5. 観察（役に立った事実）

- **1 はリセット。** 行 1 が 1 の列はどの高さでも `RunA` の頭になれる。
  1 を含む語は既存の道具で通る。
- **junk 自体は最初からあった。** 壁は junk ではなく「junk の条件をどの集合の
  上で言うか」。生まれる場所と複製される場所が同じ集合なら通る。
- **平坦な複製（(x,0,0) の列）は登らない。** 各段は「junk 付きのブロック」で
  読めるので、`oper_flat` + 普遍 junk の帰納法で通る。
- **順序数は目安。** 行 294 → 295 は `psi_1(W_3)` → `psi_1(W_4)` で、
  「2 で折り返す複製」が初めて要る場所。行 293 が長く関門だったのと同じ種類の
  段差が 1 段上で起きていた。
- **junk の条件は「どの集合の上で言うか」が全て。** 生まれる場所と複製される
  場所が違うときは、条件を「任意の台座（∃ で消す）の上」にすると閉じる。
  exact な集合の上の条件は close（吸収）に、普遍な条件は複製に効く。両方
  要るときは、集合自体を「∃ 台座」で定義する（`PkGA`, `P3U`, `PU y`）。
- **値で一様な界面。** 2 の snoc は `Iface`、3 は `Ifc3`、4 は `Ifc4` と
  1 つずつ作ったあとで、`IfcV v`（`IfcV (v+1) F = IfcV v F ∧ 置き直し可能性`）に
  まとめると ∀ v が出た。個別に 2〜3 個作ってから一般化する（§1 の 3〜4 と同じ）。
- **結局は 2 行の整礎性 + 吊るし。** 持ち上げた列 (x+1, y+1, 0) のうち y ≥ 1 は
  2 行と同じ展開、y = 0（持ち上げると 1 の列）だけが行列全体を吊るす。
  この手法の全部は後者の整備である。

## 7. 行 296 の先: z=1 の列と、台座の使い回し（行 297〜316）

行 296 `R296 = (0,0,0)(1,1,1)(1,1,0)(2,2,1)` の先は、R296 の上に同じ構造が
繰り返す。移送定理（e: (a,b,c) ↦ (a+1,b+1,c) で W 0 を運ぶ）を作ろうとすると、
最後の列の row1 が 0 のとき（(k,0,0) が (k+1,1,0) になり bad root が根に変わる）
に本体プロジェクトの残核と同じ形になる。作らずに済んだ理由:

- **底の集合は「Aok な行列はレベル 0 の台座」**（`BaseOk_zero`）。Q に特別な
  資格はなく、R296 も Aok なので同じ資格の台座。単位 (h+1,1,0)(h+2,2,1) は
  任意の台座の上で使えるセグメント（`SegA_unit11`）なので R296 ∈ RunA 0 1 で、
  既存の階層が丸ごと乗る。行 306 `R296(2,2,0)` は `RunG_snoc2` の 1 行。
- **z=1 の列 (a+1,b+1,1) が (a,b,0) の直上にあると、展開は対角**
  (a,b,0)(a+1,b+1,0)(a+2,b+2,0)… になる（bad root = (a,b,0)、d0 = d1 = 1、
  junk がなければ上昇のマスクは自明）。対角は任意の台座の上で W 0
  （`DiaV`, `PU_DiaV`: 記録の値が 1 ずつ上がる列は `PU y` の連鎖）。
  だから z=1 の列は **記録の普遍 junk** として扱える（`JkGU_z1`, `JkU_z1`）。
  行 312〜315 は「記録 + z=1 の junk + 次の記録」を積むだけ。
- **行 316 `(0,0,0)(1,1,1)(1,1,1)`** は Q の複製が (1,1) ずつ上がる塔で、
  各段は上の 2 つの繰り返し。
- **梯子の段としての単位**: 梯子の元 A（段 r、レベル h）の上に
  (h+1,1,0)(h+2,2,1) を置くと段 r+1、レベル h+1 の梯子の元（`LvB_unit11`）。
  条件は「どの梯子の元の上でも対角」で済む。Y_n = R296 (2,1,0)(3,2,1)…(n,1,0)(n+1,2,1)
  が段 n の梯子の元で、行 300〜305 の台座。
- **1 の列 (d,1,0) は「レベル d-1 の吊るし」があれば継げる**（`snocd_gen`）。
  塔は行列全体の複製で、段 n+1 = Y ++ (段 n を高さ d に吊るしたもの)。

教訓: 移送定理（大きい一般化）を作る前に、**既存の集合が新しい台座をそのまま
受け入れないか**を見る。台座の資格が「Aok」だけなら、繰り返し構造は新しい
定理なしで乗る。

## 8. 行 317〜336: junk の語と、影の W 帰納法

- 記録の直上の junk は「語」になる: z = (c+2,y+2,1) と、任意の高さ e ≥ 2 の平坦な列 (c+e,0,0)。
  最後が z なら展開は記録からの複製（z は (1,1)、平坦な列は (1,0) 上がる。le1 のマスクは
  「row1 = 0 の列は row1 の親を持たない」で決まる）。最後が平坦なら、最も近い低い列からの
  区間の平坦な複製。
- **影の W 帰納法**: 語の z を (1,0,0) に置き換えた平坦行列（影）は 2 行の定理で W 0。
  「影がこれなら語は普遍」を A2'（W の最小性）で示す。最後が z の場合の帰納法の仮定は
  影⟦1⟧ = 影.dropLast、最後が平坦の場合は影⟦n⟧、n = 0 の段は n = 1 の段の接頭辞（W_take）。
  順序数を陽に書かずに済むのが利点。先に作った K と個数の二重帰納法（Good_blocks）は不要になった。
- **平坦な列の壁**（行 337 以降、§9 で解消）: 平坦な列を頂上に持つ台座（Q(2,0,0)）の上に階層を
  建てようとすると、展開が項を複製したりブロックを小さくしたりして、影ともブロックの多重集合とも
  辞書式に噛み合わない（notes 続き49）。読み替え（荷つきの字の語）で台座を建てずに済んだ。

## 9. 行 337〜345: 荷（payload）つきの字と、壁の言い換え

- **壁だと思った形**: 行 337 の塔 `Q(2,0,0)(3,1,0)(4,2,0)...` を「平坦な列 (2,0,0) を頂上に
  持つ台座の上の対角」と読むと、台座の階層（`Iface` の rebase）が必要になり、
  「平坦な森に W ブロックを吊るした項の列」の帰納法が要る（順序が噛み合わない、notes 続き49）。
- **言い換え**: 同じ行列を `Q ++ Q↑2 = (0,0,0) :: ((1,1,1) :: Q↑2)` と読む。すなわち
  **字 zY := (a+1,b+1,1) :: Y↑(a+2)（z の列に荷 Y を載せたもの）** の語。平坦な列 (2,0,0) は
  台座ではなく荷の根になる。台座はもとの `LwA` / `RunG` / `IfcV` のままで、階層を建てる必要がない。
- 語 `ws = [Y1,...,Yk]`（荷はすべて `Bok`）の普遍性 `GoodP` は §8 と同じ形（4 段: `PU` / `PkGA` /
  `SegA` / 根）で、影の W 帰納法で出る。影は `(0,0,0) :: Σ((1,0,0) :: Yi↑2)`。
  影が W 0 に入ることは、和 `A ++ B`（`B` の根が高さ 0）が W 0 に閉じること（`bump_mem3` の
  bump を外した写し）から出る。
- 影の展開が語の展開を正確に写す（3 つの場合）:
  - 最後が裸の z → 影は末尾の (1,0,0) を落とす（n = 1）。語は対角の塔（§7 の z=1 の列）。
  - 最後の荷の末尾が (0,0,0) → 影も語も「最後の字が n 個に複製」。
  - 最後の荷の末尾が非零 → 荷だけが Y[n] に縮む（展開が `bump` / シフトをすり抜ける）。
- z=1 の列の展開の**マスク付き一般形**が要る: 間の列は高さ ≥ a+1 なら何でもよく、行 1 が上がるのは
  「行 1 の親鎖でバッドルートに繋がる列」だけ。荷の列は繋がらない（荷の根 (a+2,0,0) の行 1 が 0 で
  鎖が止まる）ので、荷は上がらず z の列だけが上がる。
- 使い方は 2 つだけだった: 根（`Q ++ B↑2 ∈ W 0`、任意の `Bok B`）と、
  段（`A(h+1,1,0)(h+2,2,1) ++ B↑(h+3) ∈ W 0`、`A` は梯子の元）。この 2 本から
  337〜345 が全部出る（残りは `snocd_gen` と既存の `RunG_snoc2` / `LwA_unit11`）。
- **教訓**: 「台座の上に何を建てるか」で詰まったら、同じ行列を「字の語」として読み替えられないか
  試す。台座を高くするのは高くつくが、語の字を重くするのは既存の枠のままでできることがある。

## 10. 行 346〜360: 台座を作り直す型と、junk の族

- **型**: 新しい行が出るたびに、その行の末尾数列を「どの梯子の元の上でも通るセグメント」に
  一般化しておく。すると、その行自身が新しい台座（走りの底の元、レベル 1）になり、
  既に作った段（1 の列、単位、`(·,2,0)` の記録、対角、z の列）がそのまま全部乗る。
  実際 344 と 351 をセグメント化しただけで 346〜348, 352〜356 が出た。
- **junk の族 `GoodFb`**: 記録 `(a,b,0)` の右に付ける junk を、行列ではなく
  「(a,b) を受け取って列の並びを返す族 J」として抽象化し、3 段（`PU` / `PkGA` / `SegA`）で
  普遍であることを 1 つの構造にまとめた。族が決まれば、記録と junk の対角の塔 `Dzf J` の
  連鎖補題（4 本）が自動で付いてくる。個別の junk ごとに同じ 100 行を書き直さずに済む。
- **junk の字**: 記録の右に付く列は、実際には少数の型しかない。
  z の列 `(a+1,b+1,1)`、1 の列 `(a+2,1,0)`、荷 `Y↑(a+2)`（`Y` は `Bok`）。
  語（字の列）として扱うと、展開の場合分けがその語の中で閉じる。
- **マスクの見分け方**: 記録の上の z の列を展開すると、記録の子孫だけが行 1 で上がる。
  行 1 の親鎖は行 1 を狭義に増やすので、行 1 の値が記録以下の列（1 の列は行 1 = 1）は上がらない。
  この 1 つの補題で「どの列が上がるか」が決まる。
- **残った壁（行 361 以降）**: 語の最後に 1 の列を足すには「語 + 任意の `Bok` の荷」の普遍性が要る。
  展開の場合分けは字の集合で閉じるのに、測度が閉じない: 荷の複製は 1 の列の個数を増やし、
  1 の列の処理は荷を任意に増やす。多重集合順序も辞書式も効かない。
  影（記録↦(0,0,0), z↦(1,1,1), 1 の列↦(2,1,0), 荷↦Y↑2）を取ると、junk `[z, 1]` の影は
  `(0,0,0)(1,1,1)(2,1,0)` そのもの。「影が梯子の元なら junk は普遍」という反映定理が次の一手。

## 11. 行 361〜369: 複合字（z + 1 の列 + 荷）と、整礎性の作り方

- **壁**: 記録の右の junk に「1 の列」を足すには、その手前までの junk に任意の `Bok` ブロックを
  高さ `a+2` で吊るせること（hang）が要る。`snocd_gen` が要求する塔は台座の複製を含み、
  その複製を吊るす操作が hang そのものなので、迂回できない。
- **答え**: 1 の列を「語の字」として独立に扱うのをやめ、**1 つの字を
  「z の列 ＋ 1 の列 `k` 本 ＋ 荷 `Y`」の複合**にする。すると hang が字の中に閉じる:
  「1 の列を 1 本減らして、その分を荷にする」= 字 `(k+1, [])` の hang は字 `(k, B)` になる。
- **整礎性**: 多重集合順序（Dershowitz–Manna）を型クラスで持ち込まなくてよい。
  字を継ぐ操作 `S(k,Y)` を **`k` の強帰納法 ＋ 内側で荷 `Y` の W 帰納法（A2'）** で示せば、
  展開の 4 つの場合がちょうど閉じる:
  - 荷の末尾が非零 → 荷が `Y[n]` に（内側の帰納法）
  - 荷の末尾が `(0,0,0)` → 字が `n` 個に複製（内側の帰納法を `n` 回）
  - 荷が空で 1 の列が `k+1` 本 → `snocd_gen`、hang は字 `(k, B)`（外側の帰納法、`B` は任意）
  - 荷が空で 1 の列が 0 本 → 対角の連鎖（語が 1 字短くなる）
  「複製で 1 の列の本数が増える」「hang で荷が任意に増える」ので、本数だけ・荷だけの
  辞書式は効かない。複合字にまとめると、増える方（複製）は荷が縮み、減る方（hang）は
  本数が減る、と役割が分かれる。
- **1 段上の壁**（行 370 以降）: junk の中に高さ `a+3` の 1 の列（＝ z の上の梯子）が出る。
  複合字の 1 の列は全部 `a+2` なので届かない。ここから先は「z の上にもう 1 段の階層」を
  作る再帰的な文法が要る（記録の右 = z ＋ 次の段の梯子、を入れ子にする）。

## 5.5 木の字（`Jk1`）: z の上にもう 1 段の階層を作る

行 370 で見えた壁「複合字の 1 の列は全部高さ `a+2` なので、`a+3` の 1 の列に届かない」
に対する答えが、**字の junk を木にする**こと。

```
inductive Jk1 | nil | pay : Jk1 → TrioSeq → Jk1 | one : Jk1 → Jk1 → Jk1
jk1 l nil = []
jk1 l (pay N Y) = jk1 l N ++ Y↑(l+1)          -- 荷（高さ l+1）
jk1 l (one N M) = jk1 l N ++ ((l+1,1,0) :: jk1 (l+1) M)  -- 1 の列とその junk
```
字は `colJ a b N = (a+1,b+1,1) :: jk1 (a+1) N`、語は字の並び。
複合字 `colC a b (k,Y)` は「1 の列が全部兄弟」の特別な場合なので、これは真の一般化。

### 技法 A: z の展開の質量計算は「上がる列」だけ分かればよい
`oper_z1_mask` は junk `C` について一般（高さ `a+1` 以上なら何でもよい）。
族ごとに要るのは「どの列が行 1 で上がるか」だけ。木の場合、上がるのは z の列だけ:
- 1 の列は行 1 = 1 ≤ b なので、記録（行 1 = b）の行 1 の子孫になれない（鎖は行 1 を増やす）
- 荷は根の行 1 = 0 なので `block_root` + `le1_lower_bound` で鎖が入れない
これを木の構造帰納法で 1 本にまとめたのが `not_le1_jk1`。

### 技法 B: 荷の追加は「深さ 0」と「深さ ≥ 1」で別物
荷の末尾が `(0,0,0)` のとき、複製されるのは
- 深さ 0（字の直下）なら **字そのもの**。語 `ws` は定理の引数なので、複製は
  `GoodFb_rep` 型の反復で自由に作れる（測度に効かない）。
- 深さ ≥ 1 なら **1 つ外側の項**（1 の列 + その junk）。この複製の連鎖には
  「その junk を、任意の木の右に項として継げる」という仮定が要る。
この非対称性のせいで、荷の追加は AY0 と AYs の 2 本に分かれる。

### 技法 C: 深い位置の祖先条件
`snocd_gen` は `Ancd d Y`（高さ `d` 未満の可視な列は行 1 ≥ 1）を要求する。
木の荷の根は行 1 = 0 なので、そのままでは通らない。効くのは
**「高さ `l+1` 以上の塊の直後に高さ `l+1` 以下の列を置くと、塊の列は不可視」**
（`Ancd_append_low`）。木では各段の junk の直後に必ずその段の 1 の列が来るので、
文脈に沿って `Ancd_plug_jk1` で組み上げられる。

### 残っている壁
`APt nil ctx`（深さ ≥ 1 で junk のない 1 の列を継ぐ）だけが、蓄積した木 V について
「V を任意の木の右に項として継げる」を ∀V で要求する。V は複製の連鎖で
際限なく大きくなるので、木の大きさでも深さでも測度が回らない。詳細は notes 続き72。

## 5.6 深さを命題の添字にする（`APd k`）── 入れ子の壁の破り方

木の字（5.5）で「1 の列の上に 1 の列」を何段でも積むには、
「木 `V` を項として継げる」を**深さで添字づけ**するのが鍵だった。

```
APd 0 V     := ∀ U, GOK U → GOK (one U V)
APd (k+1) V := ∀ U, APd k U → APd k (one U V)
```

素朴に「∀ 蓄積した木」で書くと、木の構造帰納法で
- `one N M` の場合は**深さが 1 増える**（部分項だが深い）
- `nil` の場合は**深さが 1 減る代わりに任意の木**を要求する

となって、順序数 1 本の測度が作れない。深さを添字にすると:
- `one N M`: 部分項 M について**深さ k+1** の帰納法の仮定を使う。
  命題を `∀ k` で述べておけばそのまま取れる ✓
- `nil`: `APd (k+1) nil` は `APd_oneNil k`（同じ k の `APd_pay k` だけを使う）に
  帰着し、**任意の木を要求しない** ✓

つまり「深さの増減」を測度から**添字に移す**と矛盾が消える。
`APd k V ↔ ∀ ctx（長さ k+1 の良い文脈）, GOK (plug ctx V)` という言い換え
（`APd_iff`）を用意しておくと、文脈一般の補題（`APnil_gen`,
`GoodFb_snoc_innerJs/dupJs`）がそのまま使える。

もう一つの要点は、荷の A2' 帰納法（`AYd`）で**複製の連鎖に沿って仮定を持ち回る**こと。
`flat_mem''` の複製で作られる木 `V ⊕ (1の列 + 荷)ⁱ` は、
`APd k` を保つ（`APd k V'_{i+1}` が `APd k V'_i` と帰納法の仮定から出る）ので、
連鎖の各段で必要な仮定が自動的に揃う。

## 5.7 2 の記録を木に入れる（`two`）── 述語を「置ける場所」で割る

行 373〜374 で必要になったのは、木の junk の中に **2 の記録** `(l+1,2,0)` を置くこと。
構成子は `one` と同じ形にする:
```
two N M     jk1 l (two N M) = jk1 l N ++ ((l+1,2,0) :: jk1 (l+1) M)
```

### 置ける場所と置けない場所
2 の記録が上がるかどうかは `nextrel1` の**最小性**で決まる:
```
nextrel1 j0 j1 の最小性: ∀ j, j0 < j ∧ le0 j j1 → 行1(j1) ≤ 行1(j)
```
- **字の z の直上（先頭段）に置くと上がる**。le0 の親が z `(a+1,b+1,1)` で行 1 = b+1 ≥ 2
  なので最小性が通り、記録 `(a,b,0)` の行 1 の子になってしまう
- **1 の列の右なら上がらない**。le0 の祖先をさかのぼると行 1 = 1 の 1 の列で止まり、
  そこで `2 ≤ 1` になって最小性が破れる
- **2 の記録の直上に 2 の記録が来ても上がらない**。さらに左の 1 の列で止まるから

つまり必要なのは「**左のどこかに 1 の列があるか**」だけ。これを補題にする:
```
Grd M p q0 l = ∃ r v, p < r < q0 ∧ 行1(r) = 1 ∧ 行0(r) = v ∧ v ≤ l ∧
                      (∀ j, r < j < q0 → v+1 ≤ 行0(j))
not_le1_two : 1 ≤ 行1(p) → (Grd の r) → 行1(q) = 2 → ¬ le1 M p q
```

### 述語を割る
「置ける場所」が違うので、木の妥当性の述語を **1 つでなく 2 つ**にする:
```
JkJ  T   -- 1 の列の上（junk レベル）。two 可
JkOk T   -- 字の先頭段。two 不可
JkOk (one N M) = JkOk N ∧ JkJ M      ← 左兄弟は同じレベル、junk は 1 段下
```
`WOk`（語の字）は `JkOk` のままにすると、**質量計算（`rise_wordJ` など）を
一行も変えずに済む**。これが分割の勘所。

`not_le1_jk1` の仮定も選言にする:
```
JkOk N ∨ (JkJ N ∧ Grd M p q0 l)
```
`one N M'` の場合は、右（M'）に降りるとき 1 の列の位置を `r` として `Or.inr` を作る。

### 2 の記録を項として継ぐ
塔は `snocY_mem`（内部アンカー）で、その塔は**階段の木** `stairJ`:
```
Mtw Y0 [(l+1,1,0)] n = Y0 ++ jk1 l (stairJ n)
```
`stairJ` は `nil` と `one` だけでできているので、`APd`（深さ）の全ての段で使える。
だから `APd_two2 k`（2 の記録はどの深さでも継げる）が循環せずに出る。

## 5.8 「全深さ」が要るとき ── `APd` の限界

行 375（`R344 (4,2,0)(5,2,0)`）は `snocYd_mem`（歩幅 `dl = 2`）で出るが、
その塔には「2 の記録が自分の junk を持つ」木が要る。さらにその中で
**2 の記録が同じ高さに n 個並ぶ**（`bms -d` で確認）。

ここで `APd k`（深さ `k` の添字）の限界に当たる:
- 「木 `W` を**任意の深さ**で継げる」は、**定理**なら `∀ k` で書けるので取れる
  （`APd_all : ∀ T, JkJ T → ∀ k, APd k T`。部分項なら帰納法の仮定として使える）
- しかし `APd` の**定義の中**で ∀ 束縛した木については取れない。
  `APd` は添字（深さ）の構造帰納で定義するので、`APd ks' W` の `ks'` が任意だと壊れる

よって「2 の記録の左兄弟 `W`」を `APd` の定義に入れてはいけない。正しい形は
文脈の**意味的な良さ**を仮定として持ち回ること:
```
AllD T  := ∀ k, APd k T                          -- 部分項なら帰納法の仮定
PlugG D := ∀ T, (T が D に埋められる形) → AllD T → GOK (plug D T)
```
`PlugG` は `APd` を使うだけなので循環しない。フレーム（1 の列 / 1 の列 + 2 の記録）を
1 つ継ぐ補題を `PlugG D → AllD U → PlugG (D ++ [frame])` の形で作り、
2 の記録のフレームの塔では `AllD N` を使う。

**教訓**: 「深さ」を命題の添字にする（5.6）のは強力だが、
**添字にできるのは有限の深さまで**。塔が「同じ木を任意の深さで使う」形になったら、
文脈の良さを仮定に上げる（`PlugG`）方に切り替える。

## 6. 進め方の型（1 本の行列を通すときのチェックリスト）

1. `bms -s` で標準形か確認。`bms -d` で [1], [2] の展開を見る。
2. 展開の bad root と delta から塔の形を決める（`snocY_mem` 歩幅 1、
   `snocYd_mem` 歩幅 dl、`oper_flat` 平坦）。
3. 塔の各段を、既存の集合（`RunA`, `RunG E`, `PkG E`, `Pk3 E`, `Lay n`,
   `Lk k`, `StkF B y k`, `PU y`）の元として読めるか試す。読み方（どの列が階段で
   どれが junk か）を複数試す。
4. 読めなければ、足りないのは (a) 新しい列の条件の普遍性か、(b) 集合の
   吊るし可能性か、(c) 層をまたぐ複製か、を特定して notes.md に書く。
5. 新しい集合を作るときは `BaseOk` → `Iface` → 段の補題 → snoc の補題の順。
   既存の証明（`BaseOk_RunA`, `Pk23_hang`, `Lk_tower`）をほぼ写せる。
6. 緑になったら commit。notes.md に「何が通り、何が壁か」を 1 行ずつ足す。

## 5.9 ガードを迂回する前に、まず本体で使われているか grep する

`Rq (false :: ks) V = TopOk V`（「2 の記録の直上に 2 の記録」を弾くガード）は
`AYdT` から `GoodFb_snoc_dupJt` / `GoodFb_snoc_innerJt` / `APd_chainT` に
渡されていた。3 つとも**シグネチャに書いてあるだけで本体では使っていなかった**。

```
awk -v s=$L -v e=$E 'NR>=s && NR<=e && /hZt|hTop/ {print NR": "$0}' Small.lean
→ 出るのはシグネチャの行だけ ⇒ 死んだ仮定
```

外して呼び出し 4 か所を直したら 1 回で緑。`oper_snocYd` の `MidD` も同様で、
本体は `hM.ne` / `hM.head` / `hM.tail` の 3 つしか見ておらず、
`head1 : 1 ≤ 行 1` は死んでいた（`snocYd_mem0` に分離。これで bad part が
行 1 = 0 の 1 列でも塔が使える）。

**教訓**: 壁にぶつかったら、迂回路を設計する前に
「そのガードは証明本体で本当に使われているか」を grep で 30 秒で測る。
仮定の弱めは件数が測れる（追記8 の規則）ので、構成の一般化より先に試す。

## 5.10 代役の述語を立てる ── `APd (false :: ks) Z` → `TwoOk Z`

`TopOk` を外しても `AYdT` は `APd (false :: ks) Z` を要求し、
`Z = two nil nil` ではこれが作れない（`APd_cf` の hNall が固定 `ks` 版しか
供給しないので `APd_twoTwoGen` の強い hNall と噛み合わない）。

そこで `APd (false :: ks) Z` を**使っている場所の結論**を述語にした:
```
TwoOk Z := ∀ N, JkA N → (∀ j kk, APd (rep j true ++ (true::kk)) N) →
             ∀ j kk, APd (rep j true ++ (true::kk)) (two N Z)
```
`AYdT` が `APd (false::ks) Z` を使うのは `APd_chainT` の hstep を作る所だけで、
その結論がちょうど `TwoOk` の中身。`APd_two_of_ctx` の側は
`APd_iff (true::ks)` で `GOK (plug ctx ·)` に落とせば要らない。

**教訓**: 仮定 H が作れないとき、H の**使われ方**を全部見て、
「H から導かれる結論」だけを述語にすると、H より弱い仮定で証明が通ることがある。

## 5.11 破った 2 の記録の「後ろ」に何が置けるか

`TwoOk Z`（2 の記録の直上に木 `Z` を置ける）を 3 回に分けて広げた。
広がる／広がらないの境界がはっきりしたので記録する。

```
通る  Z = nil                  後ろに何も無い
      Z = pay Z' Y             荷は colJ_plug_pay で base ++ Y↑s に分解できる
      Z = two nil nil          極限（snocYd_mem の塔）。後ろに何も無い
      Z = one V nil            1 の列は荷の族 pay V C の極限（APnil_gen0）
      Z = one V (pay nil Y)    上 2 つの合成（AYs の TwoQ 版）
通らない
      Z = one V W  （W が木）
      Z = two N M  （M ≠ nil）
```

**理由は 1 つ**: 極限補題 `snocYd_mem` は列の**末尾**に 1 本継ぐ形しかない。
荷が後ろに来る場合だけは `colJ_plug_pay` で
「荷なしの字の列 ++ `shiftr01 s 0 Y`」に分解でき、`Y` について `W 0` の帰納法
（`GoodFb_snoc_dupJ*` / `innerJ*`）が回る。木は分解できない。

**代役クラスの型**（5.10 の続き）:
`AYs` の `CtxX ctx X` は `hAP`（`∀ V` で量化）にも渡っていて弱められなかった。
そこで `CtxX` の役目（= その位置に置ける）を果たすクラスを作った:
```
TwoQ U := JkA U ∧ TwoOk U ∧ (∀ C, Bok C → TwoOk (pay U C))
```
`TwoQ` が `U ↦ one U (pay nil Y')` で閉じていることが、`GOK_chainJ` の鎖に要る
`CtxX_itJ` の代わりになる。**「∀ V で量化された仮定」は、V の走る範囲を
クラスに絞れば弱められる。クラスは鎖の構成子で閉じていればよい。**

## 5.12 「クラス」は∀文脈でなければならない

5.10 / 5.11 で `CtxX` を代役クラスに置き換えたが、どこまで弱められるかを測った。

`AYs` が `CtxX ctx X` を使うのは 3 か所だけで、必要なのは
```
(a) hAP に渡す      → JkA V ∧ GOK (plug D V)
(b) dupJs / innerJs → JkT (plug D ·)（PT D）
(c) GOK_chainJ      → JkA_itJ
```
なので `AYs` 自体は「JkA ∧ GOK」というクラスで通る（荷の閉包は要らない。
鎖の各段の `GOK` は Y の帰納法の仮定が供給する）。

**しかし** `hAP` を `Z = nil` で作る `APnil_gen0` が
「任意の良い `V` について荷の族 `∀C, GOK (plug D (pay V C))`」を要求する。
これは 1 つの文脈での `GOK` からは出ず、`TwoOk V`（∀文脈）が要る。

**教訓**: 代役クラスを作るとき、
- その補題**自身**が要求するのは弱い性質でよい（測れる）
- だが**仮定 `hAP` を供給する側**が強い性質（∀文脈）を要求することがある

弱められるかを見るときは、補題の本体だけでなく
「その仮定を誰がどう作るか」まで辿る。

## 5.13 「荷閉包を枠条件に入れて、枠の本数で帰納する」

5.12 の「`APnil_gen0` の `hang`（`∀C, GOK (plug D (pay V C))`）は 1 つの文脈の
`GOK` からは出ない」は、**荷閉包そのものを枠条件に入れる**と解ける。

```
Fok 0 fs      := fs = []
Fok (n+1) fs  := ∃ W fs', fs = fs' ++ [fone W] ∧ Fok n fs' ∧ JkA W
                   ∧ Dk n W ∧ (∀ C, Bok C → Dk n (pay W C))
Dk n X        := ∀ fs, Fok n fs → TTwA (plug fs X)
```

枠木 `W` に課すのは「1 段浅い世界での良さ」と「荷閉包」の 2 つだけで、
`∀ D''`（どの文脈でも良い）は要らない。効く理由は

- `APnil_gen0` と `GoodFb_snoc_*Js0` が文脈を選ばず `hJT` しか要求しない
- 荷 `Y` の `W 0` 帰納で出る鎖 `itJ (pay X Y') k W` は
  **1 段浅い文脈 `fs'` に置かれた木**なので、`Dk_one` で組め、
  その荷閉包は `Dk_pay n`（1 段浅い段の定理）で出る

の 2 点。つまり **`n` の帰納が「鎖の荷閉包」を供給する**。

これで `Dk` は `nil` / `one` / `pay` について閉じ、右に伸びる鎖 `chn m` が
2 の記録の枠の上に置けるようになった（`P(8,2,0)`）。

**測り方**: 「∀文脈が要る」と見えたら、まず
「その ∀ は何段の枠まで必要か」を数える。段数で層を切って、
層ごとに（荷閉包込みで）定理を立てれば、∀文脈を避けられることがある。

**まだ届かない所**: `Dk` は `two`（2 の記録の枠）について閉じていない。
ラン塔の階段は文脈を**ブロック単位**で伸ばすので、`fone` 1 枚ずつの
`Fok` では受けられない。ブロック列版（`Trm` / `PreQ` / `TipQ`）で
同じことをやるのが次。

## 5.14 層が無限に要るときは、文脈そのものを述語の添字にする

「木 X が良い」を、X が置かれる**文脈**の形で相対化していくと
（枠 1 枚 → 2 枚 → …）、木の深さが可変のときに層が無限に要る。
そのときは、層を 1 枚ずつ名前を付けて積むのをやめて、
**文脈の形を添字にした 1 つの述語**にする。

  Ck j n Z := ∀ ctx, Cok j n ctx → GOK (plug ctx Z)

`Cok` を「文脈の作り方」の帰納的定義にすると、枠の良さの条件は
「1 段下の文脈での良さ」だけで書けて循環しない。鍵は、使う補題
（APnil_gen0 / GOK_twoNil_gen / GoodFb_snoc_dupJs0 / innerJt0 …）が
すべて文脈一般なこと。文脈一般の補題を先に作っておくと、この折り畳みができる。

添字の刻み方は「証明で必要になる文脈の作り方」に合わせる。
trio では 2 の枠が必ず 1 の枠の直後に来る（走りは別扱い）ので、
`(1 の枠, 2 の枠)` を 1 対として数える添字 j と、その上の 1 の枠の本数 n の
2 つで足りた。階段（塔）が対を増やさないことが、この刻みが閉じる理由。

## 5.15 添字の停止性の測度が足りないときは、順序数でなく多重集合にする

文脈の形を添字にした述語（5.14）で、添字の停止性を
「入り目の総和」のような**自然数の対の辞書式**で取ると、
族の節が書ける形が「総和が真に小さい形」に限られる。

trio の走りはここで詰まった。走りの階段 `nstN N i` を差すには
兄弟 `N` が「2 の枠 `i` 本ぶん深い形」に差せることが要り、`i` は非有界。
総和では `i` 対 `2` になって `i ≥ 3` で負ける。

直し方は **添字を多重集合にして Dershowitz–Manna 順序を使う**。

    測度  μ(ks) = ks を多重集合と見たもの
    順序  Multiset.IsDershowitzMannaLT （Mathlib.Data.Multiset.DershowitzManna）
    整礎  Multiset.instWellFoundedIsDershowitzMannaLT

DM 順序は「1 個取り除いて、それより真に小さい元を何個足してもよい」。
だから

    {1}*i < {2}    が全ての i について成立

となり、階段の高さが非有界でも減る。総和ではこれが取れない。

順序数 `Σ ω^kᵢ`（自然和）と同型だが、**順序数も ZFC も要らない**。
Mathlib の多重集合の定理 1 本で済む。

**測り方**: 「族の節が届かない」と見えたら、
届かせたい形の集合を並べて、その形の添字がどう増えるかを数える。
増え方が「1 個の大きい入り目を、小さい入り目 n 個に割る」形なら
多重集合の DM 順序で取れる。辞書式や総和で取ろうとしない。

## 5.16 節が張る形は「複製」でなく「族」にする

多重集合の測度で族 `WPd : List ℕ → Jk1 → Prop` を作るとき、
2 の枠の節が張る形を

    0^m ++ ks     （1 の枠を m 本足しただけ）

にすると、荷（pay）の連鎖が閉じない。連鎖の 1 歩は
兄弟 `X` を `two X T` に置き換えるので、`T` が形
`(k+1) :: (q ++ 0^m ++ ks)` に要る（`q` は連鎖の位置）。
`0^m` しか張っていないと `q` を吸収できない。

    ((k+1) :: ks), V => ∀ r, (∀ x ∈ r, x ≤ k) → ∀ U N, … → WPd (r ++ ks) (one U (two N V))

と**入り目 `k` 以下の任意の `r`** を張ると

    WPd ((k+1)::ks) T → WPd ((k+1)::(a ++ ks)) T     （a の入り目 ≤ k）

が出て、`a = q ++ r` で閉じる。族は付け足しで閉じている必要がある。

**測り方**: 節が張る形の集合を書き出して、
証明の中で要る形が全部その集合に入るか確かめる。
入らないなら、集合を「閉じた族」に広げる。広げても測度が減るなら通る。

## 5.17 兄弟の形の頭は固定する

同じ族で、2 の枠の兄弟に許す形を

    (0 :: q) ++ ks    （頭が 0 ＝ 1 の枠に固定、その先は入り目 ≤ k 自由）

にする。頭を自由（`q ++ ks`、`q` の頭が 0 でなくてよい）にすると、
荷の連鎖の 1 歩で `two X T` を 2 の枠の直下に差すことになり、
それは走りそのもので循環する。頭を 0 に固定すると連鎖の 1 歩が
`WPd_twoOf` に落ちて閉じる。

階段のほうは頭が 0 でも困らない（階段が要る形は `(0 :: (q ++ 1^t)) ++ B`）。
**両方が通る唯一の書き方**がこれだった。

## 5.18 走りの補題の「予算」を固定しない

走りの一般形

    WPd ((k+1)::ks) (two A nil)      左の兄弟 A は任意、底は nil

を `GOK_oneUV_RunSB` の階段で証明するとき、階段の各段で使う
`WPd_twoOf` の予算 `b` を **0 に固定してはいけない**。予算 0 だと

    hAall : ∀ ks, WPd (1 :: ks) A     A は入り目 1（予算 0）の形で良い

を要求してしまい、`A` 自身が走りを含む木（例: `two nil nil`）だと
そこが旧来の壁に当たって通らない。予算を `b`（`b+1 ≤ k`）にすると

    hAall : ∀ ks, WPd ((b+1) :: ks) A

でよくなり、`A` が深さ `b` までの走りを含んでいても通る。
階段の形が伸びる向きも `[b+1]` になるので、兄弟の条件（入り目 ≤ k）に収まる。

これで平らな走り（Δ=0、2 の記録が同じ高さに `m` 本）が

    WPd_twoIt_nil : m ≤ k → ∀ ks, WPd ((k+1)::ks) (twoIt nil nil m)

と `m` の帰納で出る。塔 `TWm m` が全部立ち、シートの
`(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(5,2,0)^(m+1)` が全部出た。

**測り方**: 補題の中で定数に固定しているパラメータ（予算・歩幅・深さ）を
探し、それを一般化したときに何が要求として弱まるかを見る。
「兄弟に要求する形」が弱まるなら、その兄弟に置ける木の種類が増える。

## 5.19 証明中を決める前に、行 1・行 2 の小さい行を先に測る

`…(5,2,0)(6,2,0)` を証明中にして壁に突っ込んでいたが、同じ前置きの

    …(4,2,0)(5,2,0)(6,0,0)

は展開が `…(5,2,0)(5,2,0)^n` で、既に緑の `R373_copies52_mem` そのものだった
（`flat_mem''` を 1 回使うだけ）。行 1 が小さい行（`(d,0,0)` / `(d,1,0)`）は
`flat_mem''` / `snocYd_mem0` のような「平らな塔」の補題で落ちることが多い。

**証明中を決める前に、同じ前置きで行 1・行 2 を下げた行列を `bms` で測る。**
展開の単位が既に緑の塔なら、その場で落ちる。

（ユーザーの指摘で気づいた。2026-09-11）

## 5.20 membership が取れたら `Aok` にする。一般補題が全部効くようになる

`P(6,0,0) = R375m ++ [(6,0,0)]` は木（`Jk1`）では書けない
（`(6,0,0)` は行 1 が 0 なので `MidD` の列でなく、`Aok_append_Mid` で伸ばせない）。
そのせいで「この行の先は全部壁」と判断していたが、間違いだった。

`Aok A` の 5 条件は

    mem : A ∈ W 0     ← これだけが難しい
    ne / deep / zroot / mono   ← 列を見るだけ（decide / simp で終わる）

なので、**membership さえ語のレベルで取れれば `Aok` になる**。`Aok` になれば

    Lv_snoc   : Aok A → A ++ [(1,1,0)] ∈ W 0          （Lv 1 0 A = Aok A）
    Lv_snoc2  : Aok A → A ++ [(1,1,0),(2,2,0)] ∈ W 0
    flat_mem''/ Aok.copies_Bok → A ++ [(1,0,0)] ∈ W 0
    flat_of_chain / snocd_gen / hangU11 …

が全部使える。実際これで 3 行いっぺんに進んだ。

**木が作れないことと、行が証明できないことは別。**
membership を取ったら必ず `Aok` を作ってから次を考える。

（2026-09-11）

## 荷の展開の鎖が「空荷」なら、平らな走りと同じ（2026-09-12, 行が進んだ）

`GOK (one U (two nil (two nil (pay nil [(0,0,0)]))))`（= `T6` を文脈一般に
したもの、`WPd (0::ks) Tb60`）が無条件で緑になった。効いた手順:

1. 荷の末尾 `(0,0,0)` を `GoodFb_snoc_dupJt0` で展開する。要る鎖は
   `twoIt N (pay Z Y) n` で、ここでは `Y = []` なので **鎖の荷が空**。
2. 空荷の `pay nil []` は語に何も出さない（`jk1_pay_nil`）ので、
   鎖 `twoIt nil (pay nil []) n` は**平らな走り** `twoIt nil nil n` と語が同じ。
   `WPd_congr` で乗り換えられる。
3. 平らな走りは `WPd_twoIt_nil n n` で予算 `n+1`。その上に `WPd_twoOf (k := n)`
   を被せると `WPd (0::ks) (two nil (twoIt nil nil n))`。
   **兄弟が `nil` なので `WPd_nilAll` がどの予算でも効き、幅 `n` に上限が要らない。**
4. 文脈は `plug_snoc2` で `ctx ++ [ftwo nil]` に 1 段伸ばして
   `GoodFb_snoc_dupJt0` の形（`two N (pay Z (Y ++ [(0,0,0)]))`）に合わせる。

**教訓**: 「予算が幅で頭打ちになる」のは兄弟が一般の木のときだけ。
**兄弟が `nil` なら予算は場面ごとに好きなだけ大きく取れる**ので、
無限に広がる走りも通る。壁だと思っていたものの一部はここで割れた。

これで
`(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(5,2,0)(6,0,0)(4,0,0)` と
`R341 ++ ((3,1,0)(4,2,0)(5,2,0)(6,0,0))^n`（全ての n）が無条件で緑。

## `Aok` の輪を回す（2026-09-12、シートが 10 行以上進んだ）

新しい行 `X ∈ W 0` を無条件に証明できたら、**次にやるのは新しい証明ではなく
`Aok X` を取ること**。`Aok` から先は既存の道具が輪になっていて、
1 周ごとに行列が大きくなる。

    Aok X
      → LwA_of_Aok            LwA 0 X
      → LwA_U11               RunA 0 1 (X ++ U11 0 m)      U11 0 m = (1,1,0)(2,2,1)^m
      → (BaseOk_RunA 0).aok   Aok
      → RunG_snoc2            ++ (2,2,0)
      → PkGA ⟨RunA 0, Iface_RunA0, 0, 1, ·, wordJ 2 2 ws, rfl, ·, rfl,
              (GoodFb_wordJ ws hw).pk 1⟩                    junk の語（WJ ws）
      → PkGA_Aok              Aok（★ここで戻る。反復できる）
      → LadB_mem              PU の梯子
      → LadB_flat_mem / LadB_flat_PU + (BaseOk_PU (n+3)).aok
                              梯子＋平らな段、そして **また Aok**

`Aok X` の作り方: `Aok_append_Mid (d := ...) (by omega) hAok_prev (MidD_one ...) hX`。
`Lv 1 0 A = Aok A` なので `Lv_snoc` / `Lv_snoc2` もそのまま使える。

**実測の注意**:
- 1 段 `(1,1,0)(2,2,1)^m(2,2,0)(3,3,1)^p` は **`p ≤ m` のとき標準形**。
- junk に `AltT i`（`i ≥ 1`）を入れると非標準になる。`AltT 0 = nil` は可。
- `RunA` の段を上げた族（`U11 h m` を積む）は標準形だが、`U11 0 m` を
  反復する族より**小さい**。

**なぜ効くか**: 1 行だけ証明して止めるとシートが 1 行しか進まないが、
`Aok` を取ると 3〜4 パラメータの無限族がまとめて出る。1 回のハートビートで
シート 10 行を全部入れ替えられる。

## 予算の型を上に伸ばす

予算を `ℕ` に固定すると、「兄弟の予算 `≤ k`」が鎖の幅の上限になる。
鎖の幅は荷の重複から**いくらでも大きく**なるので、`ℕ` では届かない。
予算を整礎な全順序 `Bud` にして、`ℕ` の段の**上**に 1 つ予算を置くと届く。
DM 測度は `Bud` が整礎なら整礎のままなので、整礎性は失わない。

**手順**:
1. 層の定義と補題を `Bud`（`LinearOrder` + `OrderBot` + `WellFoundedLT`）で書く。
2. `Scale Bud`（真に増える `nb : ℕ → Bud`）と「その上の予算 `t`」を仮定に持つ形で
   鎖の補題を述べる。
3. 具体型（`ℕ ×ₗ ℕ` など）で実体化する。段が足りなくなったら型だけ取り替える。

### 2026-09-13 追記: 伸ばすのは「ω 冪の**指数**の型」

`Colex (α →₀ ℕ)` は `ω^α` 未満の順序数を表す（`owG k m = toColex (single k m)` が
`ω^k · m`）。`α = ℕ` なら `< ω^ω` まで。荷の階数が `ω` に届くと（例: 荷
`(0,0,0)(1,0,0)` は `⟦n⟧ = (0,0,0)^n` で予算 `ω^n` を要求する）親には `ω^ω` が
要るので、`α = ℕ` では足りない。**指数の型を `ℕ ×ₗ ℕ`（順序型 ω²）にする**と
`ω^(ω·i+j)` が使えて届く。

- `Finsupp.Colex.wellFoundedLT` が要求するのは
  `[LT α] [Std.Trichotomous α (·<·)] [WellFoundedLT α]` だけなので、`ℕ ×ₗ ℕ` で通る。
- 順序の補題（`owG_zero` / `owG_ltR` / `owG_ltL` / `owG_add_lt` / `owG_add_same`）は
  `α` を全称にして 1 度書けば両方で使える。`omega` の代わりに
  `ne_of_lt` / `lt_trans` を使うだけ。
- これで `R600 (7,0,0)(6,0,0)^k`（どの `k` でも）が出て、`flat_mem''` の極限で
  `R600 (7,0,0)(6,0,0)(7,0,0)` が出た。シートの台座が 1 段上がった。

**測り方**: 壁の補題が「幅 `m` の走りを予算 `k` の兄弟に置く」形になっていて、
`m` が仮定から抑えられないなら、この手が効く。
`m ≤ k` が仮定から出るなら別の原因。

## 族が作れたら極限を取る

`flat_mem''` は `∀ n, Y0 ++ M^n ∈ W 0` から `Y0 ++ M ++ [(d,0,0)] ∈ W 0` を出す
（`M ≠ []`、`M` の頭の高さ `< d`、残りの高さ `≥ d`）。

**なぜ効くか**: 木で直接証明すると荷の W 帰納の段数だけ予算が要る。段数が
非有界だと予算では届かない。極限の側から入ると、必要なのは**族**（各 `n` で
段数は有限）だけなので、段数の壁を迂回できる。

実測: `R600 ++ (7,0,0)` は木で書くと荷 `(0,0,0)(1,0,0)` の展開が
`(0,0,0)^n` まで降りて段数が非有界。`R600 ++ (6,0,0)^k`（各 `k` は予算 `ω^k`）
の極限としては `flat_mem''` 1 回で出る。

**手順**: 新しい行が欲しくなったら、まず `bms "<行>[n]"` で展開を見る。
`Y0 ++ M^n` の形なら族を作りに行く。

### 2026-09-13 追記2: 荷を 1 つずつ足さない。荷の**階**を作る

`R600(7,0,0)(7,0,0)` の次に `R600(7,0,0)(7,0,0)(7,0,0)` を証明しようとすると、
荷 `(0,0,0)(1,0,0)(1,0,0)` のために指数の型を `ℕ ×ₗ (ℕ ×ₗ ℕ)` に取り替えることになる。
その次は `ℕ ×ₗ (ℕ ×ₗ (ℕ ×ₗ ℕ))`。これは 1 行ごとに型を作り直す作業で、収束しない。

**正しい手順**: 荷を族にして、階の帰納法を 1 回だけ書く。

1. 荷の族 `Yv j = (0,0,0)(1,0,0)^j` を定義し、`(Yv (j+1))⟦n⟧ = (Yv j)^n` を出す。
2. 指数の型を、その族の要求する順序数が**全部入る**型にする。
   階 `j` は `ω^j` を要求するので、指数の型は `Bw = Colex (ℕ →₀ ℕ)`（順序型 ω^ω）。
   予算は `BwG Bw = Colex (Bw →₀ ℕ)`（順序型 ω^(ω^ω)）。
3. 3 つの述語（鎖 `AStat j` / 枠に入れる `BStat j` / 予算に乗せる `CStat j`）を
   `def` で書き、`A(0) → B(0) → A(1) → B(1) → …` の一本道の帰納で回す。
   `C(j)` は `B(j)` の `WPdT_cb` 包み、`A(j+1)` は `C(j)` を使う、
   `B(j+1)` は `A(j+1)` と `B(j)` を使う。
4. 荷に依存しない `_at` 補題（`GoodFb_snoc_innerJt0` の包み）を 1 本作っておくと、
   `B(j)` は「`Y⟦n+1⟧` の段が全部置ける」だけを示せばよくなる。

これで 1 行ではなく **`R600 (7,0,0)^k` が全部の `k` で**出て、さらに
`flat_mem''` をもう一段当てて極限 `R600(7,0,0)(8,0,0)` が取れた。

### 2026-09-13 追記3: 荷の世界は原始数列（PrSS）。部品を汎用にしてから登る

`Jk1.pay N Y` の荷 `Y` が平らなとき、`Y⟦n⟧` は原始数列システムの展開そのもの
（悪い部分の繰り返し）。順序型は `ε_0`。だから荷を 1 個ずつ足す限り、
行 1 本ごとに指数を手で置く作業になり終わらない。

**汎用にしておく部品**（荷に依存しない形で 1 回だけ書く）:

- `WPdT_twoAY_at`: 荷 `Y` の `⟦n+1⟧` の段が全部置ければ `pay Z Y` が置ける。
- `TopLd_of_AtLd`（`WPdT_cb` 包み）、`RunLd_of_TopLd`（鎖の二重帰納）。
- `AtLd_iter`（`Y⟦n+1⟧ = Y'^(n+1)`）と `AtLd_fam`（`Y⟦n+1⟧ = F n`）。
- 指数の型 `α` はパラメータにする（`Pws α`）。予算は `BwG α`。
  足りなくなったら `α := BwG α` に取り替えるだけで全部が回る。

この形にすると、新しい荷 1 つは「`oper` の計算＋指数の不等式」だけ、60 行で済む。

### 2026-09-14 追記: 台座固定の行は、たいてい台座一般に移植できる

`SmallA` の行（`R375t*_mem` / `R375h*_mem` など）は台座が `R338` 固定に見えるが、
証明の中身は

    rowJ_mem_genF hA hG'     （`hG'` は木の `GOK` で、台座に依らない）
    flat_of_chain / SegB_snoc2 / snocd_gen / snocY_mem / hangU11

だけで、**`Aok_R338` を任意の `hA : Aok A` に置き換えれば通る**。
2026-09-14 に `H` 族 11 本（`(2,0,0)`〜`(5,1,0)`）をこれで移植し、
新しい台座 `Z789` に載せてシートを一気に伸ばした。

**手順**:
1. `grep -n "(hA : Aok A) : A ++" lean/SmallA.lean` で台座一般の単位を数え、最大を取る。
2. その単位の上の行（`R375<x>*_mem`）の証明を読み、`Aok_R338` を `hA` にする。
   台座の部分列（`R341 = R338 ++ (1,1,0)(2,2,1)` など）は
   `A ++ [(1,1,0),(2,2,1)]` に置き換え、`Aok` は `LvB_unit11` / `snocd_gen` で作る。
3. 単位の等式（`R375h_eq'` など）は `A` 付きで書き直す（`UaH_cons` / `UaH_eqc` / `UaH_eqd`）。

**新しい台座を作ったら、必ずこの移植をしてからシートを書くこと。**
台座を 1 つ進めるより、既存の行 11 本を載せ替えるほうが遥かに大きい。

## 2026-09-12 追記4: 登る前に、既存の台座一般の補題で出る行と比べる

`U375aH`（= `U375a ++ jk1 4 (TW 3)`）の上に `(2,0,0)` から `(7,0,0)` まで
11 列を 1 つずつ登った。ところが `SmallA` に既にあった

    U375a6_mem_gen {A} (hA : Aok A) : A ++ U375a6 ∈ W 0        （台座一般、緑）
    U375a6 = (1,1,0)(2,2,1)(3,1,0)(4,2,0)(5,2,0)(6,0,0)

1 本で出る `Z789 ++ U375a6` の方が、登り切った `Z789 ++ U375aH ++ (7,0,0)` より
**大きい**（`bms -c` で測定）。`(6,0,0)` は `(5,2,0)^k` の極限なので、
`jk1 4 (TW n)` の塔（`TowOkM`、無条件で緑）を全部飲み込む。

**新しい行に取りかかる前に、`grep -n "_mem_gen" lean/SmallA.lean` で台座一般の
補題を全部出し、その結論の行列を `bms -c` で今のシートの頭と比べる。**
台座一般の補題は台座を差し替えるだけで新しい行になるので、いつでも一番安い。

## 2026-09-13 追記5: 族の壁で止まったら、最新の行の台座一般の補題から平らな極限を先に取る

`R375m (6,1,0)` の次の行 `R375m (6,1,0)(7,1,0)` は族の壁（走りの直下の空木）で
止まっていた。その間の行 `R375m (6,1,0)(2,0,0)` は、最新の行の台座一般の補題
`A375m61_gen : Aok A → A ++ U375a ++ (6,1,0) ∈ W 0` と `flat_of_chain` だけで出た
（新しい補題は `MidD_U375a61` の `decide` 1 本）。壁を攻める前に、
「最新の行を台座一般にした補題 + `MidD` + `flat_of_chain` / `Aok_chain`」で
届く行を先に取る。

## 2026-09-13 追記6: 予算族で詰まったら、既存の W 階層の定義の方を直す

最終目標は、字の文法と予算の族（WPd / WQt / WEd / WVd）を何本作っても出なかった。
出たのは `Wset.Aop` を 2 か所だけ変えた族 `Wg`（分岐 2 に natDom ガード、分岐 3 を
行 2 = 0 の孤児に限る）を作り、既存の `GoodFb` の閉包補題を生の字の語に写したときだった。
- 過去にガードが反証された例（行 2 の死んだ孤児）は、分岐 3 の絞りでどの分岐にも入らなくなる。
- 族の帰納法は「文脈」を持たないので、文脈側の走りの問題が起きない。
- 語の文脈（持ち上がり・頭の潰れ）は既存の `GoodFb` がすでに扱っていた。

## 2026-09-13 追記7: 根に語を吊るすときは、W* の根の値を固定する

字の頭 `(x,y,1)` の行 2 の親は、字の直前の記録（根）。`W*` の長さの帰納は木の根を切り離すので、
字を含む列には使えない。一般のリフト（`TowerGraft2`）はタイで壊れる。
効いたのは、`Wstarg_closed` の証明が根の行 1 の値 `v` を一度も変えないことに気づいて、
`v` を固定した `Wstarv v` を作ることだった。根の直後の語 `A = rword 0 v l` は `v` に依存するので
`Wstarg`（全ての `v`）には入らないが、`Wstarv v` には入る。すると既存の連結の手
（`XAg_closed`）がそのまま使え、「語の後ろに `Wg` の任意の元を吊るす」補題が 3 行で出た。

- 全称の族で詰まったら、証明の中で本当に動いている変数だけを全称に残し、残りは固定した族を作る。
- 語の根への依存（字の行 2 の親）は、語を族の添字 `A` に押し出すと帰納の外に出る。

## 2026-09-13 追記8: 台座に吊るして、残りを Wg の世界の問題にする

目標 `(0,0,0)(1,1,1)(2,1,0)(1,1,1)` は、シートの行を 1 つずつ登る代わりに次の 2 段で出た。

1. **台座の吊るし（BH）**: `Aok A`、`T ∈ Wg 2`（先頭の高さ 1）なら `A ++ T ∈ W 0`。
   T の列の行 0 の祖先は T の中と A の根だけなので、`GOKR_of_Wg2` の場合分けがそのまま使える。
   これで W 0 の行（台座 R338 の上の 1 本の木）は、Wg の中の木の所属に帰着する。
2. **Wg の世界**では、W 0 の文脈の型（seg / pk / pu、IfcV、PU）が要らない。部分頭 `(1, v+1, 0)` の
   後ろは `Wstarv (v+1)` と吊るしの補題 `hang_Wg` で無料。残るのは段 v の語 1 本だけで、
   それは根が生き返らせる塔と、段 v0 に一般化した字の閉包で出た。

- 目標の展開 `G⟦n+1⟧ = R338 ++ lift(G⟦n⟧)` のように、展開が「台座 + 1 段持ち上げた自分」になっているなら、
  持ち上げた側を Wg の世界で帰納する。
- 字の中身の条件（RiseOk）は、段（根の行 1）ごとの形 `RiseOkv v0` にしておくと、タイの中身も入る。

## 2026-09-13 追記9: 段 0 も Wg の世界で組む。台座は要らない

行 423〜518（約 90 行）は、W 0 の台座（Aok）を使わず、段 0 から Wg の世界で出た。

- `Wg 0 ⊆ W 0`。段 0 には graft の分岐が無いので、ガードを外すだけで入る。
  すると段 0 の語にも `Wstarv 0` と吊るし `hang_Wg` が使え、行は「段 0 の語 + 1 段上げた Wg の元」の形で
  部品を足し合わせるだけになった（1 行あたり数行の証明）。
- 既存の閉包補題の仮定を見直すと、使っていない仮定（`1 ≤ b`）や、段 0 で空になる場合（`Wg 0` の中身の孤児）が
  見つかった。仮定を落とした写しで、段 0 までの族がまとめて出た。
- 新しい形（先頭にタイの中身の字）は、潰れの計算（持ち上がる列のマスク）だけを書き直し、閉包の場合分けは
  元の証明をそのまま写した。

## 2026-09-13 追記10: 展開の場合分けを中身の規則として一度だけ書き、行は分解スクリプトで組む

行 519〜611（93 行）は、行ごとの補題を書かずに出た。

- 中身の最後の列の展開の場合分け（中身の中の親・平らな列・孤児・根からの塔）を、中身の閉包 `GT` の規則として
  一度だけ証明した。行は「語 + 吊るした部分木」に再帰的に分解でき、分解に沿って規則を並べるだけで証明項になる。
  そこでスクリプトで分解と証明項の生成をした（1 行 3 行、93 行が 1 回の検査で緑）。
- 持ち上げと展開は `slift_oper` で交換する。中身の中に親がある場合は、段ごとの潰れを計算しなくてよい。
  既にある一般補題（`slift_oper`、`hasParent_append_gen`、`oper_shift`）を先に探したことで、規則 1 つが 20〜40 行で済んだ。
- 段をまたぐ一様性の壁は、前提の側を「全ての段と、その段の全ての荷」に広げると越えられた。
  前提は荷の導出の帰納で出るので、導出を段の間で移す必要がない。

## 2026-09-13 追記11: 複写される部分木は「どの文脈にも継げる」形で仮定に持つ

行 612〜779 の中身（タイの下の子）は、タイの部分木の複写（`Y'' ++ (q :: Q)^n`）で閉包が崩れた。
部分木 q :: Q を「深さ d のどの文脈（全ての段）にも継げる」という性質で文脈の仮定（Att）に入れると、
複写は同じ性質を n 回使うだけになり、荷の Wg の導出の帰納がそのまま通った。

- 複写が起きる場所（開いた単位の列）は文脈の側に持たせ、新しく足す側（荷・タイ・子の森）は文脈によらない規則にする。
- 仮定の再帰（深さ d+1 の文脈が深さ d の文脈を参照する）は、深さについての構造的な再帰で定義できる。

## 2026-09-14 追記12: 一般の理論が詰まったら、展開が既存の級に入る行を展開で直接出す

行 1317〜1337（クラス I の先頭 20 行）は、遠い字の一般の規則（挿入で閉じた族）が閉じないまま出た。

- 最小の行 (0,0,0)(1,1,1)(2,1,1) は、展開 ⟦n⟧ が全て遠い字を含まない塔になる。
  塔を高さの帰納で既存の規則に分解し、natDom と展開の等式（oper_zcone）で行そのものを入れた。
- その木を段 u で全て Wg に入れておくと、あとは既存の吊るし（starOK_hang）、語の追加（GT）、
  根の並び（XAg_closed）で、形の近い行がまとめて出た。
- 次の行が要る規則（遠い字が中身の中や 2 番目の語にある）を測ってから、一般の理論に戻る。

## 2026-09-14 追記13: 形を 1 つずつ足す前に、同じ形の n 個の並びの帰納にする

行 1338〜1385 の 46 行（1344, 1345 は非標準形）は、「先頭に続く遠い語 n 個」の補題 1 組で出た（GzF）。

- 遠い語 1 個（GzD）の証明を 2 個目に写す代わりに、n 個の列 farR r n を定義し、
  「全ての錨の列と状態で GpT」を n の帰納で示した。n+1 個目の潰れの塔の各段は、
  n 個の列（帰納法の仮定）と、字の中身の F の子（錨の列を 1 つ伸ばした GpT、塔の高さの帰納）だけでできる。
- 仮定を「全ての錨の列と状態」にしておくと、塔の段で錨の列が伸びても同じ仮定が使えた。
  塔の段ごとの語の持ち上げ（hBase の型）を別に作らずに済んだ。
- 行 1 が一定の列（CR）では amin・coneV・mlift・slift が全て「行 1 を足すだけ」になり、
  持ち上げの計算が 1 行の補題で済んだ。
- 最上段（BwT）も同じ n の帰納。節点の下の補題（n 個）を塔の中で呼ぶだけ。

## 2026-09-14 追記14: 族が作れないときは、動かない部分を「単位」にして帰納を 2 段にする

行 1386〜1449 の 64 行（GzH〜GzK）。遠い字のあとの中身を族（CtxP）にすると、flat が接頭辞に語を足し、
錨の挿入の壁に戻る。

- 遠い字のあとの項のうち、写しでも再持ち上げでも行 1 が動かないもの（荷、行 1 が b+1 の子のないタイ）だけを
  「単位」にした。並びの形が (A, o, f, b) に対して b + liftOff + 1 だけで決まるので、
  「全ての錨の列と状態で GpT」の帰納が回った。
- 単位の段では族を作らず、語の並びの族そのもの（GpT_ax）の tie / oper / orph を深い位置で直接使った。
  flat だけは語の複写になるので、「標準の並びの後ろに足せる」（GTU）の繰り返しで出した。
- 証明を書く前に規則を測り直した: 最上段の写しは閾値が根の段なのでタイが持ち上がる。
  最上段の単位を荷だけにすると 172 行 → 130 行。書いてから気づくより安い。
- 既存の証明が「接頭辞 L ごと」の形なら、条件つきの版は写して引数を 1 つ足すだけで済む（GTC）。

## 2026-09-14 追記15: 単位を増やすときは、単位の並びの続きを差し込み口の族にして一般の規則を借りる

行 1450〜1453（と、その間の行列 4 つ、GzL）。遠い字のあとに子つきのタイが来る形。

- ユーザーの指摘で、シートの行の間の行列（Q1〜Q4、Q3 = Q4⟦2⟧）を先に埋めた。Q1・Q2 は既存の規則で出て、
  Q3・Q4 で初めて「子つきのタイ」が要ることが分かった。飛んでいるシートの行より、間の行列の方が要る規則を 1 つずつ見せる。
- 単位の種類を足す代わりに、遠い字のあとの中身を任意の低い列 X にし、
  「X のあとの続き」の族 okW u X := Hd X ∧ LowC X ∧ （全ての錨の列で語が良い）が SlotAx を満たすことを 1 回だけ示した。
  すると荷は slot_load、子つきのタイは GTs の nslot という既存の一般の規則でそのまま足せた。
- 族の定義に形の条件（Hd、低さ）を入れておくと、oper の仮定（展開した形の族の元）が展開した形の条件を運ぶので、
  「展開で低さが保たれる」補題を書かずに済んだ。結論の側は U⟦1⟧ = U.dropLast と末尾の親だけで出る。
- 既存の単位の版（GzH）は、単位を低い列に置き換えるだけの機械的な写しで緑になった（GzM）。

## 2026-09-14 追記16: 族の FarP 補題（FarP_GpT_ge / lt）をそのまま規則として使う

行 1454〜1461（GzP〜GzR）。遠い字と低い列のあとに、行 1 が b+2 の空の節点（o = 1 なら F）が来る形。

- 新しい塔を組まずに、級の族そのものの FarP の補題（FarP_GpT_lt / ge）を錨なしの添字で具体化した。
  底の段が b+2 なので h2 は τ = 1（子つきのタイ）だけで、前回作った低い列の族の規則（okWF_tie）で出た。
  h1 は底のない並びそのもの。まず展開（tools/trio.py の ⟦n⟧）を生成器に通し、全て既存の形に入ることを確かめてから補題を探した。
- 同じ補題を段 o ≥ 2 に広げると、b+2 は節点の段以下で t の持ち上げで動かないので、語の述語（PVF）もすぐ出た。
  次の壁は「動く列」（o = 1 の F、錨 1 の再持ち上げで動く b+2）で、測った失敗の理由から分かる。
