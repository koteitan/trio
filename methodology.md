# methodology.md — 小さい行列から少しずつ証明を増やす方法論

`(0,0,0)(1,1,1)(1,0,0)` から始めて、シート行 336 `psi(W_w * psi(W))` まで来た
やり方を、考え方とメタ的な技法として書き残す。（行 374 まで到達）Lean の名前は最小限にし、
数式は ASCII で書く。対象は 3 行バシク行列（BM4, z < 2 の断片）、目標は
「行列 M が W 0 に入る」= M の全ての展開 M[n] が W 0 に入ること。

## 0. 基本の姿勢

- **証明の単位は 1 列。** 証明できている行列 A の右に列 c を 1 本足した A ++ c を
  次の目標にする。標準形の順に、飛ばさずに埋める（シートは飛んでいる）。
- **標準形かどうかは形で判断しない。** `~/code/yaBMS/c/bms -s "<matrix>"`（1 なら標準形）。
  展開も手で追わず `bms -d "<matrix>[n]"` で確かめる（bad root, delta, 複製の形）。
- **こまめに書く。** 1 回の返答で出せる量に上限があり、超えると全部消える。
  検討→ notes.md に書く→検討、の短い繰り返し。書く内容は「今の方針、分かったこと、
  失敗した手とその理由、次にやること」。補題の候補は 1 個ずつ Lean で確かめる。

## 1. 行き詰まったときの手順

A ++ c が証明できないとき、次の順に試す。

1. **c を 1 つ落とす。** c = (4,2,0) が駄目なら (4,1,0)、それも駄目なら (4,0,0)、
   高さを 1 つ下げて (3,x,0)。通るものと通らないものの境界に、壁の正体が出る。
2. **A ++ c を展開する。** `bms -d` で A ++ c[1] を見る。A ++ c[1] は A ++ c より
   「大きい」が構造が見えている行列で、A ++ c の証明にはこれが全部要る。
   まず A ++ c[1] を証明してみる。
3. **A ++ c[1], A ++ c[2], A ++ c[3] をいくつか証明する。** 個別の証明が通れば、
   その証明の形（どの集合の元として読んだか、どの補題で継いだか）にパターンが
   見えてくる。
4. **パターンが見えたら A ++ c[k] を k の帰納法で証明する一般化を考える。**
   一般化できれば「∀ n, A ++ c[n] ∈ W 0」なので、それが A ++ c の証明そのもの
   （`A1_intro` で全展開の所属に帰着、実際には `snocY_mem` / `snocYd_mem` /
   `oper_flat` が展開の形ごとに用意してある）。
5. **一般化が閉じないときは、その理由を 1 文で notes.md に書く。** 「junk の条件が
   複製先の文脈で足りない」など。次に同じ壁に当たったとき、同じ理由かどうかを
   すぐ照合できる。

### 1-a. Lean の証明も「写す」（2026-09-05 に 9 回連続で効いた）

上の 3.→4. は行列の話だが、**Lean の証明にも同じ順序が効く**。
`k` や添字について一般化した定理が要るとき、**一般形を設計する前に、
既存の 1 つ下の場合をそのまま写して `leanman check` に error を出させる。**

- 90 行の証明を写して、詰まったのが 1 行だけ、ということが起きる。
  その 1 行の goal が、設計を考えても出てこなかった本当の制約を教える
  （実例: `AYdT` → `AYdK` の k=1 で `msr_word` が出た）。
- 分析で「3 つの要求が両立しない」と結論しても、写すと 1 行に落ちることがある。
  2026-09-05 は「原理的に不可能」が 3 回とも撤回された。
- 写すたびに **`js = replicate j nil` のような「既存に一致する」固定補題**を置くと、
  あとで定義をいじったときの回帰が即分かる。

**手順**: 写す → error を見る → 詰まった 1 行の goal を貼る → そこだけ考える。

### 1-b. 道具に聞く順序

「この仮定は効いているか」「ここは何箇所壊れるか」は、読んで判断しない。

1. **linter を読む。** `leanman check` の出力の unused variable が、
   「その仮定は使われていない」を既に言っている（実例: `GoodFb_snoc_*Jt` の `hZt`）。
   読むより速く確実。
2. **消して 1 回検査する。** error の数と**種類**を見る。
   `⟨…⟩` の項数・射影・束縛の数は使う側の話で機械的。
   `unsolved goals` / `no goals` は供給側（`.mp` 側）の可能性があるので中を見る。
4. **コーパスで数えるときは、種を変えて 2 回測る。**2026-09-05 に種 4 個・2017 個で
   「0 件」と出たものが、種 10 個・8289 個では 256 件になった（narrow corpus の
   アーティファクト）。1 回の分布を結論にしない。
5. **列の見た目で木を判定しない。**同じ列の並びでも木の読み方は複数ある
   （2 の記録の直前の 1 の列は、塔の単位の `one` 枠かもしれないし、その 2 の記録の
   junk かもしれない）。proxy で数えると入れ子自身を junk として数えてしまう。
   判定には `bms -d` の `Parent Index Matrix` か、木のパーサを使う。

3. **展開は `bms -d`。** junk を全部 nil にすると区別できない形がある
   （`unitB` と `unitC` は junk の位置が違うが、nil では同じに見える）。

実例: (0,0,0)(1,1,1)(1,1,0)(2,2,0)(3,3,0)(4,3,0)(4,1,0) は壁だった。
(4,1,0)[1] = 行列全体を高さ 4 に吊るしたもの、を証明しようとして
「吊るしの不変量を、どの台座でもよい形に取る」着想が出て、壁が消えた（§4）。

## 2. 行列を「集合の元」として読む

1 本ずつ証明していると同じ証明を何度も書くことになる。同じ作り方で作れる行列を
集合にまとめ、集合について 1 回証明する。

- 集合の元は「土台 + 階段の列」。階段とは、高さ 1, 2, ..., h の列が 1 本ずつ
  この順に並んでいること。集合の**レベル**は階段の最後の列の高さ h。
- 階段の列の右に付く、その列より高い列を **junk** と呼ぶ（造語）。junk はレベルに
  数えない。junk には条件「この集合の別の元の同じ場所に付けても W 0 に入る」
  （再継ぎ条件）を付ける。
- 集合に要る性質（`BaseOk`）: 元が W 0 に入る / 頂上（レベル + 1）にブロックを
  吊るせる（hang）/ 再継ぎできる junk を頂上の列に吸収できる（close）。
  これがあれば、頂上に (h+1, 1, 0) を置く塔（行列全体の複製を上に積む）が回る。
- **同じ行列でも読み方は複数ある。** (0,0,0)(1,1,1)(1,1,0)(2,2,0)(3,3,0)(4,3,0) は
  「レベル 2、(3,3,0)(4,3,0) は 2 の junk」とも「レベル 3、(4,3,0) は 3 の junk」とも
  読める。上に置きたい列の高さに合わせて読み方を選ぶ。
- 列 c を頂上に置くとき、c の bad root は階段の列の中にある（junk は祖先にならない）。
  bad root が階段のどの列かで塔の形が決まる。
  - bad root が (1,1,0)（2 を置くとき）: 複製は 1 から始まる。前の段を台座
    （`LwA`）に飲み込んで rank 0 からやり直せる。
  - bad root が 2 のブロック（3 を真横に置くとき）: 複製は 2 のブロックの積み上げ。
    同じ層の中で `RunG` の段が 1 つ増えるだけ。
  - bad root が 2 段以上下: 複製は層を登る。層をまたぐ条件が要る（§4）。

## 3. 定義のコツ（循環を避ける）

- **集合の定義は exact rank でよい。** `RunG E (j+1)` のブロックの条件は
  `RunG E j` の元の上だけ。∀ rank を定義に入れると非構造的になる。
- **普遍な条件は、集合を定義した後の述語として書く。** 例: junk J が
  「どの j でも `RunG E j` の元の上に継げる」は `RunG` の定義後に書ける
  （`JkG`）。手で置いた junk（(3,3,0)(4,3,0) など）はこの普遍条件を満たすと
  証明できる。
- **吊るしの不変量は自由に選べる。** `blkD_memS` の不変量 P は任意の述語。
  「∃ 台座 E, Iface E ∧ ...」のように ∃ で潰した集合を不変量に取ると、
  吊るしの途中で生まれる junk（断片）は不変量全体の上で再継ぎでき、
  普遍な junk になる。これが行 295 まで届いた鍵（`PkGA`, `Pk3U`, `Lk`）。
- **台座を抽象化する界面** `Iface E` = `BaseOk E` + 再台座性（E の元は
  `LwA` の頭 ++ 単位 M で、M はどの `LwA` の台座にも継げる）。この 2 つがあれば
  `RunG E`, `Pk3 E` を E の上に積めて、界面も継承される。層は
  `Lay (n+1) = Pk3 (Lay n)` のような再帰で作る。
- **条件を足すとき、それが「木の条件」か「枠の条件」かを先に言う。**（2026-09-05）
  2026-09-05 に「2 の記録の直上に 2 の記録を禁じる」という条件を、木の言語 `JkJ`
  （その木が妥当か）に書いた。だが本当は枠の言語（`GCtx` / `APd`、どの文脈に置けるか）
  の条件だった。層を間違えたので **5 つの定義に重複して書かれ、どの証明にも効かず**、
  外すと `error 85` のうち 84 個が項数ずれ、`GCtx` の `Prop` 引数 `t` ごと消えた。
  同じ直観を正しい層（枠）に置き直したものが `GCtx0` / `APd0`（枠 junk を `nil` に限る）。
  **層を間違えた条件は、消えないまま増える。**足す前に「これは木の話か、枠の話か」を
  1 文で言えること。

- **述語が自分を「否定的な位置」で呼ぶときは、辞書式 (段数, 形) で定義する。**（2026-09-05）
  `APd ks V` の枠条件が `APd` 自身を仮定側（負の位置）で呼ぶ形は、帰納的述語
  （最小不動点）にできないので再帰で定義するしかない。そのとき**形だけの測度**を使うと、
  枠条件が要求できるものに上限が出る（ここでは `msr_word`: 「entries ≤ 自分の頭 の語」まで）。
  上限を超える要求が要ると詰む。解:
  ```
  正の再帰（自分の続き）  段数 n を保って形を減らす
  負の位置（枠条件）      段数 n を落として、形は任意
  termination_by (n, msr ks)
  ```
  段数が落ちる呼び出しは形を問わないので上限に当たらず、段数を保つ呼び出しは形が減るので
  既存の形の帰納法・証明の構造がそのまま使える。純粋な step-indexing（正の再帰でも段数を
  落とす）だと形の帰納法と噛み合わず全定理に段数が波及するが、辞書式ならそれを避けられる。
  実例: `APdL`（`SmallSI.wip`）。これで「枠 junk に base 一様（形は任意）を要求する」が
  書けるようになり、`msr` では原理的に不可能だった要求が `hU _` の 1 語になった。
  **下方閉性（`APdL (n+1) ks V → APdL n ks V`）は後から証明できない。**負の位置の
  せいで帰納が回らないので、**定義の第 1 連言に入れる**:
  `| (n+1), ks, V => APdL n ks V ∧ (match ks with …)`。
  これが要ると分かったのは書いてみて段数がずれたときで、紙では気づけなかった。
  **副産物: 壁が消えると、それを回避するために持ち回っていた引数も消える。**
  2026-09-05 に 3 回起きた（`GCtx` の `Prop` 引数 `t`、`AYdT` の `TopOk Z`、
  `JsOk ks js b` の base と level が落ちて `JsOkL n js` の 2 引数に）。
  **余分な引数が付いている定義は、まだ本当の形になっていない**という兆候として使える。

- **循環の見分け方。** 「A の条件は B の元の上で要る」「B は A を含む集合を
  定義してから作る」なら循環。壊す方法は (a) rank や層で層別して構造的にする、
  (b) 条件を ∃ で潰した集合の上に取る、(c) 条件を「自分の接頭辞」に限る。
  どれも効かないときは、その形はこの手法の外（本体プロジェクトの残核）。

- **一様性は base の側で述べる。**（2026-09-05）定義の中の「この junk はどの形でも
  継げる」という閉包を、`replicate m 0 ++ ks`（結論と同じずらした添字）の上で書くと
  降下で詰まる。指数の恒等式
  `replicate j 0 ++ (0 :: (replicate m 0 ++ ks)) = replicate (j+1+m) 0 ++ ks`
  が示すとおり、それは「0 が m+1 個以上の添字だけ」を言っており、
  降下のたびに新しい `m'` が入ると必要な添字は下に伸びるので届かない。
  向きはこう:
  - ずらした添字上の一様性 → base 上の一様性 … **出ない**
  - base 上の一様性 → `rep_norm` で吸収 → ずらした添字上の一様性 … **出る**
  だから閉包は必ず base `ks` の上で述べ、`replicate m 0` は文脈（仮定 `hc`）に持たせる。
- **「error N 個・全部機械的」は 1 回の測定では分からない。**（2026-09-05）
  定義を強めて `leanman check` を 1 回走らせ、壊れた箇所を数える手は速いが、
  **直していくと新しい obligation が出る**ことがある。実例: `fone` 枠に荷閉包を足す変更は
  初回測定で error 20（うち 17 が機械的）に見えたが、直すと 19 → 17 → 12 に収束し、
  残った 3 個が鎖（`GOK_chainJd`）の中で循環していた。
  **原因は `.mp` 側（枠を当てる側）の負担が鎖の不変量に伝播すること。**
  次に定義を強めるときは、初回測定に加えて **`.mp` 側の消費者を先に数える**こと。
  それでも 1 回の測定は有効な足切りである（20 箇所が 200 箇所でないことは分かる）。
  「機械的」と判定してよいのは、**直し終えて緑になったとき**だけ。
  **見分ける勘所: 本物は `.mp` 側（条件を供給する側）に出る。**2026-09-05 に 2 回とも
  そうだった（`fone` 枠に荷閉包を足したときの鎖の循環、ガード 5 箇所除去で残った 1 個）。
  `⟨…⟩` の項数・射影・束縛の数は使う側の話で機械的。`unsolved goals` / `no goals` が
  出たら供給側を疑って中を見る。

- **定義を強めるときは、その定義を消費する補題の仮定も同じだけ強める。1 セットである。**
  定義だけ強めて消費側を弱いまま置くと、必ずそこで型が合わなくなる。要求は呼び出し側へ
  上がっていくが、終端は「∀ 添字」を出す最上位の定理（ここでは `APd_all`）なので鎖は止まる。
  実際、閉包強化で最後に残った不整合は 1 箇所だけで、`APd_all` 自身が任意の形を出すので
  `trivial` で埋まった。


## 4. 壁の記録（何が壁で、どう消えたか）

- **「2 番目の 2 の真横の 3」**（`(2 3 2 3)`）: 壁だと思っていたが、複製が同じ層に
  積まれるだけで、junk 条件を「`RunG (Lay i)` の全 rank」で書けば通った。
- **「3 の上に 3」**: 単位の頂上が 2 から 2 段上（`(2 3 3)`）で、複製が層を登る。
  吊るしの不変量を「∃ 台座」に取り、2 の junk を普遍にした集合 `Pk3U` を作ると
  吊るしが回り、それを階層 `Lk k` にすると `(2 3^n)` 全部が出て行 295 になった。
- **「3 の上に 4」「4 の上に 4」**（行 295 の次、D_5、∀ v の D_v）: 新しい 4 の
  row1 親は頂上の 4 を飛ばして下の 3 になり、複製は (3 J3)(4 J4)^k の全部で、
  複製の 3 は「4 が頂上の段」の上に来る。3 の junk 条件を「Stk C 2 の上」で書く
  設計（層 G C）では、junk 付きの 3 の複製が次の段に入らず閉じない。
  解: junk の条件を **「任意の台座の上」** にする。`PU y` = { 任意の `IfcV (y+1)`
  台座の元 ++ (y+1 の記録) ++ 普遍 junk }。台座を ∃ で消すので、複製がどの段の
  上に来ても入る。これは `PkGA` の「∃E Iface E」と同じ手で、値 y で一様にすると
  `Dv (k+1) ∈ PU (k+2)` が k の帰納法で出て ∀ v の D_v が塔なしで出た。
  塔は「値 v の snoc」（`IfcV_snoc`）の中に閉じ込めてある。
- **行 296 の先（z=1 の列）**: 壁はなかった。§7 を見よ。

- **「この道具では書けない」は「証明できない」ではない**（行 375 の先、2026-09-05）:
  「2 の記録の直上に 2 の記録」は junk 木の言語 `JkJ` が禁じている。ここから
  「木で書けない ⟹ 証明できない」と 2 回続けて早合点した。1 回目は「添字の
  どんな整礎測度でも減らない」と書いたが、`Finsupp.Colex`（多重集合順序
  ＝ 順序数 ⊕ω^{k_j}）で減少が取れた。2 回目は「#3 以降は全部この壁」と書いたが、
  **既に建てた部分を木ではなく土台 A の側に吸収する**と壁を踏まずに通った
  （`rowJ_mem_gen : Aok A → A ++ ((1,1,0) :: wordJ 1 1 ws) ∈ W 0`、
  そこから `R375_mem_gen : Aok A → A ++ U375a ∈ W 0`）。
  **手順**: 壁だと思ったら、(a) 具体例を 1 本 Lean で試す、
  (b) 詰まっている部分を土台 `A` の側に移せないか見る、(c) 「無理」の根拠を
  Lean か bms で確かめる。この 3 つの前に「原理的に不可能」と書かない。

## 5. 観察（役に立った事実）

- **1 はリセット。** 行 1 が 1 の列はどの高さでも `RunA` の頭になれる。
  1 を含む語は既存の道具で通る。
- **junk 自体は最初からあった。** 壁は junk ではなく「junk の条件をどの集合の
  上で言うか」。生まれる場所と複製される場所が同じ集合なら通る。
- **平坦な複製（(x,0,0) の列）は登らない。** 各段は「junk 付きのブロック」で
  読めるので、`oper_flat` + 普遍 junk の帰納法で通る。
- **順序数は目安。** 行 294 → 295 は `psi_1(W_3)` → `psi_1(W_4)` で、
  「2 で折り返す複製」が初めて要る場所。行 293 が長く関門だったのと同じ種類の
  段差が 1 段上で起きていた。
- **junk の条件は「どの集合の上で言うか」が全て。** 生まれる場所と複製される
  場所が違うときは、条件を「任意の台座（∃ で消す）の上」にすると閉じる。
  exact な集合の上の条件は close（吸収）に、普遍な条件は複製に効く。両方
  要るときは、集合自体を「∃ 台座」で定義する（`PkGA`, `P3U`, `PU y`）。
- **値で一様な界面。** 2 の snoc は `Iface`、3 は `Ifc3`、4 は `Ifc4` と
  1 つずつ作ったあとで、`IfcV v`（`IfcV (v+1) F = IfcV v F ∧ 置き直し可能性`）に
  まとめると ∀ v が出た。個別に 2〜3 個作ってから一般化する（§1 の 3〜4 と同じ）。
- **結局は 2 行の整礎性 + 吊るし。** 持ち上げた列 (x+1, y+1, 0) のうち y ≥ 1 は
  2 行と同じ展開、y = 0（持ち上げると 1 の列）だけが行列全体を吊るす。
  この手法の全部は後者の整備である。

## 7. 行 296 の先: z=1 の列と、台座の使い回し（行 297〜316）

行 296 `R296 = (0,0,0)(1,1,1)(1,1,0)(2,2,1)` の先は、R296 の上に同じ構造が
繰り返す。移送定理（e: (a,b,c) ↦ (a+1,b+1,c) で W 0 を運ぶ）を作ろうとすると、
最後の列の row1 が 0 のとき（(k,0,0) が (k+1,1,0) になり bad root が根に変わる）
に本体プロジェクトの残核と同じ形になる。作らずに済んだ理由:

- **底の集合は「Aok な行列はレベル 0 の台座」**（`BaseOk_zero`）。Q に特別な
  資格はなく、R296 も Aok なので同じ資格の台座。単位 (h+1,1,0)(h+2,2,1) は
  任意の台座の上で使えるセグメント（`SegA_unit11`）なので R296 ∈ RunA 0 1 で、
  既存の階層が丸ごと乗る。行 306 `R296(2,2,0)` は `RunG_snoc2` の 1 行。
- **z=1 の列 (a+1,b+1,1) が (a,b,0) の直上にあると、展開は対角**
  (a,b,0)(a+1,b+1,0)(a+2,b+2,0)… になる（bad root = (a,b,0)、d0 = d1 = 1、
  junk がなければ上昇のマスクは自明）。対角は任意の台座の上で W 0
  （`DiaV`, `PU_DiaV`: 記録の値が 1 ずつ上がる列は `PU y` の連鎖）。
  だから z=1 の列は **記録の普遍 junk** として扱える（`JkGU_z1`, `JkU_z1`）。
  行 312〜315 は「記録 + z=1 の junk + 次の記録」を積むだけ。
- **行 316 `(0,0,0)(1,1,1)(1,1,1)`** は Q の複製が (1,1) ずつ上がる塔で、
  各段は上の 2 つの繰り返し。
- **梯子の段としての単位**: 梯子の元 A（段 r、レベル h）の上に
  (h+1,1,0)(h+2,2,1) を置くと段 r+1、レベル h+1 の梯子の元（`LvB_unit11`）。
  条件は「どの梯子の元の上でも対角」で済む。Y_n = R296 (2,1,0)(3,2,1)…(n,1,0)(n+1,2,1)
  が段 n の梯子の元で、行 300〜305 の台座。
- **1 の列 (d,1,0) は「レベル d-1 の吊るし」があれば継げる**（`snocd_gen`）。
  塔は行列全体の複製で、段 n+1 = Y ++ (段 n を高さ d に吊るしたもの)。

教訓: 移送定理（大きい一般化）を作る前に、**既存の集合が新しい台座をそのまま
受け入れないか**を見る。台座の資格が「Aok」だけなら、繰り返し構造は新しい
定理なしで乗る。

## 8. 行 317〜336: junk の語と、影の W 帰納法

- 記録の直上の junk は「語」になる: z = (c+2,y+2,1) と、任意の高さ e ≥ 2 の平坦な列 (c+e,0,0)。
  最後が z なら展開は記録からの複製（z は (1,1)、平坦な列は (1,0) 上がる。le1 のマスクは
  「row1 = 0 の列は row1 の親を持たない」で決まる）。最後が平坦なら、最も近い低い列からの
  区間の平坦な複製。
- **影の W 帰納法**: 語の z を (1,0,0) に置き換えた平坦行列（影）は 2 行の定理で W 0。
  「影がこれなら語は普遍」を A2'（W の最小性）で示す。最後が z の場合の帰納法の仮定は
  影⟦1⟧ = 影.dropLast、最後が平坦の場合は影⟦n⟧、n = 0 の段は n = 1 の段の接頭辞（W_take）。
  順序数を陽に書かずに済むのが利点。先に作った K と個数の二重帰納法（Good_blocks）は不要になった。
- **平坦な列の壁**（行 337 以降、§9 で解消）: 平坦な列を頂上に持つ台座（Q(2,0,0)）の上に階層を
  建てようとすると、展開が項を複製したりブロックを小さくしたりして、影ともブロックの多重集合とも
  辞書式に噛み合わない（notes 続き49）。読み替え（荷つきの字の語）で台座を建てずに済んだ。

## 9. 行 337〜345: 荷（payload）つきの字と、壁の言い換え

- **壁だと思った形**: 行 337 の塔 `Q(2,0,0)(3,1,0)(4,2,0)...` を「平坦な列 (2,0,0) を頂上に
  持つ台座の上の対角」と読むと、台座の階層（`Iface` の rebase）が必要になり、
  「平坦な森に W ブロックを吊るした項の列」の帰納法が要る（順序が噛み合わない、notes 続き49）。
- **言い換え**: 同じ行列を `Q ++ Q↑2 = (0,0,0) :: ((1,1,1) :: Q↑2)` と読む。すなわち
  **字 zY := (a+1,b+1,1) :: Y↑(a+2)（z の列に荷 Y を載せたもの）** の語。平坦な列 (2,0,0) は
  台座ではなく荷の根になる。台座はもとの `LwA` / `RunG` / `IfcV` のままで、階層を建てる必要がない。
- 語 `ws = [Y1,...,Yk]`（荷はすべて `Bok`）の普遍性 `GoodP` は §8 と同じ形（4 段: `PU` / `PkGA` /
  `SegA` / 根）で、影の W 帰納法で出る。影は `(0,0,0) :: Σ((1,0,0) :: Yi↑2)`。
  影が W 0 に入ることは、和 `A ++ B`（`B` の根が高さ 0）が W 0 に閉じること（`bump_mem3` の
  bump を外した写し）から出る。
- 影の展開が語の展開を正確に写す（3 つの場合）:
  - 最後が裸の z → 影は末尾の (1,0,0) を落とす（n = 1）。語は対角の塔（§7 の z=1 の列）。
  - 最後の荷の末尾が (0,0,0) → 影も語も「最後の字が n 個に複製」。
  - 最後の荷の末尾が非零 → 荷だけが Y[n] に縮む（展開が `bump` / シフトをすり抜ける）。
- z=1 の列の展開の**マスク付き一般形**が要る: 間の列は高さ ≥ a+1 なら何でもよく、行 1 が上がるのは
  「行 1 の親鎖でバッドルートに繋がる列」だけ。荷の列は繋がらない（荷の根 (a+2,0,0) の行 1 が 0 で
  鎖が止まる）ので、荷は上がらず z の列だけが上がる。
- 使い方は 2 つだけだった: 根（`Q ++ B↑2 ∈ W 0`、任意の `Bok B`）と、
  段（`A(h+1,1,0)(h+2,2,1) ++ B↑(h+3) ∈ W 0`、`A` は梯子の元）。この 2 本から
  337〜345 が全部出る（残りは `snocd_gen` と既存の `RunG_snoc2` / `LwA_unit11`）。
- **教訓**: 「台座の上に何を建てるか」で詰まったら、同じ行列を「字の語」として読み替えられないか
  試す。台座を高くするのは高くつくが、語の字を重くするのは既存の枠のままでできることがある。

## 10. 行 346〜360: 台座を作り直す型と、junk の族

- **型**: 新しい行が出るたびに、その行の末尾数列を「どの梯子の元の上でも通るセグメント」に
  一般化しておく。すると、その行自身が新しい台座（走りの底の元、レベル 1）になり、
  既に作った段（1 の列、単位、`(·,2,0)` の記録、対角、z の列）がそのまま全部乗る。
  実際 344 と 351 をセグメント化しただけで 346〜348, 352〜356 が出た。
- **junk の族 `GoodFb`**: 記録 `(a,b,0)` の右に付ける junk を、行列ではなく
  「(a,b) を受け取って列の並びを返す族 J」として抽象化し、3 段（`PU` / `PkGA` / `SegA`）で
  普遍であることを 1 つの構造にまとめた。族が決まれば、記録と junk の対角の塔 `Dzf J` の
  連鎖補題（4 本）が自動で付いてくる。個別の junk ごとに同じ 100 行を書き直さずに済む。
- **junk の字**: 記録の右に付く列は、実際には少数の型しかない。
  z の列 `(a+1,b+1,1)`、1 の列 `(a+2,1,0)`、荷 `Y↑(a+2)`（`Y` は `Bok`）。
  語（字の列）として扱うと、展開の場合分けがその語の中で閉じる。
- **マスクの見分け方**: 記録の上の z の列を展開すると、記録の子孫だけが行 1 で上がる。
  行 1 の親鎖は行 1 を狭義に増やすので、行 1 の値が記録以下の列（1 の列は行 1 = 1）は上がらない。
  この 1 つの補題で「どの列が上がるか」が決まる。
- **残った壁（行 361 以降）**: 語の最後に 1 の列を足すには「語 + 任意の `Bok` の荷」の普遍性が要る。
  展開の場合分けは字の集合で閉じるのに、測度が閉じない: 荷の複製は 1 の列の個数を増やし、
  1 の列の処理は荷を任意に増やす。多重集合順序も辞書式も効かない。
  影（記録↦(0,0,0), z↦(1,1,1), 1 の列↦(2,1,0), 荷↦Y↑2）を取ると、junk `[z, 1]` の影は
  `(0,0,0)(1,1,1)(2,1,0)` そのもの。「影が梯子の元なら junk は普遍」という反映定理が次の一手。

## 11. 行 361〜369: 複合字（z + 1 の列 + 荷）と、整礎性の作り方

- **壁**: 記録の右の junk に「1 の列」を足すには、その手前までの junk に任意の `Bok` ブロックを
  高さ `a+2` で吊るせること（hang）が要る。`snocd_gen` が要求する塔は台座の複製を含み、
  その複製を吊るす操作が hang そのものなので、迂回できない。
- **答え**: 1 の列を「語の字」として独立に扱うのをやめ、**1 つの字を
  「z の列 ＋ 1 の列 `k` 本 ＋ 荷 `Y`」の複合**にする。すると hang が字の中に閉じる:
  「1 の列を 1 本減らして、その分を荷にする」= 字 `(k+1, [])` の hang は字 `(k, B)` になる。
- **整礎性**: 多重集合順序（Dershowitz–Manna）を型クラスで持ち込まなくてよい。
  字を継ぐ操作 `S(k,Y)` を **`k` の強帰納法 ＋ 内側で荷 `Y` の W 帰納法（A2'）** で示せば、
  展開の 4 つの場合がちょうど閉じる:
  - 荷の末尾が非零 → 荷が `Y[n]` に（内側の帰納法）
  - 荷の末尾が `(0,0,0)` → 字が `n` 個に複製（内側の帰納法を `n` 回）
  - 荷が空で 1 の列が `k+1` 本 → `snocd_gen`、hang は字 `(k, B)`（外側の帰納法、`B` は任意）
  - 荷が空で 1 の列が 0 本 → 対角の連鎖（語が 1 字短くなる）
  「複製で 1 の列の本数が増える」「hang で荷が任意に増える」ので、本数だけ・荷だけの
  辞書式は効かない。複合字にまとめると、増える方（複製）は荷が縮み、減る方（hang）は
  本数が減る、と役割が分かれる。
- **1 段上の壁**（行 370 以降）: junk の中に高さ `a+3` の 1 の列（＝ z の上の梯子）が出る。
  複合字の 1 の列は全部 `a+2` なので届かない。ここから先は「z の上にもう 1 段の階層」を
  作る再帰的な文法が要る（記録の右 = z ＋ 次の段の梯子、を入れ子にする）。

## 5.5 木の字（`Jk1`）: z の上にもう 1 段の階層を作る

行 370 で見えた壁「複合字の 1 の列は全部高さ `a+2` なので、`a+3` の 1 の列に届かない」
に対する答えが、**字の junk を木にする**こと。

```
inductive Jk1 | nil | pay : Jk1 → TrioSeq → Jk1 | one : Jk1 → Jk1 → Jk1
jk1 l nil = []
jk1 l (pay N Y) = jk1 l N ++ Y↑(l+1)          -- 荷（高さ l+1）
jk1 l (one N M) = jk1 l N ++ ((l+1,1,0) :: jk1 (l+1) M)  -- 1 の列とその junk
```
字は `colJ a b N = (a+1,b+1,1) :: jk1 (a+1) N`、語は字の並び。
複合字 `colC a b (k,Y)` は「1 の列が全部兄弟」の特別な場合なので、これは真の一般化。

### 技法 A: z の展開の質量計算は「上がる列」だけ分かればよい
`oper_z1_mask` は junk `C` について一般（高さ `a+1` 以上なら何でもよい）。
族ごとに要るのは「どの列が行 1 で上がるか」だけ。木の場合、上がるのは z の列だけ:
- 1 の列は行 1 = 1 ≤ b なので、記録（行 1 = b）の行 1 の子孫になれない（鎖は行 1 を増やす）
- 荷は根の行 1 = 0 なので `block_root` + `le1_lower_bound` で鎖が入れない
これを木の構造帰納法で 1 本にまとめたのが `not_le1_jk1`。

### 技法 B: 荷の追加は「深さ 0」と「深さ ≥ 1」で別物
荷の末尾が `(0,0,0)` のとき、複製されるのは
- 深さ 0（字の直下）なら **字そのもの**。語 `ws` は定理の引数なので、複製は
  `GoodFb_rep` 型の反復で自由に作れる（測度に効かない）。
- 深さ ≥ 1 なら **1 つ外側の項**（1 の列 + その junk）。この複製の連鎖には
  「その junk を、任意の木の右に項として継げる」という仮定が要る。
この非対称性のせいで、荷の追加は AY0 と AYs の 2 本に分かれる。

### 技法 C: 深い位置の祖先条件
`snocd_gen` は `Ancd d Y`（高さ `d` 未満の可視な列は行 1 ≥ 1）を要求する。
木の荷の根は行 1 = 0 なので、そのままでは通らない。効くのは
**「高さ `l+1` 以上の塊の直後に高さ `l+1` 以下の列を置くと、塊の列は不可視」**
（`Ancd_append_low`）。木では各段の junk の直後に必ずその段の 1 の列が来るので、
文脈に沿って `Ancd_plug_jk1` で組み上げられる。

### 残っている壁
`APt nil ctx`（深さ ≥ 1 で junk のない 1 の列を継ぐ）だけが、蓄積した木 V について
「V を任意の木の右に項として継げる」を ∀V で要求する。V は複製の連鎖で
際限なく大きくなるので、木の大きさでも深さでも測度が回らない。詳細は notes 続き72。

## 5.6 深さを命題の添字にする（`APd k`）── 入れ子の壁の破り方

木の字（5.5）で「1 の列の上に 1 の列」を何段でも積むには、
「木 `V` を項として継げる」を**深さで添字づけ**するのが鍵だった。

```
APd 0 V     := ∀ U, GOK U → GOK (one U V)
APd (k+1) V := ∀ U, APd k U → APd k (one U V)
```

素朴に「∀ 蓄積した木」で書くと、木の構造帰納法で
- `one N M` の場合は**深さが 1 増える**（部分項だが深い）
- `nil` の場合は**深さが 1 減る代わりに任意の木**を要求する

となって、順序数 1 本の測度が作れない。深さを添字にすると:
- `one N M`: 部分項 M について**深さ k+1** の帰納法の仮定を使う。
  命題を `∀ k` で述べておけばそのまま取れる ✓
- `nil`: `APd (k+1) nil` は `APd_oneNil k`（同じ k の `APd_pay k` だけを使う）に
  帰着し、**任意の木を要求しない** ✓

つまり「深さの増減」を測度から**添字に移す**と矛盾が消える。
`APd k V ↔ ∀ ctx（長さ k+1 の良い文脈）, GOK (plug ctx V)` という言い換え
（`APd_iff`）を用意しておくと、文脈一般の補題（`APnil_gen`,
`GoodFb_snoc_innerJs/dupJs`）がそのまま使える。

もう一つの要点は、荷の A2' 帰納法（`AYd`）で**複製の連鎖に沿って仮定を持ち回る**こと。
`flat_mem''` の複製で作られる木 `V ⊕ (1の列 + 荷)ⁱ` は、
`APd k` を保つ（`APd k V'_{i+1}` が `APd k V'_i` と帰納法の仮定から出る）ので、
連鎖の各段で必要な仮定が自動的に揃う。

## 5.7 2 の記録を木に入れる（`two`）── 述語を「置ける場所」で割る

行 373〜374 で必要になったのは、木の junk の中に **2 の記録** `(l+1,2,0)` を置くこと。
構成子は `one` と同じ形にする:
```
two N M     jk1 l (two N M) = jk1 l N ++ ((l+1,2,0) :: jk1 (l+1) M)
```

### 置ける場所と置けない場所
2 の記録が上がるかどうかは `nextrel1` の**最小性**で決まる:
```
nextrel1 j0 j1 の最小性: ∀ j, j0 < j ∧ le0 j j1 → 行1(j1) ≤ 行1(j)
```
- **字の z の直上（先頭段）に置くと上がる**。le0 の親が z `(a+1,b+1,1)` で行 1 = b+1 ≥ 2
  なので最小性が通り、記録 `(a,b,0)` の行 1 の子になってしまう
- **1 の列の右なら上がらない**。le0 の祖先をさかのぼると行 1 = 1 の 1 の列で止まり、
  そこで `2 ≤ 1` になって最小性が破れる
- **2 の記録の直上に 2 の記録が来ても上がらない**。さらに左の 1 の列で止まるから

つまり必要なのは「**左のどこかに 1 の列があるか**」だけ。これを補題にする:
```
Grd M p q0 l = ∃ r v, p < r < q0 ∧ 行1(r) = 1 ∧ 行0(r) = v ∧ v ≤ l ∧
                      (∀ j, r < j < q0 → v+1 ≤ 行0(j))
not_le1_two : 1 ≤ 行1(p) → (Grd の r) → 行1(q) = 2 → ¬ le1 M p q
```

### 述語を割る
「置ける場所」が違うので、木の妥当性の述語を **1 つでなく 2 つ**にする:
```
JkJ  T   -- 1 の列の上（junk レベル）。two 可
JkOk T   -- 字の先頭段。two 不可
JkOk (one N M) = JkOk N ∧ JkJ M      ← 左兄弟は同じレベル、junk は 1 段下
```
`WOk`（語の字）は `JkOk` のままにすると、**質量計算（`rise_wordJ` など）を
一行も変えずに済む**。これが分割の勘所。

`not_le1_jk1` の仮定も選言にする:
```
JkOk N ∨ (JkJ N ∧ Grd M p q0 l)
```
`one N M'` の場合は、右（M'）に降りるとき 1 の列の位置を `r` として `Or.inr` を作る。

### 2 の記録を項として継ぐ
塔は `snocY_mem`（内部アンカー）で、その塔は**階段の木** `stairJ`:
```
Mtw Y0 [(l+1,1,0)] n = Y0 ++ jk1 l (stairJ n)
```
`stairJ` は `nil` と `one` だけでできているので、`APd`（深さ）の全ての段で使える。
だから `APd_two2 k`（2 の記録はどの深さでも継げる）が循環せずに出る。

## 5.8 「全深さ」が要るとき ── `APd` の限界

行 375（`R344 (4,2,0)(5,2,0)`）は `snocYd_mem`（歩幅 `dl = 2`）で出るが、
その塔には「2 の記録が自分の junk を持つ」木が要る。さらにその中で
**2 の記録が同じ高さに n 個並ぶ**（`bms -d` で確認）。

ここで `APd k`（深さ `k` の添字）の限界に当たる:
- 「木 `W` を**任意の深さ**で継げる」は、**定理**なら `∀ k` で書けるので取れる
  （`APd_all : ∀ T, JkJ T → ∀ k, APd k T`。部分項なら帰納法の仮定として使える）
- しかし `APd` の**定義の中**で ∀ 束縛した木については取れない。
  `APd` は添字（深さ）の構造帰納で定義するので、`APd ks' W` の `ks'` が任意だと壊れる

よって「2 の記録の左兄弟 `W`」を `APd` の定義に入れてはいけない。正しい形は
文脈の**意味的な良さ**を仮定として持ち回ること:
```
AllD T  := ∀ k, APd k T                          -- 部分項なら帰納法の仮定
PlugG D := ∀ T, (T が D に埋められる形) → AllD T → GOK (plug D T)
```
`PlugG` は `APd` を使うだけなので循環しない。フレーム（1 の列 / 1 の列 + 2 の記録）を
1 つ継ぐ補題を `PlugG D → AllD U → PlugG (D ++ [frame])` の形で作り、
2 の記録のフレームの塔では `AllD N` を使う。

**教訓**: 「深さ」を命題の添字にする（5.6）のは強力だが、
**添字にできるのは有限の深さまで**。塔が「同じ木を任意の深さで使う」形になったら、
文脈の良さを仮定に上げる（`PlugG`）方に切り替える。

## 6. 進め方の型（1 本の行列を通すときのチェックリスト）

1. `bms -s` で標準形か確認。`bms -d` で [1], [2] の展開を見る。
2. 展開の bad root と delta から塔の形を決める（`snocY_mem` 歩幅 1、
   `snocYd_mem` 歩幅 dl、`oper_flat` 平坦）。
3. 塔の各段を、既存の集合（`RunA`, `RunG E`, `PkG E`, `Pk3 E`, `Lay n`,
   `Lk k`, `StkF B y k`, `PU y`）の元として読めるか試す。読み方（どの列が階段で
   どれが junk か）を複数試す。
4. 読めなければ、足りないのは (a) 新しい列の条件の普遍性か、(b) 集合の
   吊るし可能性か、(c) 層をまたぐ複製か、を特定して notes.md に書く。
5. 新しい集合を作るときは `BaseOk` → `Iface` → 段の補題 → snoc の補題の順。
   既存の証明（`BaseOk_RunA`, `Pk23_hang`, `Lk_tower`）をほぼ写せる。
6. 緑になったら commit。notes.md に「何が通り、何が壁か」を 1 行ずつ足す。
