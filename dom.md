# dom 関数の意味

出典: P進大好きbot「拡張Buchholz OCF に伴う順序数表記」(2018-06-09)
`~/proofs/papers/pbot/2018-P進大好きbot_拡張Buchholz OCFに伴う順序数表記 _ 巨大数研究 Wiki _ Fandom.html`

拡張 Buchholz OCF = $`\psi_a(b)`$ の添字 $`a`$ に自身の再帰（$`a`$ も項）を許した版。
その順序数表記 $`(\mathrm{OT}, \lt )`$ に付随する基本列系が $`\mathrm{dom}`$ と $`X[Y]`$ で与えられる。

## 記法の読み替え

| 原文 | 読み替え |
|---|---|
| `()` | $`0`$ |
| `<X1,X2>` | $`\psi_{X_1}(X_2)`$ （$`= D_{X_1} X_2`$） |
| `(X1,...,Xm)` | $`X_1 + \cdots + X_m`$ （降順の自然和、$`m \ge 2`$） |
| `<(),()>` | $`\psi_0(0) = 1`$ |
| `(1,...,1)` | $`n`$ （$`1`$ を $`n`$ 個並べた和） |
| `<(),1>` | $`\psi_0(1) = \omega`$ |

また $`\psi_v(0) = \Omega_v`$（$`v \gt 0`$）である。

## 一言でいうと

**$`\mathrm{dom}(X)`$ は $`X`$ の共終度 (cofinality) を、順序数ではなく項として計算する関数。**

名前の由来もそのままで、**$`\mathrm{dom}(X)`$ = 基本列写像 $`Y \mapsto X[Y]`$ の定義域**。
つまり $`Y`$ の動く範囲が $`\{Y \mid Y \lt \mathrm{dom}(X)\}`$。

$`\mathrm{dom}(X)`$ の値は必ず次の 4 種のどれかである。

| $`\mathrm{dom}(X)`$ | 意味 | $`X[Y]`$ の役割 |
|---|---|---|
| $`0`$ | $`X = 0`$ | なし（$`X[Y] = 0`$） |
| $`1`$ | $`X`$ は後続順序数 | $`X[Y]`$ = 前者（$`Y`$ は無視される） |
| $`\omega`$ | 共終度 $`\omega`$ の極限 | $`X[n]`$ が普通の（自然数添字の）基本列 |
| $`\Omega_Z = \psi_Z(0)`$ | 正則非可算基数 | $`X[Y]`$ は $`Y \lt \Omega_Z`$ で添字づけられた共終列 |

FGH の定義がこの 4 分類をそのまま使う:

- $`\mathrm{dom}(X) = 0`$ なら $`f_X(n) = n+1`$
- $`\mathrm{dom}(X) = 1`$ なら $`f_X(n) = f_{X[0]}^n(n)`$
- それ以外なら $`f_X(n) = f_{X[n]}(n)`$

## 再帰の流れ

### $`X = 0`$

$`\mathrm{dom}(X) = 0`$、$`X[Y] = 0`$。

### $`X = \psi_{X_1}(X_2)`$

#### (a) $`\mathrm{dom}(X_2) = 0`$（つまり $`X_2 = 0`$）→ 添字 $`X_1`$ に再帰

$`X = \psi_{X_1}(0) = \Omega_{X_1}`$ の場合。ここが拡張 Buchholz（= 添字が項）で初めて出てくる分岐。

**(a-1) $`\mathrm{dom}(X_1) = 0`$**（$`X_1 = 0`$）: $`X`$ は後続。
$`\mathrm{dom}(X) = X`$、$`X[Y] = 0`$。

> 例: $`X = \psi_0(0) = 1`$。$`\mathrm{dom}(X) = 1`$、$`X[Y] = 0`$。
> この $`X`$ 自身が「後続」を表す標識 $`1`$ になる。

**(a-2) $`\mathrm{dom}(X_1) = 1`$**（$`X_1`$ が後続）: $`X = \Omega_{X_1}`$ は**正則基数**。
$`\mathrm{dom}(X) = X`$ 自身（正則 = 自分が自分の共終度）、$`X[Y] = Y`$。

> 例 1: $`X = \psi_1(0) = \Omega_1`$。$`\mathrm{dom}(X) = \Omega_1`$、$`X[Y] = Y`$。
> 基本列が恒等写像なのは「$`\Omega_1`$ 未満の任意の $`Y`$ がそのまま共終列の項」という意味。
> 同様に $`X = \psi_2(0) = \Omega_2`$ でも $`\mathrm{dom}(X) = \Omega_2`$、$`X[Y] = Y`$。
>
> 例 2（添字が和）: $`X = \psi_{\omega + 1}(0) = \Omega_{\omega + 1}`$。
> $`X_1 = \omega + 1`$ は和なので $`\mathrm{dom}(X_1) = \mathrm{dom}(1) = 1`$、つまり後続。
> よって $`\mathrm{dom}(X) = \Omega_{\omega+1}`$、$`X[Y] = Y`$。
> 添字が自然数でなくても、**末尾の加数が $`1`$ なら正則**。
> ちなみに $`\Omega_\omega`$ は特異（(a-3) 例 1）で、その次の $`\Omega_{\omega+1}`$ は正則、という
> 通常の基数論そのままの挙動になっている。
>
> 例 3（添字が非可算・拡張 Buchholz 固有）: $`X = \psi_{\Omega_1 + 1}(0) = \Omega_{\Omega_1 + 1}`$。
> $`X_1 = \Omega_1 + 1`$ も末尾が $`1`$ なので $`\mathrm{dom}(X_1) = 1`$、
> よって $`\mathrm{dom}(X) = \Omega_{\Omega_1+1}`$、$`X[Y] = Y`$。
> (a-3) 例 2 の $`\Omega_{\Omega_1}`$（添字が極限 ⟹ 特異、$`\mathrm{dom} = \Omega_1`$）と対になる。
> **添字の $`\mathrm{dom}`$ を 1 段計算しないと正則か特異かが決まらない**のが
> PSS（添字が自然数、常に正則）との決定的な違い。

**(a-3) $`\mathrm{dom}(X_1) \notin \{0, 1\}`$**（$`X_1`$ が極限）: $`X = \Omega_{X_1}`$ は**特異基数**。
共終度は添字経由で流れる: $`\mathrm{dom}(X) = \mathrm{dom}(X_1)`$、$`X[Y] = \psi_{X_1[Y]}(0)`$。

> 例 1: $`X = \psi_\omega(0) = \Omega_\omega`$。$`X_1 = \omega`$ で $`\mathrm{dom}(\omega) = \omega`$ だから
> $`\mathrm{dom}(X) = \omega`$、$`X[n] = \psi_{\omega[n]}(0) = \psi_n(0) = \Omega_n`$。
> すなわち $`\Omega_\omega = \sup_n \Omega_n`$、共終度 $`\omega`$。
>
> 例 2（拡張 Buchholz 固有）: $`X = \psi_{\Omega_1}(0) = \Omega_{\Omega_1}`$。
> $`X_1 = \Omega_1`$ は (a-2) より $`\mathrm{dom}(X_1) = \Omega_1`$ なので
> $`\mathrm{dom}(X) = \Omega_1`$、$`X[Y] = \psi_{Y}(0) = \Omega_Y`$（$`Y \lt \Omega_1`$）。
> **$`\mathrm{dom}`$ が $`\omega`$ でも「$`X`$ 自身」でもない非可算値を返す**のはこの分岐だけ。

#### (b) $`\mathrm{dom}(X_2) = 1`$（$`X_2`$ が後続）

$`\psi_a(b+1) = \sup_k \bigl(\psi_a(b) \cdot k\bigr)`$ なので $`\mathrm{dom}(X) = \omega`$。

- $`Y = 1`$ なら $`X[Y] = \psi_{X_1}(X_2[0])`$
- $`Y = k`$（$`2 \le k \lt \infty`$）なら $`X[Y] = \underbrace{\psi_{X_1}(X_2[0]) + \cdots + \psi_{X_1}(X_2[0])}_{k}`$
- それ以外（$`Y = 0`$ など）なら $`X[Y] = 0`$

> 例 1: $`X = \psi_0(1) = \omega`$。$`X_2 = 1`$ で $`1[0] = 0`$ だから
> $`X[k] = \underbrace{\psi_0(0) + \cdots + \psi_0(0)}_{k} = k`$。すなわち $`\omega = \sup_k k`$。
>
> 例 2: $`X = \psi_0(2) = \omega^2`$。$`2[0] = 1`$ だから
> $`X[k] = \underbrace{\psi_0(1) + \cdots + \psi_0(1)}_{k} = \omega \cdot k`$。
>
> 例 3（非可算側でも同じ）: $`X = \psi_1(1) = \omega^{\Omega_1 + 1}`$。
> $`X[k] = \underbrace{\psi_1(0) + \cdots + \psi_1(0)}_{k} = \Omega_1 \cdot k`$。
> $`X_1`$ が大きくても $`X_2`$ が後続なら共終度は $`\omega`$。

#### (c) $`\mathrm{dom}(X_2) = \omega`$

$`X_2`$ の共終度がそのまま $`X`$ の共終度になる:
$`\mathrm{dom}(X) = \mathrm{dom}(X_2) = \omega`$、$`X[Y] = \psi_{X_1}(X_2[Y])`$。

> 例: $`X = \psi_0(\omega) = \psi_0(\psi_0(1)) = \omega^\omega`$。
> $`\mathrm{dom}(X_2) = \mathrm{dom}(\omega) = \omega`$ だから
> $`X[n] = \psi_0(\omega[n]) = \psi_0(n) = \omega^n`$。
> 引数の基本列を $`\psi`$ の中にそのまま送り込むだけ。

#### (d) $`\mathrm{dom}(X_2) = \Omega_Z`$（非可算）→ 崩壊の分岐点

$`\mathrm{dom}(X_2) = \psi_Z(0)`$ と置く。

**(d-1) $`\Omega_Z \lt X`$ のとき**: この $`\psi`$ はその基数を潰していない。
$`X_2`$ の共終度がそのまま $`X`$ の共終度になる:
$`\mathrm{dom}(X) = \mathrm{dom}(X_2) = \Omega_Z`$、$`X[Y] = \psi_{X_1}(X_2[Y])`$。

> 例: $`X = \psi_2(\Omega_1)`$。$`\mathrm{dom}(X_2) = \Omega_1`$ で、
> $`X \ge \Omega_2 \gt \Omega_1`$ だからこちらの枝。
> $`\mathrm{dom}(X) = \Omega_1`$、$`X[Y] = \psi_2(\Omega_1[Y]) = \psi_2(Y)`$（$`Y \lt \Omega_1`$）。
> $`\psi_2(\Omega_1) = \omega^{\Omega_2 + \Omega_1} = \sup_{Y \lt \Omega_1} \omega^{\Omega_2 + Y}`$ で確かに共終度 $`\Omega_1`$。

**(d-2) そうでない（$`\Omega_Z \ge X`$）とき**: この $`\psi_{X_1}`$ が $`\Omega_Z`$ を潰している。
$`X_2`$ の共終度 $`\Omega_Z`$ は $`X`$ に伝わらず、$`\mathrm{dom}(X) = \omega`$ となる。
基本列は $`\psi_{X_1}`$ を入れ子にした**塔**になり、$`Y`$ によってさらに 2 つに分かれる。

以下、次の 2 つを共通の題材にする（どちらも $`X_1 = 0`$、$`\Omega_v[Y] = Y`$（(a-2)）を使う）。

- **題材 A**: $`X = \psi_0(\Omega_1) = \varepsilon_0`$。
  $`\mathrm{dom}(X_2) = \Omega_1 = \psi_1(0)`$ なので $`Z = 1`$、$`Z[0] = 0`$。
  $`\varepsilon_0 \lt \Omega_1`$ だから (d-2)。
- **題材 B**: $`X = \psi_0(\Omega_2)`$。
  $`\mathrm{dom}(X_2) = \Omega_2 = \psi_2(0)`$ なので $`Z = 2`$、$`Z[0] = 1`$。
  $`X \lt \Omega_2`$ だから (d-2)。

**(d-2-1) $`Y = h`$（$`1 \le h \lt \infty`$）かつ $`X[Y[0]] = \psi_{X_1}(\Gamma)`$ と書ける $`\Gamma`$ があるとき**:
塔を一段上げる。

```math
X[Y] = \psi_{X_1}\bigl(X_2[\psi_{Z[0]}(\Gamma)]\bigr)
```

前段 $`X[h-1]`$ の中身 $`\Gamma`$ を取り出し、$`\psi_{Z[0]}`$ をかぶせて次段の引数に埋め戻す。

> 例（題材 A、$`Y = 1`$）: $`Y[0] = 0`$ で、後述のとおり $`X[0] = \psi_0(1)`$ だから $`\Gamma = 1`$。
> $`X[1] = \psi_0\bigl(\Omega_1[\psi_0(1)]\bigr) = \psi_0(\psi_0(1)) = \omega^\omega`$。
> $`Y = 2`$ なら $`\Gamma = \psi_0(1)`$ で $`X[2] = \psi_0(\psi_0(\psi_0(1)))`$。
>
> 例（題材 B、$`Y = 1`$）: $`X[0] = \psi_0(\Omega_1)`$ だから $`\Gamma = \Omega_1`$。
> $`Z[0] = 1`$ なのでかぶせるのは $`\psi_1`$ であり、
> $`X[1] = \psi_0\bigl(\Omega_2[\psi_1(\Omega_1)]\bigr) = \psi_0(\psi_1(\Omega_1))`$。
> $`Y = 2`$ なら $`\Gamma = \psi_1(\Omega_1)`$ で $`X[2] = \psi_0(\psi_1(\psi_1(\Omega_1)))`$。

**(d-2-2) そうでないとき**（$`Y = 0`$、または $`Y`$ が自然数でないとき）: 塔の底。

```math
X[Y] = \psi_{X_1}\bigl(X_2[\psi_{Z[0]}(0)]\bigr)
```

(d-2-1) の $`\Gamma`$ を $`0`$ で代用した形。

> 例（題材 A、$`Y = 0`$）: $`Z[0] = 0`$ なので
> $`X[0] = \psi_0\bigl(\Omega_1[\psi_0(0)]\bigr) = \psi_0(\Omega_1[1]) = \psi_0(1) = \omega`$。
>
> 例（題材 B、$`Y = 0`$）: $`Z[0] = 1`$ なので
> $`X[0] = \psi_0\bigl(\Omega_2[\psi_1(0)]\bigr) = \psi_0(\Omega_2[\Omega_1]) = \psi_0(\Omega_1) = \varepsilon_0`$。

なお (d-2) の枝では $`X[Y]`$ はどの $`Y`$ に対しても $`\psi_{X_1}(\cdots)`$ の形になるので、
(d-2-1) の条件の後半（$`X[Y[0]] = \psi_{X_1}(\Gamma)`$ と書ける）は
$`Y`$ が自然数である限り常に成り立つ。実質的な分かれ目は $`Y = 0`$ か $`Y \ge 1`$ かだけで、
**(d-2-2) が塔の底、(d-2-1) が塔の一段上げ**、という役割分担になっている。

まとめると、題材 A は

```math
X[0] = \psi_0(1) = \omega, \quad
X[1] = \psi_0(\psi_0(1)) = \omega^{\omega}, \quad
X[2] = \psi_0(\psi_0(\psi_0(1))) = \omega^{\omega^{\omega}}, \ \ldots
```

で $`\varepsilon_0 = \sup_n X[n]`$、題材 B は

```math
X[0] = \psi_0(\Omega_1) = \varepsilon_0, \quad
X[1] = \psi_0(\psi_1(\Omega_1)), \quad
X[2] = \psi_0(\psi_1(\psi_1(\Omega_1))), \ \ldots
```

で $`\psi_0(\Omega_2) = \sup_n X[n]`$ となる。
どちらも $`\mathrm{dom}(X_2)`$ は非可算だが $`\mathrm{dom}(X) = \omega`$ であり、
$`n`$ が数えているのは共終列の添字ではなく $`\psi`$ の入れ子の深さになっている。

### $`X = X_1 + \cdots + X_m`$（$`m \ge 2`$）

$`\mathrm{dom}(X) = \mathrm{dom}(X_m)`$（最後の項の共終度）。$`X[Y]`$ は $`X_m[Y]`$ で末尾を差し替え:

- $`X_m[Y] = 0`$ かつ $`m = 2`$ なら $`X[Y] = X_1`$
- $`X_m[Y] = 0`$ かつ $`m \gt 2`$ なら $`X[Y] = X_1 + \cdots + X_{m-1}`$
- $`X_m[Y]`$ が主要項なら $`X[Y] = X_1 + \cdots + X_{m-1} + X_m[Y]`$
- $`X_m[Y] = Z_1 + \cdots + Z_{m'}`$ なら $`X[Y] = X_1 + \cdots + X_{m-1} + Z_1 + \cdots + Z_{m'}`$

## lean-yapss / trio との対応

### PSS（2 行 = lean-yapss）

PSS では添字が自然数なので、分岐 (a) は $`\mathrm{dom}(X_1) \in \{0, 1\}`$ しか起きない。
（$`\Omega_{n+1}`$ は常に正則で、特異基数 (a-3) が出てこない。）

- lean-yapss `Wset.lean` の `domT M m`
  （末尾の対が row-1 値 $`m+1`$ を持ち row-1 親を持たない = row-1 孤児）
  = **$`\mathrm{dom}(\mathrm{translate}\,M) = \Omega_{m+1}`$**、すなわち (a-2) の正則ケース。
- 同 `natDom M`（`entry M 1 (len-1) = 0` または row-1 親を持つ）
  = **$`\mathrm{dom}`$ が可算**（$`0`$ / $`1`$ / $`\omega`$ のどれか）。
- 同 `graft M z`（末尾の $`\Omega_{m+1}`$ 葉を森 $`z`$ で置換し、その葉の row-0 深さに再ベース）
  = 非可算 $`\mathrm{dom}`$ を持つ項の **$`X[Y]`$**（$`Y`$ を基数未満で走らせる）の PSS 版。
  $`W_u`$ の反復帰納的定義はこの graft の上に乗っている。

### trio（3 行 = $`z \lt 2`$ 制限 TSS）

trio では添字 $`a`$ 自身が項になるので、PSS では起こり得なかった分岐が現れる。

- **(a-3)**（共終度が添字経由で流れる、特異基数 $`\Omega_{\text{極限}}`$）が新規に発生。
- **(d) の判定 $`\Omega_Z \lt X`$ か否か**（添字同士の比較で崩壊するかが決まる）が本質的になる。
  PSS では添字が自然数なのでこの比較が自明だった。

Bachmann 共終性（$`N \lt M`$ ならある $`n`$ で $`N \le M[n]`$）を PSS から移植するとき、
場合分けの骨格はこの $`\mathrm{dom}`$ の再帰そのものになるはず。

## BM4 展開分岐との対応表（計測済み）

計測: `tools/probe_dom.py`。pbot の $`T`$ / $`\lt `$ / $`\mathrm{dom}`$ / $`X[Y]`$ を忠実移植し
（ブログの計算例 $`\psi_0(\Omega_1)[2] = \psi_0(\psi_0(\omega))`$ 等で回帰テスト）、
trio.py の BFS 到達標準形 4115 個（種: $`z`$ 頭打ち・$`z`$ 全零対角列、$`n \le 3`$、深さ 8）を分類した。

BM4 の分岐は末尾列 $`(x, y, z)`$ と $`t = \max\{y \mid S_{X-1,y} \gt 0\}`$、
親 $`r = P_t(X-1)`$ で決まる。

| BM4 分岐 | 件数 | $`\delta`$ | dom 終端ケース | 意味 |
|---|---|---|---|---|
| 末尾列全零 (P0) | 29 | — | (a-1) 経由で $`\mathrm{dom} = 1`$ | 後続。末尾を落とすだけ |
| $`t = 0`$ (T0) | 835 | $`(0,0,0)`$ | **(b)** | 引数が後続。同一コピーを $`n`$ 個並べる |
| $`t = 1`$・親あり (T1) | 2480 | $`(d_0,0,0)`$, $`d_0\gt 0`$ | **(d-2)** | 崩壊塔。行 0 上昇 = $`\psi`$ の入れ子 1 段 |
| $`t = 2`$・親あり (T2) | 771 | $`(d_0,d_1,0)`$, $`d_0,d_1\gt 0`$ | **(a-3)** 経由 | 添字が極限。行 1 上昇 = 添字の基本列 |
| $`t \ge 1`$・親なし (ORPH) | **0** | — | (a-2) / 非可算 | 正則基数の葉。**標準形の末尾には現れない** |

検証結果:

- **2 行断片（$`z`$ 全零、1798 形）で全数一致**。$`z`$ 全零の標準形は Buchholz 記法
  （添字 $`= \underline{y}`$）への翻訳が既知なので、BM4 分岐と $`\mathrm{dom}`$ 終端ケースを
  直接照合できる: P0 ↔ (a-1)、T0 ↔ (b)、T1 ↔ (d-2)。不一致 0。
- **(d-2) の発火ノード = バッドルート**: 940/940。$`\mathrm{dom}`$ 再帰が (d-2) を発火させる
  $`\psi`$ ノードの添字 $`= \underline{y_r}`$（バッドルートの行 1 値）、潰される基数
  $`= \Omega_{y_x}`$（末尾列の行 1 値）。**$`\mathrm{dom}`$ の再帰は $`P_t`$ の親探索そのもの**:
  (c) / (d-1) / 和の素通し = 祖先鎖の climb、(d-2) の発火 = バッドルートの発見。
- **孤児は標準形の末尾に現れない**（4115 形で 0 件）。trio.py の「バッドルート無し → Pred」
  分岐は保険であって、生きた標準形では踏まれない。(a-2)（正則基数、$`\mathrm{dom}(X) = X`$）が
  現れるのは **W 階層の graft 対象ブロック**（先頭 $`y \gt 0`$ の切り出し）としてのみ:
  - $`B_1 = [(0,1,0)]`$（$`= \Omega_1`$ の葉）→ ORPH1、$`\mathrm{dom}`$ 終端 (a-2)。一致。
  - $`B_2 = [(0,2,0),(1,1,0)]`$（$`= \psi_2(\Omega_1)`$: 祖先はいるが全部 $`y \ge 1`$）→ 同上。
  - $`B_3 = [(0,1,1)]`$（$`z = 1`$ で行 2 親なし = **添字レベルの孤児**）→ ORPH2。
    これが Wstar2（行 2 接ぎ木）の graft 対象。
- **T2 の構造**: $`\delta_0 \gt 0`$ かつ $`\delta_1 \gt 0`$ が全件で成立（$`P_2`$ は $`P_1`$ 鎖を
  登るので行 1 値は必ず真に減る）。コピーごとに行 1 が上昇する =
  $`\Omega_{X_1}[Y] = \psi_{X_1[Y]}(0)`$ の添字基本列を BM4 が行 1 で歩いている。
  例: $`D_1 = (0,0,0)(1,1,1)`$ は $`\psi_0(\psi_\omega(0))`$ で、domchain は
  (c) → (a-3) → (b)。$`D_1[n]`$ = 2 行対角列（添字上昇）。
- $`y = 0, z = 1`$ の末尾列は 0 件（$`z \le y`$ が保たれる）。
- **psi(I) 行列** $`(0,0,0)(1,1,1)(2,1,1)(3,1,0)(2,0,0)`$ は末尾列 $`(2,0,0)`$ なので
  分岐 T0 = ケース (b): 同一ブロックの $`n`$ 個並べで
  $`\psi(\Omega_\Omega), \psi(\Omega_{\Omega_\Omega}), \ldots`$ の塔が出る。
  eB の極限 $`\psi_0(\Lambda)`$（$`\Lambda`$ = 最小 $`\Omega`$ 不動点）が
  z<2 断片では**内部の 1 標準形**として現れる（= z<2 断片は eB を真に超える）。

まとめの対応図:

| $`\mathrm{dom}`$ 再帰の部品 | BM4 側の対応物 |
|---|---|
| 和 → 最終加数、(c)/(d-1) の素通し | $`P_t`$ の祖先鎖 climb（最右深部の葉へ降りる） |
| (d-2) の発火（$`\Omega_Z \ge X`$） | バッドルート発見（$`y_r \lt y_x`$ の最近祖先） |
| (d-2) の塔（$`\Gamma`$ 埋め戻し） | T1: コピーの行 0 一様シフト（入れ子が 1 段深くなる） |
| (b) の $`k`$ 個並べ | T0: $`\delta = 0`$ の同一コピー |
| (a-3) の添字基本列 $`X_1[Y]`$ | T2: コピーの行 1 上昇（$`\delta_1 \gt 0`$） |
| (a-2) の $`X[Y] = Y`$（基数添字） | graft（W 階層）。展開規則の外 |
| (a-1) / $`\mathrm{dom} = 1`$ | 末尾列全零 → drop |

## 実例: $`\psi_0(\Omega_{\omega+1})`$ — 番地が創発することの教訓

**訂正の記録**: 本節の初版は $`\psi_0(\Omega_{\omega+1}) = (0,0,0)(1,1,1)(2,2,0)`$ と誤同定していた
（v0.118.196）。BM4-Analysis シートが正:

| 行列 | シートのラベル | eB 読み |
|---|---|---|
| $`(0,0,0)(1,1,1)`$ | psi(W_w) | $`\psi_0(\Omega_\omega)`$ |
| $`(0,0,0)(1,1,1)(2,1,0)`$ | psi(W_w*W) | $`\psi_0(\Omega_\omega \cdot \Omega)`$ |
| $`(0,0,0)(1,1,1)(2,1,0)(3,1,0)`$ | psi(W_w^W) | $`\psi_0(\Omega_\omega^{\Omega})`$ |
| $`(0,0,0)(1,1,1)(2,1,0)(3,2,0)`$ | **psi(W_(w+1))** | $`\psi_0(\Omega_{\omega+1})`$ |
| $`(0,0,0)(1,1,1)(2,1,1)`$ | psi(W_(w^2)) | $`\psi_0(\Omega_{\omega^2})`$ |
| $`(0,0,0)(1,1,1)(2,1,1)(3,1,0)`$ | psi(W_W) | $`\psi_0(\Omega_{\Omega})`$ |
| $`(0,0,0)(1,1,1)(2,1,1)(3,1,0)(2,0,0)`$ | psi(I) | $`\psi_0(\Lambda)`$ = eB の極限 |

（`tools/probe_dom.py` §5 がシートとの照合を自動化している。）

### 正しい方の観察

$`\psi_0(\Omega_{\omega+1}) = (0,0,0)(1,1,1)(2,1,0)(3,2,0)`$ は分岐 **T1**
（末尾 $`(3,2,0)`$ の行 1 親 = 列 2 の $`(2,1,0)`$）、ケース **(d-2)**。

- BM4 の基本列: $`(2,1,0)`$ に $`(k,1,0)`$ を継ぎ足す =
  シートのラベルで $`\psi(\Omega_\omega^{\Omega}), \psi(\Omega_\omega^{\Omega_\omega^{\Omega}}), \ldots`$ の指数塔。
- eB の基本列: $`X[n] = \psi_0(\psi_\omega(\cdots\psi_\omega(0)))`$（$`\psi_\omega`$ の反復）。
- **両者は点ごとには一致しない**。同じ順序数への別の共終列である。
  「分岐 ↔ dom ケース」(T1 ↔ (d-2)) は保たれるが、基本列そのものは系ごとに違ってよい。

発火位置のズレは正しい同定でも残る: eB の (d-2) は $`\psi_0`$（列 0）で発火するが、
BM4 のバッドルートは列 2 の $`(2,1,0)`$ — 番地エンコードの内部の列である。
2 行断片では番地が文字通り（添字 = $`y`$ 値）なので両者が一致した（940/940）。
番地が超限になると、**BM4 は番地を列として実体化しその中からバッドルートを選ぶ**のに対し、
**eB は番地を添字の中に持ち、(d-2) の $`\psi_{Z[0]}`$ で層を再生成する**。

### 誤同定の解剖 — 何を踏んだか

1. **番地は創発する**（lean-yapss セッションの設計論そのまま）。列 $`(y,z)`$ を見て
   「$`z=0`$ なら $`\psi_y`$、$`(1,1)`$ なら $`\psi_\omega`$」と**ノード局所で**読んだのが誤り。
   同じ形の列でも、行 1 祖先鎖のどの番地の上に立つかで意味が変わる:
   $`(2,1,0)`$ は $`(1,1,1)`$ の後では $`\cdot\Omega`$ の材料になり、
   $`(2,1,1)`$ は番地を $`\omega`$ から $`\omega^2`$ へ引き伸ばす。
2. **(d-2) の塔は自己相似**。「$`S[n+1]`$ と $`X[n]`$ の形が 1 列 = 1 層で一致する」ことは
   dom **ケース**の一致しか意味しない。同じ形の塔が $`\psi_0(\Omega_{\omega+1})`$ にも
   はるか先にも現れる。**形の一致から順序数の値を同定してはいけない**。
   値の正解表は BM4-Analysis シートだけ。
3. 誤同定した $`(0,0,0)(1,1,1)(2,2,0)`$ の実際の位置: その塔は $`(k,1,1)`$ 列の連鎖で、
   1 列ごとに番地再帰そのものが 1 段深くなる。列辞書式順序で
   $`S[3] \gt`$ psi(I) 行列（= eB の極限 $`\psi_0(\Lambda)`$）、
   $`S[4] \gt`$ シート最終行列 psi(C(1,0;0))*w（弱コンパクト圏）。
   すなわち **2021 年解析の全範囲（psi(K) 主脈込み）の先**にあり、eB では項として書けない。
   末尾 $`(2,2,0)`$ は「レベル 1 の $`z=1`$ 番地再帰全体の極限の、その次の正則基数」を閉じる列である。

## 次の一手（案）

対応表はできた（上節）。残るのは (a-2)/(a-3) 系（graft = W 階層）の共終性が
trio の W-membership 残核（WCONVEX / SNOC 系）にどう効くかの読み替え。
