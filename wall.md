# 壁

シート行376 `(0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(5,3,0)`。

## 壁の最小形（実測）

    hang6_R375m : ∀ B, Bok B → R375m ++ shiftr01 6 0 B ∈ W 0
    ⟺ GOK (one nil (two nil (two nil (pay nil B))))     （= GOK (bdAC B [2])）

`R375m = (0,0,0)(1,1,1)(2,1,0)(1,1,0)(2,2,1)(3,1,0)(4,2,0)(5,2,0)` は
`(4,2,0)(5,2,0)` という**長さ 2 の走り**で終わる。
**走りの上に荷を吊るす**のが壁。1 の列の上（`hang6_R375s` / `hang6_R375t`）も、
走りの上に荷なし（`GOK (bdA [2])`）も緑。

## 行376 への還元（全部緑）

    R376_of_stk        : (∀q, GOK (one nil (stk q))) → 行376
    R376_of_FoneB      : FoneB → 行376              FoneB ⟺ ∀js, GOK (bdA (js ++ [0]))
    R376_of_HangB      : HangB → 行376              行列の言葉（族を使わない）
    R376_of_WFd_nilAll : (∀ks, WFd ks nil) → 行376

## 族 `WFd`（融合族）でどこまで来たか

緑:

    WFd_nilF'   幅 ≥ 1 の入り目の空木（`i+1 ≤ k`）
    WFd_chainF  平らな鎖 `twoIt N (pay Z Y') m` を走りの兄弟に（予算固定で回る）
    WFd_payF    幅 ≥ 1 の入り目の荷（`PayShp k i ks Z` だけ要る）
    WFd_oneNilF 幅 ≥ 1 の入り目の `one U nil`（枠の予算下げだけ要る）
    WPd_FLr     平らな荷の鎖は**枠の位置**なら緑

残り 1 点:

    WFd ((1,2) :: p) nil        （予算 1・幅 2 の入り目の空木）

`PayShp k 1 [] nil = ∀k₂ (1 ≤ k₂ ≤ k), WFd ((k₂,2)::p) nil` のうち、
`WFd_nilF'` は `2 ≤ k₂` でしか出ない。`k₂ = 1` だけが空く。

## なぜ 1 ずれるか（3 通り測った）

| 設計 | 階段が要る予算 | 鎖が要る予算 | 結果 |
|---|---|---|---|
| `WFd`（予算 k、兄弟 `j ≤ k₂ ≤ k`） | 塔は `k-1` | `k₂ = i` で空木が要る | `(i,i+1)` で割れる |
| 兄弟を `j+1 ≤ k₂` に | 塔は `i+1 ≤ k-1` | `k₂ = i+1` | `(i+1,i+1)` に移るだけ |
| 入り目 (幅 w, 束縛 b) | 塔は `b-1` | `b' = i` で幅 `i+1 > b'` | 同型 |

**階段は束縛（予算）を 1 下げ、鎖は幅を 1 上げる。**この向きの差は
パラメータを増やしても消えない。詳細は notes 追記282・283・284。

## 死んだ道

- `WGd`（`Ins`/`InsT` の相対化）: 空虚（`WGd_zero_bad`）。
- 枠を `nil` に固定: 階段の塔の枠が最後の兄弟になるので成立しない（追記281-4）。
- `FrLow`（枠の予算下げ）: `WFd` は予算について単調でないので出ない。
- `WPd` で平らな荷の鎖を走りに: `WPd_twoA_runB` が鎖 1 本ごとに予算を 1 消費し、
  展開が作る鎖の長さは無界なので追いつかない。

## 次

行列側（`HangB`）を族を経由せずに攻める。道具は `W0_acc`（荷の W 順序の整礎性、緑）。
