# R2 の測定ノート（`CoreCap` の展開の形）

担当: R2（測定）。作業ディレクトリ `/home/koteitan/proofs/dbms`（branch `dbms`）。
プログラム: `tools/dbms/r89.py`（分布）/ `r89b.py`（形の検算）/ `r89c.py`（木の不変量）/
`r89d.py`（`b,c` 依存）。見積もり: `r89est.py` / `r89est2.py`。

---

## §R89 `CoreCap` の展開は 3 分岐しかない —— そのどれもが `WCat` 形ではない

### 結論（先に）

**`CoreCap` の展開に `A ++ B`（＝独立な 2 つの `W` 元の連結、`WCat` 形）は現れない。**
現れるのは次の 3 つだけで、**3 つとも Lean に無条件の等式がある**:

| 分岐 | 条件 | 展開の形 | Lean の等式 | 状態 |
|---|---|---|---|---|
| **P3** `noparent` | `¬ hasParent M ...` | `S⟦n⟧ = S.dropLast` ＝ `CtxOK` の接頭辞 `k=|M|-1` そのもの | `oper_eq_pred_of_noParent` | **無条件・証明ずみ** |
| **P1** `j0 >= 1` | `hasParent R ...` | `S⟦n⟧ = (0,v,z) :: R⟦n⟧`（**根が cons のまま残る**） | `oper_cons_nat`（`Wset.lean:2041`） | **無条件・証明ずみ** |
| **P2** `j0 = 0` | `domT R m` ＋ `hasParent M ...` | 根つき塔 `tow v z R n` / `Lift1` 付きの塔 | `oper_cons_tower1`（`:2789`, `srow=1`）／ `oper_cons_tower2`（`:3231`, `srow=2`） | **無条件・証明ずみ** |

⟹ **`CoreCap` が当たる壁は `WCat` ではなく `TowerOK`。**
§131 の鎖（塔 ⟹ `rsum` が逆向きに破れる ⟹ `WCat` が要る）は **`WSnoc` 路線の話**であって、
`CoreCap` には移らない。理由は算術で書ける:

    WSnoc の塔    shTower Q e (n+1) = shTower Q e n ++ shiftr01 (n*e) 0 Q   ← **連結**。rsum が要る
    CoreCap の塔  tow v z R (k+1)   = (0,v,z) :: graft R (tow v z R k)      ← **graft**。rsum は要らない

`graft` は「末尾の孤児を差し替える」ので `W_add` を通らない。⟹ §131 の障害は発生しない。

### (a) バッドルート `j0` はどこか

母集団: 列アルファベット row0∈{1,2,3} × row1∈{0,1,2} × row2∈{0,1}（18 列）、
`|M| ∈ {1,2,3}`（6174 文脈）、`v∈{0,1,2}`, `z∈{0,1}`, `t∈{0,1,2}`,
cap `b∈{0,1,2,3}`, `c∈{0,1,2}`。**全数 1,333,584 件**（サンプリングなし）。

    branch/noparent   600,210  (45.0%)   → Pred。CtxOK の接頭辞が直接くれる
    branch/copy       733,374  (55.0%)
      j0 = 0          413,694  (31.0% / copy の 56.4%)   → **根つき塔**
      j0 >= 1         319,680  (24.0% / copy の 43.6%)   → **cons 保存**

**`j0 = 0` は起きる。しかも `|M| = 1` に限らない**:

    |M|=1  j0=0 :   1,944   (100%. **j1=1 なので親は 0 しかない ＝ 構造的に強制**)
    |M|=2  j0=0 :  25,704   j0=1 :  11,664
    |M|=3  j0=0 : 386,046   j0=1 :  98,064   j0=2 : 209,952

⚠ **`j0` は `n` に依存しない**（`Trio.lean:98` の `oper` の定義に `j0 = parent M i1 j1` が
`n` の外にある）。⟹ `n` を振る必要がない。

⚠ 「`j0` が cap 列になる」は**定義上ありえない**。コピー塊は `List.range' j0 (j1 - j0)`
なので **cap 列 `j1` は塊に入らない**（消える）。

### (b) コピーされる塊 `S[j0:j1]`

    root-included      413,694   j0=0。塊は根 `(0,v+t,z)` を含む
    M-all(dropLast)    109,728   j0=1。塊 = `M.dropLast` 全体
    M-proper-suffix    209,952   j0>=2。塊 = `M.dropLast` の真の接尾辞

`j0 >= 1` のとき土台 `S.take j0 = Lift1 ((0,v,z) :: M.take (j0-1)) t` は
**`CtxOK` が `k = j0-1` でそのまま供給する**。

### (c) `j0` は `b, c` に依存する —— **∀b∀c は見かけだけではない**

111,132 個の `(M,v,z,t)` について `(b,c)` の 12 通りを振った:

    c1 分岐 (noparent / copy) が変わる      111,132 / 111,132  = **100%**
    c2 j0 の値が変わる                       18,072 / 111,132  = 16.3%
    c3 **二分 (j0=0 か j0>=1 か) が反転**     16,920 / 111,132  = **15.2%**
    c4 j0>=1 のとき j0 = 行 0 の親 p1        314,928 / 319,680  = 98.5%

⟹ **`b, c` を消して場合分けを軽くすることはできない。**
ただし `j0 >= 1` の枝では `j0` はほぼ行 0 の親（98.5%）で、`b,c` は
「行 1・行 2 の鎖が根まで登るか」だけに効いている。

`j0 = 0` の 15% は**行 0 の親が根でない**のに行 1・行 2 の鎖が根まで登る場合:

    j0=0 かつ p1=0   351,864  (85.0%)
    j0=0 かつ p1>=1   61,830  (15.0%)

### (d) 増分 `d0` / `d1`

    d0=0, d1=0   111,132  (15.2%)   `srow=0`（cap が `b'=0` かつ `c=0`）。**塔ではなく素の反復**
    d0>0, d1=0   246,456  (33.6%)   `srow=1`
    d0>0, d1>0   375,786  (51.2%)   `srow=2`

`d0 = 0 ⟺ srow = 0` は定義から（`d0 = if 0 < i1 then ... else 0`）。
`d0=0` の塔は `shiftr01 0 0` なので**同じ塊の n 回反復**。
このとき `rsum(A,P)` は `entry P 0 0 = 0` なので**自明に成り立つ**
⟹ `W_flatMap_copies`（`Wset.lean:2552`）が効く。§131 の障害は `d0 >= 1` のときだけ。

`j0=0`（塔）のときの `(srow, z)`:

    srow=0 z=0 : 29,322   srow=0 z=1 : 29,322
    srow=1 z=0 : 69,876   srow=1 z=1 : 69,876
    srow=2 z=0 : 145,422  srow=2 z=1 : 69,876   ← **c >= 2 でのみ起きる**

⚠ `tower2_root_z_zero`（「`srow=2`, `z=1` は起きない」）は **`c <= 1` を使っている**。
`CoreCap` は `∀ c : ℕ` なので `c = 2` を入れると `srow=2 & z=1` が起きる（69,876 件）。
**`CoreCap` を証明するときは `c >= 2` の場合を別に扱う必要がある。**（H12 の H56 と整合）

---

## §R89-2 形の検算（P1 / P2 / P3）—— **破れ 0**

`|M| <= 3` の全 1,333,584 件で、次の等式を `oper` の計算結果と逐一照合した（`r89b.py`）。
**神託ゼロ。`W` の所属は一切仮定していない。**

    (P1) j0>=1  ⟹ S⟦n⟧ = S[0] :: (S.tail)⟦n⟧          319,680 / 319,680  **100%**
    (P2) j0=0   ⟹ S⟦n⟧ = concat_{k<n} Lift1 (shiftr01 (k*d0) 0 Q) (k*d1),  Q = S[0:j1]
                                                        413,694 / 413,694  **100%**
    (P3) noparent ⟹ S⟦n⟧ = S.dropLast                  600,210 / 600,210  **100%**

`n = 1,2,3,4`。⟹ **4 つ目の分岐は無い。**

---

## §R89-3 木の不変量（全ノードを降りる）

`r89c.py`。`|M| <= 2`、深さ 9、`maxlen 26`、`n ∈ {1,2,3}`、訪問ノード 1,705,886。

    I1 先頭列が入口の根 `(0,v+t,z)` のまま     1,705,886 / 1,705,886  **破れ 0**
    I2 先頭列の lev = 段 a のまま               1,705,886 / 1,705,886  **破れ 0**
    I3 尾が argOK（行 0 >= 1）                  1,609,688 / 1,705,886  **破れ 96,198（5.6%）**

**⟹ 段 `a` は木のどこでも消費されない**（I2）。「親のある列は段を食わない」が
入口だけでなく木全体で成り立っている。

⚠ **`argOK` は木の下では保たれない**（I3）。反例: `[(0,0,0),(1,0,0),(0,0,0),(1,0,0)]`。
これは `d0=0` の塔（`srow=0`）が同じ塊を反復して**深さ 0 の列が内部に複数現れる**もの。
`oper_cons_nat` は `argOK R` を要求するので、**「根を外して再帰」はここで止まる。**
ただしこの形は `entry P 0 0 = 0` なので `rsum` が成り立ち、`W_add` が使える側。

ノードの分岐の内訳（深さ 9）:

    node/copy/j0>=1   1,551,829  (91.0%)   `oper_cons_nat`（**無条件で証明ずみ**）
    node/noparent        82,519  ( 4.8%)   Pred
    node/copy/j0=0       67,777  ( 4.0%)   塔 —— うち srow=0: 7,060 / srow=1: 55,990 / srow=2: 4,727
    node/zero             3,761  ( 0.2%)

⚠ **教訓 12（予算依存）**: この内訳は**予算に依存する**。深さ 7 では
`j0=0` が 10.4%、深さ 9 では 4.0%。深いノードほど `oper_cons_nat` が優勢になるため。
**予算に依存しないのは (i) 入口の全数分布（上の (a)）と (ii) I1/I2 の破れ 0**（深さ 7・9 の両方）。

---

## §R89-4 対照（教訓 11/18）

    (A) 内蔵の陽性対照: `|M| = 1` は `j1 = 1` なので **j0=0 が構造的に強制**。
        実測 1,944/1,944 = 100%。⟹ 計器は `j0=0` を検出できる
    (B) `argOK` を外した母集団（M の内部に row0=0 を許す、86,544 (M,v,z)）:
        `j0>=1` が 319,680 → 835,488 に増え、`branch/zero` が 64,908 件新たに出た。
        ⟹ 計器は母集団の変化に反応している
    (C) **`CtxOK` フィルタは何も落とさなかった**: 37,044 個の `(M,v,z)` すべてが装備ずみ
        （確定 30,348 / 予算切れ 6,696 / **確定した非装備 0**）。
        `tools/probe_cap2.py` の `ctx/not-equipped = 0` とも一致。
        ⟹ **`CtxOK` はこのグリッドでは母集団を絞っていない。**
        「`CtxOK` があるから平坦な `M` は来ない」は**言えない**。

---

## §R89-5 team-lead の分岐判定への回答

STATUS.md の判定表:

> `j0 = 0` が起きる ⟹ コピー塊は根を含む ⟹ rsum は `0 + n*δ <= 0` で破れる
>                   ⟹ **`CoreCap` も `WCat` と同じ壁。路線を捨てる根拠**

**この含意の第 3 段が成り立たない。** `j0 = 0` は確かに起きる（31%）が、
そのとき現れるのは **`shTower`（連結の塔）ではなく `tow`（graft の塔）**である:

    tow v z R (k+1) = (0,v,z) :: graft R (tow v z R k)      (`Wset.lean:2784`)

`graft` は `M.dropLast ++ z.map(shift)` であって、`W_add` の `rsum` を通らない。
`oper_cons_tower1` / `oper_cons_tower2` は**この等式を無条件で証明ずみ**。
⟹ **`CoreCap` は `WCat` を要求しない。要求するのは `TowerOK` 相当の graft 閉性。**

**⟹ 路線を捨てる根拠にはならない。** 残る債務は 3 つ:

1. `srow = 2` の塔（`oper_cons_tower2`）—— `TowerOK2` そのもの
2. `c >= 2` の場合（`srow=2 & z=1` が起きる。`tower2_root_z_zero` が使えない）
3. `argOK` が木の下で破れる形（`d0=0` の反復。ただし `rsum` は成り立つ側）

---

## §R89-6 `argOK` が破れる起点は **`srow=0` の根つき塔だけ** —— 破れ 0 の全数照合

`r89e.py`。`|M|<=2`、深さ 9、`n∈{1,2,3}`。「親の尾が `argOK` ／ 子の尾が非 `argOK`」に
なる**遷移の起点**を分岐別に数えた。

    起点/tower/srow=0/n=2        4,238
    起点/tower/srow=0/n=3        4,238
    起点/（それ以外の分岐）           0      ← **ゼロ**

遷移の内訳（`ok->VIOL` が出るのは `srow=0` の塔だけ）:

    cons (P1)           ok->ok 1,712,994   ok->VIOL **0**
    noparent (P3)       ok->ok   131,031   ok->VIOL **0**
    tower/srow=1        ok->ok   139,218   ok->VIOL **0**
    tower/srow=2        ok->ok    11,085   ok->VIOL **0**
    tower/srow=0        ok->ok     4,208   ok->VIOL **8,476**   ← ここだけ

⟹ 定義からの算術と一致する:

    P3 は部分列                         ⟹ argOK 保存
    P1 のコピーは深さ entry R 0 j + k*d0 >= 1  ⟹ argOK 保存
    P2 のコピー k は深さ k*d0 から始まる。d0>=1 なら k>=1 で >= 1 ⟹ 保存
    **d0 = 0（srow=0）のときだけ深さ 0 の根が内部に反復して出る**

**そしてその `srow=0` の塔は Lean で既に閉じている。**
`d0=d1=0` の塔は同じ塊 `Q = (0,v,z) :: R'` の `n` 回反復であり、
`entry Q 0 0 = 0` なので `rsum_self_cons`（`Wset.lean:2539`）が側条件を無条件で満たす。
⟹ `W_flatMap_copies`（`Wset.lean:2551`）がそのまま適用できる。

**⟹ `argOK` の破れは債務ではない。`srow=0` の枝は無条件で閉じている。**

## §R89-7 陰性対照 —— 検算器は鳴る（教訓 12）

`r89ctl.py`。P1/P2/P3 の式を**わざと壊して**同じ検算器にかけた:

    P1' `S[0] :: (S.tail)⟦n+1⟧`          VIOL 11,664 / 11,664   **100% 鳴る**
    P2' `tower(Q, d0+1, d1, n)`          VIOL 27,648 / 27,648   **100% 鳴る**
    P2' `tower(Q, d0, d1+1, n)`          VIOL 27,648 / 27,648   **100% 鳴る**
    P2''`Q = S[0:j1+1]`（cap 列を塊に入れる） VIOL 27,648 / 27,648   **100% 鳴る**
    P3' `S.dropLast.dropLast`            VIOL 34,560 / 34,560   **100% 鳴る**

⟹ §R89-2 の「破れ 0」は空虚ではない。

## §R89-8 母集団を広げる（教訓 16）—— 結論は動かない、比率は動く

グリッドを 48 列に広げた（row0∈{1..4} × row1∈{0..3} × row2∈{0,1,2}、
`v∈{0..3}`, `t∈{0..3}`, cap `b∈{0..5}`, `c∈{0..3}`）。`|M|<=2` の全 1,806,336 件。

    P1/ok 317,952   P2/ok 709,824   P3/ok 778,560     **破れ 0（変わらず）**

**比率は動いた**（報告義務、教訓 11）:

    j0=0 の割合（copy 分岐の中）   狭いグリッド 56.4%  →  広いグリッド **69.1%**
    j0=0 で srow=2 かつ z=1        69,876 (|M|<=3)     →  188,544 (|M|<=2, 広)

`c` の上限を上げると `srow=2` が増えるため。⟹ **`c >= 2` の扱いは避けて通れない。**

## §R89-9 まとめ（この測定から言えること・言えないこと）

**言えること（実測、破れ 0、対照つき）**

1. `CoreCap` の展開の分岐は **3 つで尽きる**。`A ++ B`（`WCat` 形）は現れない
2. 3 分岐はそれぞれ Lean の**無条件に証明ずみの等式**に一致する
   （`oper_cons_nat` / `oper_cons_tower1,2` / `oper_eq_pred_of_noParent`）
3. 段 `a` は木のどこでも消費されない（先頭列が不変、`lev` = `a` のまま）
4. `argOK` が破れるのは `srow=0` の塔だけ、そこは `W_flatMap_copies` で閉じている
5. `CtxOK` はこのグリッドで母集団を絞っていない（確定した非装備 0）

**言えないこと**

- `CoreCap` が真であること。これは測っていない（`probe_cap2.py` は 5,048/5,184 が `ok`、
  違反 0、残り 136 が予算切れ、という別の測定）
- 木の分岐の比率。**予算に依存する**（深さ 7 で `j0=0` が 10.4%、深さ 9 で 4.0%）
- `|M| >= 4` の挙動（別途測定中）

---

## §R89-10 `|M| = 4, 5, 6` —— 結論は変わらない

`r89f.py`（`|M| ∈ {4,5,6}` 各 3000 文脈のランダム標本、各 648,000 件）:

    L=4  P1/ok 186,912  P2/ok 176,673  P3/ok 284,415   **破れ 0**
    L=5  P1/ok 200,268  P2/ok 165,498  P3/ok 282,234   **破れ 0**
    L=6  P1/ok 205,524  P2/ok 161,316  P3/ok 281,160   **破れ 0**

塔の内訳（`srow`）:

    L=4  srow=0 23,832 / srow=1 59,874 / srow=2 92,967
    L=5  srow=0 21,564 / srow=1 56,226 / srow=2 87,708
    L=6  srow=0 20,592 / srow=1 54,858 / srow=2 85,866

`|M|<=4` の**全数** 24,008,400 件（`r89_L4_nofilter.log`）:

    branch/noparent  10,567,098  (44.0%)
    branch/copy      13,441,302  (56.0%)   j0=0 6,651,990 (27.7%) / j0>=1 6,789,312 (28.3%)
    |M|=4 の j0 分布: j0=0 6,238,296 / j0=1 925,344 / j0=2 1,765,152 / j0=3 3,779,136

`j0 = 0` の割合（copy 分岐の中）は `|M|` とともに安定する:

    |M|<=3 全数 56.4%  →  |M|<=4 全数 49.5%  →  標本 L=4 48.6% / L=5 45.2% / L=6 44.0%

⟹ **`j0 = 0` は `|M|` を大きくしても消えない**（44% 前後に漸近）。

## §R89-11 予算依存の確認（教訓 12）

| 測定 | 予算 | 結論 |
|---|---|---|
| 形の等式 P1/P2/P3 | 予算なし（`oper` を直接計算） | **予算非依存**。破れ 0 |
| 入口の `j0` 分布 | 予算なし | **予算非依存**。全数 |
| 木の不変量 I1/I2 | 深さ 7 / 9 の両方 | **どちらも破れ 0** |
| 木の分岐の比率 | 深さ 7 で `j0=0` 10.4%、深さ 9 で 4.0% | **⚠ 予算依存。この数字は使わない** |
| `CtxOK` の装備 | 深さ 9 | 確定 30,348 / 予算切れ 6,696 / **確定した非装備 0** |
| `argOK` の破れの起点 | 深さ 9 | `srow=0` の塔だけ（他は `ok->VIOL` ゼロ） |

⚠ `|M|=4` に `CtxOK` フィルタをかける測定は予算 600 秒で完走しなかった（`memo` の肥大）。
`CtxOK` は `|M|<=3` で 1 件も落とさなかったので、`|M|=4` は**フィルタ無し**で測った。
「`|M|=4` でも `CtxOK` が落とさない」は**未確認**（「未定」であって「偽の証拠」ではない）。

---

## §R89-12 ⚠ 自己訂正 —— `c >= 2` は穴ではなかった（L3 の L109 と同期）

§R89 の「残る債務」に挙げた **2.（`c >= 2` の場合）は取り下げる。**

L3 の L109（commit `ab41f4e`）が `L53Subst.lean` を読み直して確定した:

    L53.tower2_root_spec    :2363   親は必ず根（`Wset.parent_cons_eq_zero` 経由）
    L53.tower2_zr           :2380   `z < entry R 2 (|R|-1)`
    L53.tower2_stage_fits'  :2406   段はいつでもちょうど収まる
    L53.towerOK2_of_clause3 :2432   上の 2 本を使う。**`c` について一般**

`tower2_root_z_zero`（`:1473`、「`srow=2, z=1` は起きない」）は
`tower2_z_zero_of_parent`（`:1486`）からしか呼ばれず、**そちらはどこからも呼ばれていない**
（R2 が `grep -rn` で確認。`lean/L105Cap.lean` のコメント 2 箇所に名前が出るだけ）。

⟹ **R2 が測った「`srow=2 & z=1` が 69,876 件起きる」は事実だが、穴ではない。**
穴だと思ったのは `tower2_root_z_zero` の docstring と `Final.lean` のコメントを
生きている鎖だと誤読したため。**教訓 20 の再発**（定理が見ない形を見て結論に使った）。

**⟹ `CoreCap` の残る債務は `TowerOK2` の 1 本だけ。**
ただし `towerOK2_of_clause3` は `(hlt : LiftTie)` を前提に持つので、
`LiftTie`（`L53Subst:2337`, 2 量化 / 3 前提、`MliftR` から出る）が相方として残る。

---

## §R92 残核 `z=1 ∧ c=1 ∧ srow=2 ∧ 親が根でない` —— 「等式」と「所属」を分けて測る

課題 R92（SESSION §139）。team-lead の指摘:

> R2 はこの枝を「無条件に閉じている」と報告しているが、それは**等式が無条件**という意味で、
> **所属が閉じる**という意味ではない。

そのとおり。`oper_cons_nat` が無条件なのは等式 `S⟦n⟧ = (0,v,z) :: R⟦n⟧` であって、
`(0,v,z) :: R⟦n⟧ ∈ W a` ではない。分けて測った（`tools/dbms/r92.py`）。

母集団: `z = 1`, `c = 1` に固定（残核の形）。列 row0∈{1,2,3}×row1∈{0,1,2}×row2∈{0,1}、
`v∈{0,1,2}`, `t∈{0,1,2}`, cap `b∈{0,1,2,3}`。`srow=2` になるものだけを残す。

### (R92-a) 分岐 —— **塔の枝には一度も落ちない**

    noparent        11,340  (92.1%)
    cons (j0>=1)       972  ( 7.9%)
    **tower (j0=0)       0**

算術と一致する: `z=1, c=1` では `nextrel2` の要求 `entry 2 j0 < c = 1` を
根（行 2 = `z` = 1）が満たさないので**根は親の候補から外れる**。残るのは
「`R` の内部に親がある（P1）」か「親が無い（P3）」の 2 つだけ。

### (R92-b) ★ **所属** —— 確定した非所属 0

    noparent      ok 11,340 / 11,340        VIOL **0**
    cons(j0>=1)   ok 270 / unknown 702      VIOL **0**

`unknown` は予算切れ（深さ 9）。「未定」であって「偽の証拠」ではない。

### (R92-c) ★ **再帰が `CoreCap` の形に留まるか**

P1 の枝の `R⟦n⟧`（`n = 1,2,3`）について:

    argOK (R⟦n⟧)              ok 2,916 / 2,916     VIOL **0**
    CtxOK (R⟦n⟧) (v+t) z      eq 2,430 / unk 486   **NOT-eq 0**

⟹ **`argOK` も `CtxOK` も P1 の枝で伝播する。**
⚠ ただしこれは「帰納法の仮定が当たる形になっている」という**必要条件**であって、
**整礎な測度があること**は測っていない（計器では測れない。Lean 側の仕事）。

### 陽性対照（教訓 12）—— 両方鳴る

    inW([(0,1,0)], a=0)                     = **False**   反証器は確定 False を返す
    inW([(0,0,1)], a=0)                     = **False**
    ctxOK([(1,3,1),(1,0,0)], v=1, z=1)      = eq
    ctxOK(同上、**段を 1 だけ小さくする**)   = **NOT-eq**   装備判定器も鳴る

⟹ (R92-b)(R92-c) の「破れ 0」は空虚ではない。

---

## §R94 ★★★ 健全な反証器は「根の段」以外を反証できない —— **これは定理**

### 主張

> `Wchar.lean` の厳密な特徴づけだけを使う反証器（`probe_cap2.py` / `h1/h57.py` /
> `r89.py` / `r92.py` が共有しているもの）が**確定 False を返す ⟺ `lev S 0 > a`**。
> それ以外の非所属は**原理的に検出できない**。

### 証明（定義からの算術。測定不要）

`oper`（`Trio.lean:98`）は **第 1 列を絶対に落とさない**:

    j1 = 0             → `M` そのもの
    末尾が全零 / 親なし  → `Pred M = M.dropLast`（`|M|>=2` なので第 1 列は残る）
    それ以外            → `M.take j0 ++ (range n).flatMap ...`
                          `j0 >= 1` なら先頭は `M[0]`
                          `j0 = 0` なら `take 0 = []` だが flatMap の `k=0, j=0` の項が
                          `(entry M 0 0 + 0*d0, entry M 1 0 + 0*d1, entry M 2 0) = M[0]`

⟹ 展開の木の**どのノードも先頭列は `S[0]`**。到達する単元は `[S[0]]` だけ。
反証器は「`lev > a` の単元に届いた」ときだけ False を返すので、
**鳴る ⟺ `lev S 0 > a`**。∎

これは `Wset.lean:2161` `lev_root_le_of_mem_W` の docstring が言っていることと同じで、
`Wtower2.lean:2990` `mem_Wself_iff`（`M ∈ W u ⟺ M ∈ Wself ∧ lev M 0 <= u`、**無条件・緑**）
が「段の情報はすべて根にある」と言っているのと同じ。
`Wself` の側は**最小不動点**なので、有限の検査では反証できない（真であることは示せる。
展開の木が長さ <= 1 に落ち切ればよい。**偽であることは示せない**）。

### 実測での裏づけ（乱択、対照込み）

    乱択 40,000 件（|S| ∈ 1..6、行 0/1 ∈ 0..4、行 2 ∈ 0..3、a ∈ 0..12、深さ 8）
      False かつ lev S0 > a   16,866
      True  かつ lev S0 <= a  21,324
      None  かつ lev S0 <= a   1,810
      **False と (lev S0 > a) の不一致   0 件**

    先頭列の保存（乱択 20,000 列 × n=1..4） **80,000 / 80,000**

### ★ 帰結（H12 の反証器スイープに直接効く）

`WCat` / `WSnoc` / `Subst1*` / `TowerOK*` / `CoreCap` はどれも

    「仮定が `X ∈ W u` を与える」 ⟹ 「結論 `Y ∈ W u`」

の形で、しかも **`lev Y 0 = lev X 0 <= u` が仮定から自動で出る**:

    `WCat`   `A ++ B` の根は `A` の根（`A` が空なら `B` の根）⟹ `lev <= u`（`lev_root_le_of_mem_W`）
    `WSnoc`  `C ++ [p]` の根は `C` の根 ⟹ 同上
    `CoreCap` 結論の根は `(0, v+t, z)`、`lev = 2(v+t)+z <= a` は**前提そのもの**

⟹ **これらの核はこの反証器では原理的に反証できない。「違反ゼロ」は空虚である。**

⚠ **教訓 13 の再訂正**: 「健全な反証器は存在する」は正しいが、**その反証器の射程は
`lev S 0 > a` だけ**。⟹ **「違反ゼロ」を根拠に核を「たぶん真」と言ってはいけない。**
反証器が鳴りうる形かどうかを、走らせる**前に**確かめること。
鳴りうるのは「結論の根の段が前提から出ない」核だけ（＝結論でリフトや置換が根を持ち上げる形）。

### 副産物: 既存 `inW` のメモにバグがある

    memo[key] = None      # 循環ガード。**深さを鍵に含めていない**
    memo[key] = out       # out が None でも**恒久的に**保存される

⟹ (1) 深さ切れの `None` が恒久化する（あとから深い予算で見ても `None` のまま）
   (2) 循環ガードの `None` が子に伝播して恒久メモされる（本当は `True` かもしれない）

R92b で **深さ 9 → 11 → 13 に上げても `unknown` が 702 のまま 1 件も動かなかった**のは
これが原因の可能性がある（`maxlen` の可能性もある。切り分けは `tools/dbms/winw.py` の
`inW2` で進行中）。⚠ **ただし `False` の判定は健全なまま**（`False` は必ず
`lev > a` の単元に届いた証拠）。「違反ゼロ」の結論は影響を受けない。
影響を受けるのは `ok` / `unknown` の内訳だけ。

---

## §R95 ★★★★ **どの核も反証器の射程外**。反証スイープは原理的に何も見つけられない

R94 の定理より、反証器が鳴る **⟺ `lev Y 0 > u`**（`Y` が結論の主語、`u` が段）。
⟹ 核が**反証されうる ⟺ 仮定から `lev Y 0 <= u` が出ないこと**。

仮定から使える必要条件は 1 本だけ（`Wset.lean:2161` `lev_root_le_of_mem_W`、無条件・緑）:

    `X ∈ W m` かつ `X ≠ []`  ⟹  `lev X 0 <= m`

そこで各核について**仮定の根の条件だけを満たす乱択データ**（＝本物の事例の**上位集合**）を作り、
結論の根の条件が破れることがあるかを数えた（`tools/dbms/r95.py`、各 200,000 件）。
上位集合で破れないなら、本物でも絶対に破れない。

| 核 | 本物 | 陽性対照（結論の段を 1 下げた偽物） | 判定 |
|---|--:|--:|---|
| `WCat`  `A ++ B` | **0** | 51,270 | **反証不能** |
| `WSnoc` `C ++ [p]` | **0** | 52,782 | **反証不能** |
| `LiftStage` `Lift1 X d` | **0** | 52,698 | **反証不能** |
| `LiftTie` `Lift1 ((0,v,z)::R) d` | **0** | 15,157 | **反証不能** |
| `MliftR` `mliftR X w d` | **0** | 18,171 | **反証不能** |
| `Row1Mono` | **0** | 38,127 | **反証不能** |
| `WConvex` | **0** | 38,087 | **反証不能** |
| `CoreCap` ＝ `CoreSingleton` ＝ `GraftAll` | **0** | 40,121 | **反証不能** |
| `TowerOK` | **0** | 40,036 | **反証不能** |

各核の算術（1 行ずつ）:

    WCat / WSnoc  結論の根は `A[0]` / `C[0]`。仮定が `lev <= u` をくれる
    LiftStage     根は錐に反射で入るので `lev` が `+2d`、段も `+2d`
    LiftTie       同上。`(0,v,z) → (0,v+d,z)` で `lev` が `+2d`、段も `+2d`
    MliftR        閾値を超えれば `+2d`、超えなければ据え置き。どちらも段以下
    Row1Mono      行 1 を下げるので `lev` は減る
    WConvex       `Le1 B C` と `C ∈ W a` から `lev B 0 <= lev C 0 <= a`
    CoreCap       結論の根は `(0,v+t,z)`、`lev = 2(v+t)+z <= a` は**前提そのもの**
    TowerOK       結論の根は `(0,v,z)`、`2v+z <= a` は前提

### ⟹ 結論（H12 と team-lead へ）

> **反証器の射程は「核の文の段の算術の誤り」だけ。**
> 段の算術は全核で正しく書けているので、**反証スイープは原理的に何も見つけられない。**
> `InfEquip` が反証できたのは、結論が `W` 所属ではなく**不等式** `entry M 2 p <= 1`
> だったから（射程外の別種の主張）。H12 の §146-4 の指摘と同じ。

⟹ **反証路線は打ち止め**（H12 の §146 の結論を、実測ではなく**定理**として確定した）。
⟹ 「違反 0」を核の状態欄の根拠に使ってはいけない。**情報を持たない。**

### 教訓（提案）

**計器を回す前に「この計器はこの主張を偽にできるか」を算術で確かめる。**
陽性対照が鳴っても、それが「計器の射程が主張を覆っている」ことにはならない。
段を 1 下げた対照は**計器の射程そのもの**を叩いているだけなので、
「本物も同じように見えている」ことの証拠にはならない。

---

## §R96 `unknown` はメモのバグではなく**原理的な不完全性**（決着）

R92b/R92c で「深さ 9 → 11 → 13 に上げても `unknown` が 702 のまま 1 件も動かない」のを見て、
既存 `inW` のメモのバグ（§R94 の副産物）を疑った。**違った。**

**メモを毎回まっさらにして**（＝メモの毒を完全に排除して）深さを上げた:

    S = ((0,0,1),(1,0,0),(2,1,1))  a=1  (v=0, b=1)
      depth= 9: None  (0.04s, memo=1,012)
      depth=11: None  (0.28s, memo=6,231)
      depth=13: None  (1.86s, memo=39,646)
      depth=15: None  (16.15s, memo=246,115)

    b=2, b=3 でも**まったく同じ**（memo サイズが 1 件も違わない）

⟹ **メモの毒ではない。展開の木が有限深さで長さ <= 1 に落ち切らない。**
節 2 が `∀ n >= 1` の**無限分岐**なので、有限の検査で `True` を出すには
すべての枝が底に着く必要があり、この形はそうならない。

⚠ 例が全部 `v = 0`, `z = 1`, `a = 1` なのは **H12 の §146 の「未判定は `v=0` / `b=1` に
完全に固まる」と同じ場所**。独立に同じ結論に着いた。

**修正版 `tools/dbms/winw.py` の `inW2` は正しいが遅い**（深い予算では部分木を
まるごと再計算するので、同じ 972 件が深さ 9 で 2 秒 → 110 秒超）。
`False` の健全性はどちらの版でも変わらない（R94 より `False` ⟺ `lev S 0 > a`）ので、
**既存の測定結果を測り直す必要はない。**

---

# R2 のまとめ（課題 R89 / R92 / R94 / R95）

| § | 内容 | 状態 |
|---|---|---|
| §R89 | `CoreCap` の展開は 3 分岐で尽き、`WCat` 形は現れない | 全数 2,400 万件 ＋ 標本 194 万件、破れ 0、陰性対照つき |
| §R89-6 | `argOK` が破れる起点は `srow=0` の塔だけ。そこは `W_flatMap_copies` で閉じている | 起点の全数照合、他の分岐は破れ 0 |
| §R89-12 | `c >= 2` は穴ではなかった（自己撤回、L3 の L109 と同期） | `grep` で死んだコードを確認 |
| §R92 | 残核 `z=1,c=1,srow=2` は塔の枝に落ちない。所属・`argOK`・`CtxOK` の伝播とも破れ 0 | 陽性対照 2 本とも鳴る |
| **§R94** | **健全な反証器は `lev S 0 > a` 以外を反証できない** | 定義からの算術 ＋ 乱択 4 万件で不一致 0 |
| **§R95** | **主要 9 核すべてが反証器の射程外。反証スイープは何も見つけられない** | 上位集合 20 万件 × 9 核で 0、対照は 1.5〜5 万件鳴る |
| §R96 | `unknown` はメモのバグではなく原理的な不完全性 | メモを毎回まっさらにして深さ 15 まで確認 |

**教訓（提案）**: **計器を回す前に「この計器はこの主張を偽にできるか」を算術で確かめる。**
陽性対照が鳴っても、それが「計器の射程が主張を覆っている」ことの証拠にはならない。
段を 1 下げた対照は**計器の射程そのもの**を叩いているだけである。

---

## §R97 タイの本数 —— **常に 1 本ではない。最大は `|R| - 1` で `|R|` とともに増える**

（team-lead は「課題 R94」と呼んだが、私の §R94 と衝突するので §R97 と改番した。）

母集団（教訓 19/20: **定理が見る形だけ**）＝ `TowerOK2` が実際に呼ばれる場面:

    argOK R / R ≠ [] / z <= 1 / domT R m / srow R (|R|-1) = 2
    / hasParent ((0,v,z) :: R) 2 |R|（根が孤児を復活させる）

列アルファベット 行0∈{1,2,3} × 行1∈{0,1,2,3} × 行2∈{0,1,2}（36 列）、`|R| <= 4`、
`v ∈ {0,1,2,3}`, `z ∈ {0,1}`。**場面は全数 1,821,258 件。**

### 検算: `L53.tower2_zr` の `z < c`

    z < c   **1,821,258 / 1,821,258（100%）**

⟹ team-lead の指示「母集団を `z < c` に絞れ」は**絞る必要がなかった**。
上の 6 条件を課せば `z < c` は**自動で出る**（`tower2_zr` の内容そのもの）。定義どおり。

### (t1) ★ タイの本数

    タイ 0 本   880,380
    タイ 1 本   726,444  (77.2% of タイ)
    タイ 2 本   196,938  (20.9% of タイ)
    タイ 3 本    17,496  ( 1.9% of タイ)

**`|R|` 別 —— 最大本数は `|R| - 1`。`|R|` とともに増える:**

    |R|=1  最大 0 本
    |R|=2  最大 1 本
    |R|=3  最大 2 本
    |R|=4  最大 3 本

⟹ **`split_lastTie` の 1 回適用では足りない。`split_lastTie_len`（長さの帰納）が要る。**

### ★★ 新しい定理（実測 0 件 ＋ 算術）: **最終列は絶対にタイにならない**

    「最終列がタイ」  **0 / 1,821,258**

理由（`parent_cons_eq_zero`（`Wset:2762`、緑）＋ `nextrel1` の定義）:
`domT R m` のもとで親は**必ず根**。`srow = 2` なので `nextrel2` が成り立つには
`nextrel1` の鎖が要り、`nextrel1` は `entry (cons) 1 0 = v < entry R 1 (|R|-1)` を要求する。
⟹ **最終列の行 1 は `v` より真に大きい ⟹ `≠ v`。**

⟹ これが「最大本数 = `|R| - 1`」の理由でもある。**L3 はこれを補題にできる**
（`split_lastTie` を最終列に当てる必要がないので、`R₂` は必ず空でない）。

### (t3) 剥がしたあと

    最後のタイの位置   末尾から 1: 357,654 / 2: 324,780 / 3: 258,444（**末尾から 0 は 0 件**）
    R1 に残るタイ      0 本 726,444 / 1 本 196,938 / 2 本 17,496

⟹ `split_lastTie` を 1 回当てると残りのタイがちょうど 1 本減る。**`k` 回で無タイ**（`k <= |R|-1`）。

### (t4) ⚠ **H11 の 4 分類は再現しない。母集団が違う**（教訓 11）

    H11（`Final.lean:145-155`、70,557 件）  狭義 88.5% / 無タイ 2.8% / タイ 8.7%
    R2（この測定、1,821,258 件）            狭義 33.0% / 無タイ 15.3% / **タイ 51.7%**

**どちらが `TowerOK2` の「実際の場面」かは未定。** 違いの候補:

    H11 は `Wstar` の適用点（シート由来の実際の行列）から母集団を作っている
    R2 は列アルファベットからの一様全数（構成的）

⟹ **`v` の分布が違う**のが主因と思われる（`Wstar` の `v` は全称なので、
構成的母集団では `v` が小さい値に集中し、タイが出やすい）。
**この差は報告のみ。どちらかを「正しい」と決めない。**
ただし **(t1) の「最大本数 = `|R|-1`」と「最終列は絶対にタイでない」は
母集団に依らない**（後者は算術で出る）。

### ⟹ L3 への含意

1. **「タイは常に 1 本」は成り立たない。** `split_lastTie` の 1 回適用は不十分
2. `split_lastTie_len`（長さの帰納）が必要。回数は `<= |R| - 1`
3. **最終列は絶対にタイでない**（算術で出る）⟹ `split_lastTie` の `R₂` は**必ず空でない**。
   これを補題にすれば帰納が 1 段軽くなる可能性がある
