# project
- トリオ数列（3 行バシク行列, BM4）の停止性の**構文的**証明。
- 一旦 z < 2 の断片を対象とする。生成元 = z 頭打ち対角列
  D_v = (0,0,0)(1,1,1)(2,2,1)...(v,v,1)（完全な BM4 の (0,0,0)(1,1,1)(2,2,2)[v] に等しい）。
  展開は行 2 を変えないので z < 2 は閉じている。
  BM4-Analysis-2021.4.27.xlsx の 3 行行列 20537 個のうち z≥2 は 19 個のみで、
  psi(K)（弱コンパクト）までの主脈はこの断片に入っている。
- 展開規則の出典: ~/proofs/Koteitan_バシク行列の数式的定義 _ 巨大数研究 Wiki _ Fandom.html
  の BM4 の節。**上昇行列 A_xy を含む**（悪い部分の列は、バッドルートが行 y の木で
  祖先であるときだけ行 y が上昇する）。
- 兄弟プロジェクト: ~/proofs/lean-yapss（2 行、停止性証明完成）。設計・証明術はそちらに倣う。
  記法の設計論は lean-yapss セッションで議論済み（添字は自然数の対、番地は創発、
  P進大好きbot 氏の BMOCF と同型の方針）。
- 実行可能モデル: tools/trio.py（一般行数 BM4）、検証: tools/verify_trio.py。
  既知の検証結果: ペア規則との一致（273 標準形）、psi(I) 行列の塔展開がシートと一致、
  標準形 3556 個で A 行列は一様上昇と一致（A ≡ 1 予想、2 行の場合の類推）。

# git rule
- git commit は自由（緑のマイルストーンで自動的に）。git push はリモート設定後にユーザーの指示で。
- git add -A / git add . は禁止。

# 注意事項
- ユーザーのターンのときは文末に👤、ツール待ち・エージェント待ちのときは🤖。
- AskUserQuestion ツールは使わず、直接チャットで質問する。
- 会話は日本語（ユーザーは英語で話す）。チャットの数式は ASCII（TeX は使わない）。

# 自律性
- lean-yapss と同じ: できるだけ自律的に方針を選択し、証明完成まで自走する。
- 「進めてよいですか？」だけの質問は禁止。選択肢が 1 つなら自動選択。
